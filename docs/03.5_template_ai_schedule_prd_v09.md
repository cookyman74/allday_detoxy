# Allday Detoxy 3.5차 고도화 PRD (v0.9)
## 템플릿 기반 예약 설정 및 위치 충돌 해소

> **Note**: 본 문서는 3차 고도화(v0.8) 완료 후, 사용자 경험 개선을 위한 3.5차 고도화 계획입니다.
> 기존 4차 고도화 PRD는 `04_template_ai_schedule_prd.md`를 참조하세요.

- **버전**: v0.9 (3차 고도화 v0.8 기반)
- **선행 작업**: 3차 고도화 (v0.8) 완료 필수 ✅
- **예상 기간**: 2주
- **최종 수정일**: 2025-11-02
- **관련 프로세스**: 
  - [예약설정 프로세스](./03.5_예약설정 프로세스.md)
  - [스케줄가동 프로세스](./03.5_스케쥴가동프로세스.md)

---

## 1. 배경 및 목표

### 1.1 배경

**3차 고도화 달성** (2025-10-31):
- ✅ 위치 기반 컨텍스트 + 시간 기반 스케줄 복합 시나리오 구현
- ✅ ScheduleGroup을 통한 시간표 관리
- ✅ 위치 진입/이탈 시 자동 활성화/비활성화
- ✅ QuickCreateScheduleDialog 구현 (빈 시간표 생성)

**남은 과제**:
1. **복잡한 설정 흐름**: 시간표를 만들려면 여러 화면을 왔다 갔다 해야 함
2. **템플릿 부재**: "매일", "평일", "주말", "집", "학교" 등 공통 패턴 지원 필요
3. **커스텀 시간대 직접 입력 불가**: QuickCreateScheduleDialog에서 시간대를 직접 추가할 수 없음
4. **위치 충돌 미해소**: 여러 위치가 겹칠 때 어떤 스케줄을 우선할지 불명확
5. **"어디서나 적용" 개념 부재**: 위치 무관하게 시간만으로 동작하는 스케줄 설정 불명확

**사용자 피드백**:
> "매일 같은 시간에 집중하는 템플릿이 있으면 좋겠어요"
> "집, 회사, 도서관이 겹칠 때 어떤 게 우선인가요?"
> "위치 없이 **시간만 설정**하고 싶어요" (핵심 니즈!)

### 1.2 목표

**주요 목표**:
1. **템플릿 기반 설정**: 매일/평일/주말/집/학교 등 6개 기본 템플릿 제공
2. **커스텀 직접 추가**: QuickCreateScheduleDialog에서 시간대 직접 입력 가능
3. **위치 충돌 해소**: 면적→히스테리시스→거리→최근수정 우선순위 적용
4. **"어디서나 적용" 스케줄 지원**: 위치 무관 시간 기반 스케줄 ("시간만 설정") 명확화
5. **설정 흐름 간소화**: 클릭 3회 이내로 완성된 시간표 생성

**부가 목표**:
- 템플릿 커스터마이징 (시간대, 요일, 강도 수정)
- 템플릿 미리보기
- 나만의 템플릿 저장

### 1.3 성공 지표

**기능 지표**:
- 템플릿 사용률 ≥ 60% (시간표 생성자 대비)
- 커스텀 시간대 직접 입력 사용률 ≥ 30%
- 시간표 설정 완료율 ≥ 85% (설정 시작자 대비)

**사용자 경험**:
- 시간표 설정 완료 시간 ≤ 90초 (템플릿 사용 시)
- 위치 충돌 발생 시 정확도 ≥ 95% (의도한 위치 활성화)
- 사용자 설정 중단율 ≤ 10%

**비즈니스 지표**:
- 시간표 기능 사용자 증가율 ≥ 50% (v0.8 대비)
- 주간 활성 사용자(WAU) 증가율 ≥ 20%
- 사용자 리텐션 7일 ≥ 55%

### 1.4 향후 작업 (Out of Scope for v0.9)

**데이터 모델 확장** (별도 마일스톤 예정):
- `updatedAt` 필드 추가 (ScheduleGroup, LocationBasedAutoRun, TimeBasedAutoRun)
  - **현재 상태**: 우선순위 규칙에서 `createdAt` 사용 (TODO 주석 명시)
  - **계획**: v1.0 또는 별도 스키마 업데이트 시 추가
  - **영향**: 충돌 해소 및 "어디서나 적용" 스케줄 우선순위 정확도 향상

**AI 기반 추천** (4차 고도화 예정):
- 사용자 패턴 학습 기반 시간대 추천
- 위치 기반 자동 템플릿 제안

**나만의 템플릿 저장** (커뮤니티 기능):
- 커스텀 템플릿 저장/공유
- 인기 템플릿 추천

---

## 2. 범위

### 2.1 포함

**예약 설정 프로세스** (기반: `03.5_예약설정 프로세스.md`):

#### 2.1.1 템플릿 설정
- **매일 템플릿**: 매일 동일 시간 (예: 9-12시, 14-18시)
- **평일 템플릿**: 월-금 동일 시간
- **주말 템플릿**: 토-일 동일 시간
- **집 템플릿**: 위치(집) + 시간대
- **학교 템플릿**: 위치(학교) + 시간대
- **회사 템플릿**: 위치(회사) + 시간대

각 템플릿:
- 기본 시간대 제공 (사용자 수정 가능)
- 기본 요일 설정 (사용자 수정 가능)
- 위치 템플릿은 주소/좌표 + 반경(기본 100m)

#### 2.1.2 커스텀 추가 (+)
- **위치 없음**: 제목/설명 + 시간대 직접 입력 (예: 09-11, 13-14:30, 19-22)
- **위치 있음**: 주소 검색/선택 + 반경 + 제목/설명 + 시간대 직접 입력

**중요**: QuickCreateScheduleDialog 확장으로 시간대 직접 입력 지원!

#### 2.1.3 스케줄 가동 프로세스 (기반: `03.5_스케쥴가동프로세스.md`)

**위치 충돌 해소 규칙**:
1. **면적(반경) 우선**: 반경이 작은 위치 우선 (더 구체적)
2. **최근 활성 유지 (히스테리시스)**: 직전 활성 위치 120초간 유지 (떨림 방지)
3. **거리 우선**: 가장 가까운 위치 우선
4. **최근 수정 우선**: 최근 수정한 위치 우선

**"어디서나 적용" 스케줄** (구 용어: 비위치 스케줄):
- 위치 무관 시간 기반 스케줄 ("**시간만 설정**")
- 어느 장소에서든 지정된 시간에 집중모드 자동 실행
- 사용자가 어떤 위치 기반 스케줄에도 속하지 않을 때 적용
- "매일", "평일", "주말" 템플릿이 "어디서나 적용"으로 동작

### 2.2 3차 고도화에서 구현된 기반 (v0.8)

**QuickCreateScheduleDialog** ✅:
- 위치 설정 중 빠른 시간표 생성
- 최소 입력(이름만)으로 생성
- 자동 선택 및 활성화

**3.5차 고도화에서 확장 예정**:
- **Option 1: 템플릿 선택** (신규) 🆕
  - 6개 기본 템플릿 제공
  - 템플릿 미리보기
  - 즉시 적용 (시간대 자동 생성)
  
- **Option 2: 커스텀 추가** (신규) 🎯
  - 시간대 직접 추가 UI
  - [+ 시간대 추가] 버튼
  - 시간/기간/강도 개별 설정
  
- **Option 3: AI 추천** (4차 고도화로 연기)

### 2.3 제외 (4차 고도화 이후로 연기)

- **AI 기반 추천**: 과거 패턴 분석 및 최적 시간표 추천
- **커뮤니티 템플릿**: 다른 사용자의 시간표 공유
- **고급 템플릿**: 시험 기간, 프로젝트 기간 등 상황별 템플릿
- **템플릿 공유**: Export/Import 기능
- **성과 추적**: 시간표 효과 분석

---

## 3. 사용자 흐름 (프로세스 기반 설계)

### 3.1 예약 설정 흐름 (기반: `03.5_예약설정 프로세스.md`)

```
[예약 설정 페이지 열기]
  ↓
┌─────────────────────────────┐
│ 설정 방식 선택              │
├─────────────────────────────┤
│ 📋 템플릿 선택              │ ← Option 1
│ ➕ 커스텀 추가              │ ← Option 2
└─────────────────────────────┘
```

#### **Option 1: 템플릿 선택**

```
템플릿 목록 보기
  ↓
┌─────────────────────┐
│ 매일                │ (위치 없음)
│ 평일                │ (위치 없음)
│ 주말                │ (위치 없음)
│ 집                  │ (위치 있음)
│ 학교                │ (위치 있음)
│ 회사                │ (위치 있음)
└─────────────────────┘
  ↓ 선택
┌─────────────────────────────────┐
│ 템플릿 미리보기                 │
│                                 │
│ 🏢 회사 (예시)                  │
│ ━━━━━━━━━━━━━━━━━━━━━━━        │
│ 위치: 서울시 강남구 테헤란로    │
│ 반경: 100m (수정 가능)          │
│                                 │
│ 시간대:                         │
│ • 10:00 (90분, 표준)            │
│ • 14:00 (60분, 완전 차단)       │
│ • 16:00 (45분, 표준)            │
│                                 │
│ 요일: 월-금                     │
│                                 │
│ [수정] [즉시 적용]              │
└─────────────────────────────────┘
  ↓ 즉시 적용
완성된 시간표 생성 ✅
```

**사용자 수정 가능**:
- 위치: 주소/좌표 변경
- 반경: 50m ~ 500m
- 시간대: 추가/삭제/수정
- 요일: 선택/해제
- 강도: 표준/완전 차단/중간

#### **Option 2: 커스텀 추가**

```
커스텀 스케줄 생성
  ↓
┌─────────────────────────┐
│ 위치 설정 여부          │
├─────────────────────────┤
│ ⚪ 위치 없음            │
│ ⚪ 위치 있음            │
└─────────────────────────┘
```

**2-A: 시간만 설정** ("어디서나 적용")
```
제목/설명 입력
  ↓
시간대 직접 입력 🎯
┌─────────────────────────────┐
│ [+ 시간대 추가]             │
├─────────────────────────────┤
│ • 09:00 (120분, 표준)       │
│   [편집] [삭제]             │
│ • 13:00 (90분, 완전 차단)   │
│   [편집] [삭제]             │
│ • 19:00 (180분, 중간)       │
│   [편집] [삭제]             │
└─────────────────────────────┘
  ↓
요일 선택 (월화수목금토일)
  ↓
[저장] → "어디서나 적용" 스케줄 생성 ✅
(위치 무관, 어느 장소에서든 실행)
```

**2-B: 위치 있음**
```
주소/좌표 검색 → 선택
  ↓
반경 설정 (기본 100m)
  ↓
제목/설명 입력
  ↓
시간대 직접 입력 🎯
(위와 동일한 UI)
  ↓
요일 선택
  ↓
[저장] → 위치 스케줄 생성 ✅
```

### 3.2 QuickCreateScheduleDialog 확장 (위치 설정 중 빠른 생성)

**현재 (v0.8)**:
```kotlin
QuickCreateScheduleDialog(
    onDismiss = { ... },
    onConfirm = { scheduleName -> 
        // 빈 시간표 생성 (문제: 시간대 없음!)
    },
    locationLabel = "회사"
)
```

**3.5차 고도화 (v0.9)**:
```kotlin
QuickCreateScheduleDialog(
    onDismiss = { ... },
    onConfirm = { scheduleName, mode, data -> 
        when (mode) {
            TEMPLATE -> applyTemplate(data.templateId)
            CUSTOM -> createWithTimeSlots(data.timeSlots)
        }
    },
    locationLabel = "회사",
    showTemplateOption = true,  // 🆕
    showCustomOption = true     // 🆕 (시간대 직접 추가)
)
```

**UI 구조**:
```
┌─────────────────────────────────────┐
│ 시간표 만들기                       │
├─────────────────────────────────────┤
│ 이름: [회사 시간표___________]      │
│                                     │
│ 시작 방법 선택:                     │
│ ⚪ 템플릿으로 시작 🆕               │
│ ⚪ 시간대 직접 추가 🎯              │
├─────────────────────────────────────┤
│ [템플릿 선택] 또는 [시간대 추가 UI]│
└─────────────────────────────────────┘
```

### 3.3 시간대 직접 추가 UI (핵심!)

```
┌───────────────────────────────────┐
│ 시간대 목록                       │
├───────────────────────────────────┤
│ [비어있음]                        │
│                                   │
│ [+ 시간대 추가] 버튼              │
└───────────────────────────────────┘
  ↓ 클릭
┌───────────────────────────────────┐
│ 시간대 입력                       │
├───────────────────────────────────┤
│ 시작 시간: [10] : [00]            │
│ 기간(분): [90_________]           │
│ 차단 강도: ⚪표준 ⚪중간 ⚪완전   │
│                                   │
│ [취소] [추가]                     │
└───────────────────────────────────┘
  ↓ 추가
┌───────────────────────────────────┐
│ 시간대 목록                       │
├───────────────────────────────────┤
│ • 10:00 (90분, 표준)              │
│   [편집] [삭제]                   │
│                                   │
│ [+ 시간대 추가]                   │
└───────────────────────────────────┘
  ↓ 추가 완료 후
[저장] 버튼 활성화 (시간대 1개 이상 필수)
```

### 3.4 위치 충돌 해소 시나리오 (기반: `03.5_스케쥴가동프로세스.md`)

**시나리오: 회사와 카페가 100m 거리**

```
집 (반경 300m, 비활성)
  ↓ 출근
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
카페 (반경 200m)  회사 (반경 100m)
    ← 100m 거리 →
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

사용자가 회사 건물에 있을 때:
  1. 후보: 카페, 회사 (둘 다 반경 내)
  2. 면적 우선 적용: 회사 반경(100m) < 카페 반경(200m)
  3. 결과: "회사" 스케줄 활성화 ✅

사용자가 카페로 이동:
  1. 후보: 카페만 (회사는 반경 밖)
  2. 결과: "카페" 스케줄 활성화 ✅

사용자가 다시 회사-카페 중간:
  1. 후보: 카페, 회사
  2. 히스테리시스 확인: 120초 이내 + 직전="카페"
  3. 결과: "카페" 유지 (떨림 방지) ✅
  4. 120초 경과 후: 면적 우선 → "회사" 전환
```

### 3.5 "어디서나 적용" 스케줄 동작

```
사용자가 집/회사/학교 등 어떤 위치 기반 스케줄에도 속하지 않을 때:
  ↓
"어디서나 적용" 스케줄 필터링 (요일/시간)
  ↓
┌───────────────────────────────────┐
│ "매일 집중" 스케줄                │
│ - 위치: 없음 (어디서나 OK)        │
│ - 시간: 09-12, 14-18              │
│ - 요일: 월-일                     │
│ - 적용 가능? ✅                   │
└───────────────────────────────────┘
  ↓
집중모드 ON ("어디서나 적용" 스케줄 실행)
```

---

## 4. 주요 기능 요구사항

### 4.1 템플릿 시스템

#### 4.1.1 기본 템플릿 (6개)

| 템플릿 ID | 이름 | 위치 | 기본 시간대 | 기본 요일 | 설명 |
|----------|------|------|-------------|----------|------|
| `daily` | 매일 | 없음 | 09-12, 14-18 | 월-일 | 매일 같은 시간 집중 |
| `weekday` | 평일 | 없음 | 09-12, 14-18 | 월-금 | 평일만 집중 |
| `weekend` | 주말 | 없음 | 10-12, 14-17 | 토-일 | 주말 집중 |
| `home` | 집 | 있음 | 19-22 | 월-일 | 집에서 저녁 시간 |
| `school` | 학교 | 있음 | 09-12, 14-17 | 월-금 | 학교에서 공부 |
| `office` | 회사 | 있음 | 10-12, 14-16 | 월-금 | 회사에서 업무 |

#### 4.1.2 템플릿 데이터 구조

```kotlin
data class ScheduleTemplate(
    val id: String,              // "daily", "weekday", ...
    val name: String,            // "매일", "평일", ...
    val description: String,     // 설명
    val iconType: String,        // "HOME", "WORK", "STUDY"
    val colorHex: String,        // "#4CAF50"
    val hasLocation: Boolean,    // 위치 필요 여부
    val defaultRadius: Int = 100, // 기본 반경 (위치 템플릿만)
    val timeSlots: List<TimeSlot>, // 기본 시간대
    val defaultDays: List<DayOfWeek> // 기본 요일
)

data class TimeSlot(
    val startHour: Int,          // 0-23
    val startMinute: Int,        // 0-59
    val durationMinutes: Int,    // 15-720
    val presetType: String       // "STANDARD", "COMPLETE", "MEDIUM"
)
```

#### 4.1.3 템플릿 미리보기

```kotlin
@Composable
fun TemplatePreviewDialog(
    template: ScheduleTemplate,
    onDismiss: () -> Unit,
    onApply: () -> Unit,
    onCustomize: () -> Unit
) {
    AlertDialog(
        title = { 
            Row {
                Icon(getIconForType(template.iconType), null)
                Spacer(Modifier.width(8.dp))
                Text(template.name)
            }
        },
        text = {
            Column {
                Text(template.description)
                
                Spacer(Modifier.height(16.dp))
                
                if (template.hasLocation) {
                    Text("📍 위치: 설정 필요")
                    Text("반경: ${template.defaultRadius}m")
                    Spacer(Modifier.height(8.dp))
                }
                
                Text("⏰ 시간대:")
                template.timeSlots.forEach { slot ->
                    Text("  • ${formatTime(slot)} (${slot.durationMinutes}분)")
                }
                
                Spacer(Modifier.height(8.dp))
                
                Text("📅 요일: ${formatDays(template.defaultDays)}")
            }
        },
        confirmButton = {
            Row {
                TextButton(onClick = onCustomize) {
                    Text("수정 후 적용")
                }
                Button(onClick = onApply) {
                    Text("즉시 적용")
                }
            }
        },
        dismissButton = {
            TextButton(onClick = onDismiss) {
                Text("취소")
            }
        }
    )
}
```

### 4.2 커스텀 시간대 직접 추가

#### 4.2.1 TimeSlotInputDialog

```kotlin
@Composable
fun TimeSlotInputDialog(
    existingSlot: TimeSlot? = null,
    onDismiss: () -> Unit,
    onConfirm: (TimeSlot) -> Unit
) {
    var hour by remember { mutableStateOf(existingSlot?.startHour ?: 9) }
    var minute by remember { mutableStateOf(existingSlot?.startMinute ?: 0) }
    var duration by remember { mutableStateOf(existingSlot?.durationMinutes ?: 90) }
    var preset by remember { mutableStateOf(existingSlot?.presetType ?: "STANDARD") }
    
    AlertDialog(
        title = { Text(if (existingSlot == null) "시간대 추가" else "시간대 수정") },
        text = {
            Column {
                // 시작 시간
                Row {
                    Text("시작 시간:")
                    Spacer(Modifier.width(16.dp))
                    NumberPicker(
                        value = hour,
                        onValueChange = { hour = it },
                        range = 0..23
                    )
                    Text(":")
                    NumberPicker(
                        value = minute,
                        onValueChange = { minute = it },
                        range = 0..59,
                        step = 15
                    )
                }
                
                Spacer(Modifier.height(16.dp))
                
                // 기간
                Text("기간(분):")
                Slider(
                    value = duration.toFloat(),
                    onValueChange = { duration = it.toInt() },
                    valueRange = 15f..720f,
                    steps = 47  // 15분 단위
                )
                Text("${duration}분 (${formatDuration(duration)})")
                
                Spacer(Modifier.height(16.dp))
                
                // 차단 강도
                Text("차단 강도:")
                Row {
                    PresetChip(
                        text = "표준",
                        selected = preset == "STANDARD",
                        onClick = { preset = "STANDARD" }
                    )
                    PresetChip(
                        text = "중간",
                        selected = preset == "MEDIUM",
                        onClick = { preset = "MEDIUM" }
                    )
                    PresetChip(
                        text = "완전",
                        selected = preset == "COMPLETE",
                        onClick = { preset = "COMPLETE" }
                    )
                }
            }
        },
        confirmButton = {
            Button(onClick = {
                onConfirm(TimeSlot(hour, minute, duration, preset))
            }) {
                Text(if (existingSlot == null) "추가" else "수정")
            }
        },
        dismissButton = {
            TextButton(onClick = onDismiss) {
                Text("취소")
            }
        }
    )
}
```

#### 4.2.2 QuickCreateScheduleDialog 내 시간대 목록 UI

```kotlin
@Composable
fun QuickCreateScheduleDialog(
    onDismiss: () -> Unit,
    onConfirm: (name: String, mode: CreationMode, data: Any) -> Unit,
    locationLabel: String = ""
) {
    var name by remember { mutableStateOf("$locationLabel 시간표") }
    var mode by remember { mutableStateOf(CreationMode.CUSTOM) }  // 기본값: CUSTOM (Phase 1 우선)
    
    // 커스텀 모드 상태
    var timeSlots by remember { mutableStateOf(listOf<TimeSlot>()) }
    var showTimeSlotInput by remember { mutableStateOf(false) }
    
    // 템플릿 모드 상태 (Phase 2)
    var selectedTemplate by remember { mutableStateOf<ScheduleTemplate?>(null) }
    var showTemplateSelector by remember { mutableStateOf(false) }
    
    AlertDialog(
        title = { Text("시간표 만들기") },
        text = {
            Column {
                // 💡 안내 카드
                Card(
                    colors = CardDefaults.cardColors(
                        containerColor = MaterialTheme.colorScheme.secondaryContainer
                    )
                ) {
                    Text(
                        text = "💡 시간대를 직접 추가하거나 템플릿을 선택하세요.",
                        style = MaterialTheme.typography.bodySmall,
                        color = MaterialTheme.colorScheme.onSecondaryContainer,
                        modifier = Modifier.padding(12.dp)
                    )
                }
                
                Spacer(Modifier.height(16.dp))
                
                // 시간표 이름
                OutlinedTextField(
                    value = name,
                    onValueChange = { name = it },
                    label = { Text("시간표 이름") }
                )
                
                Spacer(Modifier.height(16.dp))
                
                // 시작 방법 선택
                Text("시작 방법:")
                
                RadioButtonRow(
                    text = "✏️ 시간대 직접 추가",
                    selected = mode == CreationMode.CUSTOM,
                    onClick = { mode = CreationMode.CUSTOM }
                )
                
                RadioButtonRow(
                    text = "📋 템플릿으로 시작",
                    selected = mode == CreationMode.TEMPLATE,
                    onClick = { mode = CreationMode.TEMPLATE }
                )
                
                Spacer(Modifier.height(16.dp))
                
                // 모드별 UI
                when (mode) {
                    CreationMode.CUSTOM -> {
                        // 🎯 시간대 직접 추가 UI (Phase 1)
                        Card {
                            Column(Modifier.padding(8.dp)) {
                                Text(
                                    text = "시간대 목록",
                                    style = MaterialTheme.typography.titleSmall
                                )
                                
                                if (timeSlots.isEmpty()) {
                                    Text(
                                        text = "시간대를 추가해주세요",
                                        style = MaterialTheme.typography.bodySmall,
                                        color = MaterialTheme.colorScheme.onSurfaceVariant
                                    )
                                } else {
                                    timeSlots.forEach { slot ->
                                        TimeSlotItem(
                                            slot = slot,
                                            onEdit = { /* ... */ },
                                            onDelete = { timeSlots = timeSlots - slot }
                                        )
                                    }
                                }
                                
                                Spacer(Modifier.height(8.dp))
                                
                                OutlinedButton(
                                    onClick = { showTimeSlotInput = true },
                                    modifier = Modifier.fillMaxWidth()
                                ) {
                                    Icon(Icons.Default.Add, null)
                                    Spacer(Modifier.width(4.dp))
                                    Text("시간대 추가")
                                }
                            }
                        }
                    }
                    CreationMode.TEMPLATE -> {
                        // 📋 템플릿 선택 UI (Phase 2)
                        Card {
                            Column(Modifier.padding(12.dp)) {
                                if (selectedTemplate == null) {
                                    Text(
                                        text = "템플릿을 선택하세요",
                                        style = MaterialTheme.typography.bodySmall,
                                        color = MaterialTheme.colorScheme.onSurfaceVariant
                                    )
                                    
                                    Spacer(Modifier.height(8.dp))
                                    
                                    OutlinedButton(
                                        onClick = { showTemplateSelector = true },
                                        modifier = Modifier.fillMaxWidth()
                                    ) {
                                        Icon(Icons.Default.List, null)
                                        Spacer(Modifier.width(4.dp))
                                        Text("템플릿 선택")
                                    }
                                } else {
                                    SelectedTemplateCard(
                                        template = selectedTemplate!!,
                                        onClear = { selectedTemplate = null }
                                    )
                                }
                            }
                        }
                        
                        // TemplateSelectionBottomSheet
                        if (showTemplateSelector) {
                            TemplateSelectionBottomSheet(
                                onDismiss = { showTemplateSelector = false },
                                onTemplateSelected = { template ->
                                    selectedTemplate = template
                                    showTemplateSelector = false
                                }
                            )
                        }
                    }
                }
            }
        },
        confirmButton = {
            Button(
                onClick = {
                    when (mode) {
                        CreationMode.CUSTOM -> onConfirm(name, mode, timeSlots)
                        CreationMode.TEMPLATE -> onConfirm(name, mode, selectedTemplate!!)
                    }
                },
                enabled = name.isNotBlank() && when (mode) {
                    CreationMode.CUSTOM -> timeSlots.isNotEmpty()
                    CreationMode.TEMPLATE -> selectedTemplate != null
                }
            ) {
                Text("만들기")
            }
        },
        dismissButton = {
            TextButton(onClick = onDismiss) {
                Text("취소")
            }
        }
    )
    
    // 시간대 입력 다이얼로그
    if (showTimeSlotInput) {
        TimeSlotInputDialog(
            onDismiss = { showTimeSlotInput = false },
            onConfirm = { slot ->
                timeSlots = timeSlots + slot
                showTimeSlotInput = false
            }
        )
    }
}
```

### 4.3 위치 충돌 해소 엔진

#### 4.3.1 LocationConflictResolver

```kotlin
@Singleton
class LocationConflictResolver @Inject constructor(
    private val locationDao: LocationBasedAutoRunDao,
    @ApplicationContext private val context: Context
) {
    companion object {
        private const val TAG = "ConflictResolver"
        private const val HYSTERESIS_SECONDS = 120  // 2분
    }
    
    private var lastActiveLocationId: String? = null
    private var lastActivationTime: Long = 0
    
    /**
     * 위치 충돌 해소 규칙 (기반: 스케쥴가동프로세스.md):
     * 1. 면적(반경) 우선
     * 2. 최근 활성 유지 (히스테리시스 120초)
     *    - 새 위치 전환 시 타이머 리셋
     *    - 만료 시 즉시 재평가
     * 3. 거리 우선
     * 4. 최근 수정(updatedAt) 우선
     */
    suspend fun resolveConflict(
        currentLocation: android.location.Location,
        candidates: List<LocationBasedAutoRun>
    ): LocationBasedAutoRun? {
        
        if (candidates.isEmpty()) {
            Log.d(TAG, "No candidates")
            return null
        }
        
        if (candidates.size == 1) {
            Log.d(TAG, "Single candidate: ${candidates.first().label}")
            return candidates.first()
        }
        
        Log.d(TAG, "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
        Log.d(TAG, "🔍 Conflict Resolution Start")
        
        // 1. 면적 우선: 반경이 가장 작은 위치들만 남김
        val minRadius = candidates.minOf { it.radiusMeters }
        var filtered = candidates.filter { it.radiusMeters == minRadius }
        
        Log.d(TAG, "📐 Step 1: Area Priority (min radius: ${minRadius}m)")
        
        if (filtered.size == 1) {
            Log.d(TAG, "✅ Winner: ${filtered.first().label} (area)")
            return filtered.first()
        }
        
        // 2. 히스테리시스: 직전 활성 위치가 후보에 포함되고, 120초 이내면 유지
        val lastActive = lastActiveLocationId
        val timeSinceActivation = (System.currentTimeMillis() - lastActivationTime) / 1000
        
        Log.d(TAG, "⏱️ Step 2: Hysteresis")
        Log.d(TAG, "  Last active: $lastActive, Time since: ${timeSinceActivation}s")
        
        if (lastActive != null && timeSinceActivation <= HYSTERESIS_SECONDS) {
            val lastActiveInCandidates = filtered.find { it.id == lastActive }
            if (lastActiveInCandidates != null) {
                Log.d(TAG, "✅ Winner: ${lastActiveInCandidates.label} (hysteresis)")
                return lastActiveInCandidates
            } else {
                Log.d(TAG, "  ⚠️ Last active not in candidates → reset hysteresis")
                resetHysteresis()
            }
        } else if (timeSinceActivation > HYSTERESIS_SECONDS) {
            Log.d(TAG, "  ⚠️ Hysteresis expired → reset and re-evaluate")
            resetHysteresis()
        }
        
        // 3. 거리 우선: 가장 가까운 위치
        val distances = filtered.map { location ->
            val targetLocation = android.location.Location("").apply {
                latitude = location.latitude
                longitude = location.longitude
            }
            val distance = currentLocation.distanceTo(targetLocation)
            location to distance
        }
        
        Log.d(TAG, "📏 Step 3: Distance Priority")
        distances.forEach { (loc, dist) ->
            Log.d(TAG, "  - ${loc.label}: ${String.format("%.1f", dist)}m")
        }
        
        val minDistance = distances.minOf { it.second }
        filtered = distances.filter { it.second == minDistance }.map { it.first }
        
        if (filtered.size == 1) {
            Log.d(TAG, "✅ Winner: ${filtered.first().label} (distance)")
            return filtered.first()
        }
        
        // 4. 최근 수정(updatedAt) 우선
        // Note: updatedAt 필드는 향후 추가 예정, 현재는 createdAt 사용
        val winner = filtered.maxByOrNull { it.createdAt }  // TODO: updatedAt으로 변경
        
        Log.d(TAG, "🕐 Step 4: Recent Modification Priority")
        Log.d(TAG, "✅ Winner: ${winner?.label} (recent)")
        Log.d(TAG, "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
        
        return winner
    }
    
    /**
     * 활성화 기록 (히스테리시스용)
     * 새 위치로 전환 시 호출하여 타이머 리셋
     */
    fun recordActivation(locationId: String) {
        val previousLocation = lastActiveLocationId
        lastActiveLocationId = locationId
        lastActivationTime = System.currentTimeMillis()
        
        if (previousLocation != locationId) {
            Log.d(TAG, "📝 Location switched: $previousLocation → $locationId (timer reset)")
        } else {
            Log.d(TAG, "📝 Same location re-activated: $locationId")
        }
    }
    
    /**
     * 히스테리시스 상태 초기화
     * - 직전 활성 위치가 후보에서 제외됨
     * - 히스테리시스 시간 만료
     */
    fun resetHysteresis() {
        lastActiveLocationId = null
        lastActivationTime = 0
        Log.d(TAG, "🔄 Hysteresis reset")
    }
}
```

#### 4.3.2 ScheduleGroupManager 통합

```kotlin
@Singleton
class ScheduleGroupManager @Inject constructor(
    private val repository: ScheduleGroupRepository,
    private val alarmManager: AutoRunAlarmManager,
    private val conflictResolver: LocationConflictResolver,  // 🆕 추가
    @ApplicationContext private val context: Context
) {
    /**
     * 위치 기반 활성화 (충돌 해소 포함)
     */
    suspend fun activateGroupByLocation(
        currentLocation: android.location.Location,
        candidates: List<LocationBasedAutoRun>
    ): Result<Unit> = runCatching {
        // 충돌 해소
        val winner = conflictResolver.resolveConflict(currentLocation, candidates)
            ?: throw IllegalStateException("No suitable location found")
        
        val scheduleGroupId = winner.linkedScheduleGroupId
            ?: throw IllegalStateException("Location has no linked schedule group")
        
        // 시간표 활성화
        activateGroup(scheduleGroupId).getOrThrow()
        
        // 활성화 기록 (히스테리시스용)
        conflictResolver.recordActivation(winner.id)
        
        Log.i(TAG, "✅ Activated schedule group: $scheduleGroupId (location: ${winner.label})")
    }
}
```

### 4.4 "어디서나 적용" 스케줄 처리

#### 4.4.1 NonLocationScheduleManager

**클래스 설명**: "어디서나 적용" 스케줄 관리자 (구 용어: 비위치 스케줄 관리자)

```kotlin
@Singleton
class NonLocationScheduleManager @Inject constructor(
    private val repository: ScheduleGroupRepository,
    private val alarmManager: AutoRunAlarmManager,
    @ApplicationContext private val context: Context
) {
    companion object {
        private const val TAG = "NonLocationScheduleMgr"
    }
    
    /**
     * "어디서나 적용" 스케줄 활성화
     * (사용자가 어떤 위치 기반 스케줄에도 속하지 않을 때)
     * 
     * 우선순위 (기반: 스케쥴가동프로세스.md):
     * 1. 더 구체적인 스케줄 우선 (평일 > 매일)
     * 2. 최근 수정(updatedAt) 우선
     * 3. 생성 순서(createdAt) 우선
     */
    suspend fun activateDefaultPolicy(): Result<Unit> = runCatching {
        // 1. 모든 스케줄 그룹 조회
        val allGroups = repository.getAll().first()
        
        // 2. "어디서나 적용" 스케줄만 필터링 (linkedScheduleGroupId == null)
        val nonLocationGroups = allGroups.filter { group ->
            val linkedLocations = repository.getLinkedLocations(group.id)
            linkedLocations.isEmpty()
        }
        
        if (nonLocationGroups.isEmpty()) {
            Log.d(TAG, "No default policy available")
            return Result.success(Unit)
        }
        
        // 3. 현재 시각/요일에 맞는 스케줄 필터링
        val now = LocalDateTime.now()
        val currentDay = now.dayOfWeek
        val currentTime = now.toLocalTime()
        
        val applicableCandidates = nonLocationGroups.filter { group ->
            val timeSlots = repository.getLinkedTimeBasedAutoRuns(group.id)
            timeSlots.any { slot ->
                // 요일 체크
                val daysEnabled = slot.enabledDays.contains(currentDay)
                
                // 시간대 체크
                val slotStart = LocalTime.of(slot.hour, slot.minute)
                val slotEnd = slotStart.plusMinutes(slot.durationMinutes.toLong())
                val inTimeRange = currentTime in slotStart..slotEnd
                
                daysEnabled && inTimeRange
            }
        }
        
        if (applicableCandidates.isEmpty()) {
            Log.d(TAG, "No applicable default policy for current time/day")
            return Result.success(Unit)
        }
        
        // 4. 우선순위 적용 (2개 이상일 때)
        val selectedPolicy = if (applicableCandidates.size == 1) {
            applicableCandidates.first()
        } else {
            Log.d(TAG, "Multiple applicable schedules: ${applicableCandidates.size}")
            selectByPriority(applicableCandidates)
        }
        
        // 5. 기본 정책 적용
        repository.toggleActiveWithTimestamp(selectedPolicy.id, true, System.currentTimeMillis())
        
        // 6. 알람 등록
        val timeSlots = repository.getLinkedTimeBasedAutoRuns(selectedPolicy.id)
        timeSlots.filter { it.isEnabled }.forEach { slot ->
            alarmManager.scheduleTimeBasedAutoRun(slot)
        }
        
        Log.i(TAG, "✅ Default policy activated: ${selectedPolicy.name}")
    }
    
    /**
     * 비위치 스케줄 우선순위 선택
     * 1. 더 구체적인 스케줄 (평일 > 매일, 주말 > 매일)
     * 2. 최근 수정(updatedAt) 우선 (TODO: updatedAt 필드 추가 시)
     * 3. 생성 순서(createdAt) 우선
     */
    private fun selectByPriority(candidates: List<ScheduleGroup>): ScheduleGroup {
        // 1. 구체성 점수 계산
        val scored = candidates.map { group ->
            val timeSlots = repository.getLinkedTimeBasedAutoRuns(group.id).first()
            val specificityScore = calculateSpecificityScore(timeSlots)
            group to specificityScore
        }
        
        Log.d(TAG, "Specificity scores:")
        scored.forEach { (group, score) ->
            Log.d(TAG, "  - ${group.name}: $score")
        }
        
        val maxScore = scored.maxOf { it.second }
        var filtered = scored.filter { it.second == maxScore }.map { it.first }
        
        if (filtered.size == 1) {
            Log.d(TAG, "Winner: ${filtered.first().name} (specificity)")
            return filtered.first()
        }
        
        // 2. 최근 수정 우선
        // TODO: updatedAt 필드 추가 시 사용
        // filtered = filtered.sortedByDescending { it.updatedAt }
        
        // 3. 생성 순서 우선
        val winner = filtered.maxByOrNull { it.createdAt }!!
        Log.d(TAG, "Winner: ${winner.name} (created at)")
        
        return winner
    }
    
    /**
     * 구체성 점수 계산
     * - 7일 모두 활성화: 1점 (매일)
     * - 5일 활성화: 2점 (평일)
     * - 2일 활성화: 2점 (주말)
     * - 그 외: 3점 (특정 요일만)
     */
    private fun calculateSpecificityScore(timeSlots: List<TimeBasedAutoRun>): Int {
        val allDays = timeSlots.flatMap { it.enabledDays }.distinct()
        
        return when (allDays.size) {
            7 -> 1  // 매일
            5 -> {
                val isWeekday = allDays.containsAll(listOf(
                    DayOfWeek.MONDAY, DayOfWeek.TUESDAY, DayOfWeek.WEDNESDAY,
                    DayOfWeek.THURSDAY, DayOfWeek.FRIDAY
                ))
                if (isWeekday) 2 else 3  // 평일 or 특정 5일
            }
            2 -> {
                val isWeekend = allDays.containsAll(listOf(DayOfWeek.SATURDAY, DayOfWeek.SUNDAY))
                if (isWeekend) 2 else 3  // 주말 or 특정 2일
            }
            else -> 3  // 특정 요일만
        }
    }
}
```

### 4.5 시간대 전환 이벤트 처리

#### 4.5.1 시간대 시작 이벤트

**트리거**: AlarmManager로 등록된 시간대 시작 시각 도달

```kotlin
/**
 * 시간대 시작 이벤트 처리 (기반: 스케쥴가동프로세스.md)
 */
private suspend fun handleTimeSlotStart(autoRunId: String) {
    val autoRun = timeBasedAutoRunDao.getById(autoRunId) ?: return
    
    // 1. 히스테리시스 체크: 위치 전환이 발생했는지 확인
    val currentLocation = getCurrentLocation()
    if (currentLocation != null) {
        val nearbyLocations = findNearbyLocations(currentLocation)
        if (nearbyLocations.isNotEmpty()) {
            // 위치 기반 우선 재평가
            scheduleGroupManager.activateGroupByLocation(
                currentLocation,
                nearbyLocations
            )
            return
        }
    }
    
    // 2. 시간표 활성화 (비위치 스케줄)
    val scheduleGroupId = autoRun.scheduleGroupId
    if (scheduleGroupId != null) {
        val group = scheduleGroupDao.getById(scheduleGroupId)
        if (group != null && !group.isActive) {
            // 시간표 활성화
            scheduleGroupManager.activateGroup(scheduleGroupId)
            Log.i(TAG, "✅ Schedule activated by time slot start: ${autoRun.id}")
        }
    }
    
    // 3. 알람 등록 (시간대 시작)
    alarmManager.scheduleTimeBasedAutoRun(autoRun)
    
    // 4. 집중모드 시작
    startFocusMode(autoRun)
}
```

#### 4.5.2 시간대 종료 이벤트

**트리거**: 집중모드 타이머 종료

```kotlin
/**
 * 시간대 종료 이벤트 처리 (기반: 스케쥴가동프로세스.md)
 */
private suspend fun handleTimeSlotEnd(autoRunId: String) {
    val autoRun = timeBasedAutoRunDao.getById(autoRunId) ?: return
    
    // 1. 집중모드 종료
    endFocusMode(autoRun)
    
    // 2. 다음 시간대 확인
    val scheduleGroupId = autoRun.scheduleGroupId
    if (scheduleGroupId != null) {
        val nextTimeSlots = repository.getLinkedTimeBasedAutoRuns(scheduleGroupId)
        val now = LocalDateTime.now()
        
        val upcomingSlots = nextTimeSlots.filter { slot ->
            val slotTime = LocalTime.of(slot.hour, slot.minute)
            slotTime.isAfter(now.toLocalTime())
        }
        
        if (upcomingSlots.isNotEmpty()) {
            Log.d(TAG, "Next time slot: ${upcomingSlots.first().id}")
            // 다음 시간대 알람 이미 등록되어 있음
        } else {
            Log.d(TAG, "No more time slots today")
            // 오늘 남은 시간대 없음 → 시간표 비활성화
            scheduleGroupManager.deactivateGroup(scheduleGroupId)
        }
    }
    
    // 3. 현재 시각/요일/위치 재평가
    reevaluateCurrentContext()
}
```

#### 4.5.3 위치 반경 진입/이탈 이벤트

**트리거**: Geofencing API 이벤트

```kotlin
/**
 * 위치 반경 진입/이탈 이벤트 처리 (기반: 스케쥴가동프로세스.md)
 */
private suspend fun handleLocationEvent(event: GeofenceEvent) {
    val currentLocation = event.location
    
    // 1. 히스테리시스 체크
    // - 직전 활성 위치가 후보에 포함되는지?
    // - 120초 이내인지?
    val nearbyLocations = findNearbyLocations(currentLocation)
    
    if (nearbyLocations.isEmpty()) {
        // 어떤 위치에도 없음 → "어디서나 적용" 스케줄 적용
        conflictResolver.resetHysteresis()
        nonLocationScheduleManager.activateDefaultPolicy()
        return
    }
    
    // 2. 위치 충돌 해소
    val result = scheduleGroupManager.activateGroupByLocation(
        currentLocation,
        nearbyLocations
    )
    
    if (result.isSuccess) {
        Log.i(TAG, "✅ Location-based schedule activated")
    } else {
        Log.e(TAG, "⚠️ Failed to activate schedule", result.exceptionOrNull())
        // Fallback: "어디서나 적용" 스케줄 적용
        nonLocationScheduleManager.activateDefaultPolicy()
    }
}
```

#### 4.5.4 이벤트 우선순위

이벤트가 동시에 발생할 때의 우선순위 (높음 → 낮음):

1. **위치 반경 진입/이탈** (최우선)
   - 사용자의 물리적 이동이 가장 중요
   - 히스테리시스 체크 후 즉시 재평가

2. **시간대 시작**
   - 위치 체크 후 시간표 활성화
   - "어디서나 적용" 스케줄 우선순위 적용

3. **시간대 종료**
   - 다음 시간대 확인
   - 컨텍스트 재평가

---

## 5. 구현 우선순위

### Phase 1: 커스텀 시간대 직접 추가 (Week 1, Day 1-5) 🎯

**목적**: 근본 문제 해결 - 사용자가 원하는 시간대를 직접 입력 가능

- [ ] TimeSlotInputDialog 구현 (~150줄)
- [ ] QuickCreateScheduleDialog에 CUSTOM 모드 추가 (~200줄)
- [ ] 시간대 목록 UI (추가/편집/삭제)
- [ ] ScheduleGroupViewModel에 `createWithTimeSlots()` 추가
- [ ] AddLocationAutoRunDialog 통합
- [ ] 빌드 검증

**예상 효과**: 커스텀 시간표 사용자 30% 만족 ✅

### Phase 2: 템플릿 기능 (Week 1, Day 6-7 + Week 2, Day 1-3)

**목적**: 빠른 시작 지원 - 템플릿으로 즉시 적용

- [ ] ScheduleTemplate 데이터 모델 정의
- [ ] DefaultTemplates.kt 생성 (6개 템플릿)
- [ ] TemplateViewModel 구현 (~200줄)
- [ ] TemplateSelectionBottomSheet 구현 (~150줄)
- [ ] TemplatePreviewDialog 구현 (~200줄)
- [ ] QuickCreateScheduleDialog에 TEMPLATE 모드 추가
- [ ] 템플릿 미리보기 및 커스터마이징
- [ ] 빌드 검증

**예상 효과**: 템플릿 사용자 60% 만족 ✅

### Phase 3: 위치 충돌 해소 (Week 2, Day 4-7)

**목적**: 여러 위치 겹칠 때 정확한 스케줄 선택

- [ ] LocationConflictResolver 구현 (~300줄)
- [ ] 면적 우선 로직
- [ ] 히스테리시스 로직 (120초)
- [ ] 거리 우선 로직
- [ ] 최근 수정 우선 로직
- [ ] ScheduleGroupManager 통합
- [ ] GeofenceTransitionsReceiver 수정
- [ ] 충돌 해소 로그 및 Analytics
- [ ] 빌드 검증

**예상 효과**: 위치 충돌 시 정확도 95% ✅

### Phase 4: "어디서나 적용" 스케줄 (Week 2, Day 8-10)

**목적**: 위치 무관 시간 기반 스케줄 자동 적용 ("시간만 설정")

- [ ] NonLocationScheduleManager 구현 (~350줄)
- [ ] "어디서나 적용" 스케줄 필터링 로직
- [ ] 현재 시각/요일 매칭 로직
- [ ] 우선순위 선택 로직 (구체성→최근수정→생성순)
- [ ] 백그라운드 엔진 통합
- [ ] 시간대 전환 이벤트 처리
- [ ] 빌드 검증

**예상 효과**: 어느 장소에서든 시간 기반 집중모드 자동 실행 ✅

---

## 6. 기술 스택

### 6.1 기존 스택 (v0.8)

- Room Database v6
- Geofencing API
- Hilt
- Jetpack Compose
- Kotlin Coroutines & Flow

### 6.2 추가 스택 (v0.9)

- **Location API**: 거리 계산 (`Location.distanceTo()`)
- **JSON Serialization**: 템플릿 데이터 (kotlinx.serialization)
- **Analytics**: 충돌 해소 결과 로깅

### 6.3 성능 최적화

- **히스테리시스**: GPS 떨림 방지 (120초)
- **캐싱**: 마지막 활성 위치 메모리 캐싱
- **효율적 필터링**: 3단계 충돌 해소 (평균 O(n) 복잡도)

---

## 7. 성공 지표 (상세)

### 7.1 기능 지표
- 템플릿 사용률 ≥ 60%
- 커스텀 시간대 직접 입력 사용률 ≥ 30%
- QuickCreateScheduleDialog에서 템플릿/커스텀 사용률 ≥ 90%
- 위치 충돌 해소 정확도 ≥ 95%

### 7.2 사용자 경험
- 위치 + 시간표 설정 완료 시간 ≤ 90초 (템플릿 사용 시)
- 시간표 설정 중단율 ≤ 10%
- 위치 충돌 발생 시 사용자 만족도 ≥ 80%

### 7.3 비즈니스 지표
- 시간표 기능 사용자 +50% (v0.8 대비)
- WAU +20%
- 7일 리텐션 ≥ 55%

---

## 8. 위험 요소 및 대응

### 8.1 위험 요소

1. **GPS 정확도 문제**
   - 실내/지하에서 GPS 오차 증가
   - 대응: 히스테리시스 120초로 떨림 방지

2. **배터리 소모**
   - 위치 추적으로 인한 배터리 소모
   - 대응: Geofencing API 사용 (효율적)

3. **템플릿 부족**
   - 6개 템플릿이 모든 사용자 커버 못할 수 있음
   - 대응: 커스텀 직접 추가 기능 병행 제공

4. **UI 복잡도 증가**
   - 템플릿/커스텀 선택지로 인한 혼란
   - 대응: 명확한 안내 텍스트 및 미리보기

### 8.2 롤백 계획

- 버전별 기능 플래그 관리
- v0.8 호환성 유지 (기존 빈 시간표 생성 동작)
- 문제 발생 시 템플릿/충돌 해소 기능 비활성화 가능

---

## 9. 참조 문서

- [예약설정 프로세스](./03.5_예약설정 프로세스.md) 🆕
- [스케줄가동 프로세스](./03.5_스케쥴가동프로세스.md) 🆕
- [3.5차 고도화 작업계획](./03.5_template_ai_schedule_todolist_v09.md)
- [3차 고도화 PRD](./03_complex_time&location_todolist.md)
- [3차 고도화 작업 기록](../working_history/2025-10-31_3rd_advanced_3.4.md)

---

> **v0.9 설계 철학**: 
> 
> "프로세스 중심 설계로 사용자 멘탈 모델과 시스템 모델을 일치시킵니다. 
> **템플릿으로 빠르게**, **커스텀으로 유연하게**, **충돌 해소로 정확하게**. 
> 3가지 원칙으로 완성도 높은 예약 설정 경험을 제공합니다."
