# Phase 2: 문자열 추출 - 작업 결과서

> **작업 기간**: 2026-01-15 ~ 2026-01-17 (진행중)  
> **작성자**: AI Assistant  
> **상태**: 🟡 진행중

---

##  진행 현황

| 우선순위 | 파일 수 | 완료 | 진행률 |
|----------|---------|------|--------|
| 🔴 High (20개+) | 8 | 4 | 50% |
| 🟠 Medium (10-19개) | 16 | 0 | 0% |
| 🟡 Low (5-9개) | 31 | 0 | 0% |
| ⚪ Minimal (1-4개) | ~40 | 0 | 0% |

---

## ✅ 완료된 작업

### 2026-01-15
- strings.xml 기반 구조 생성 (~160개 문자열)
- MainActivity.kt 문자열 교체 (커밋: `5e92a6c`)

### 2026-01-17: High Priority 파일 작업 (4/8 ~ 7/8)
- **대상 파일**:
  - `AutoRunHistoryScreen.kt` (30 strings)
  - `TimeSlotInputDialog.kt` (29 strings)
  - `LocationEditDialog.kt` (28 strings)
  - `AddTimeBasedAutoRunDialog.kt` (26 strings)
- **변경 내용**:
  - 각 파일의 하드코딩된 한글 문자열을 `strings.xml` 리소스로 추출 및 `stringResource()` 적용.
  - `strings.xml`에 약 80개 이상의 새 문자열 추가 (프리셋, 타이머, 위치 설정 등).
  - 공통 액션(`action_edit`, `action_delete`) 및 요일 문자열 통합.
- **결과**:
  - 모든 High Priority 파일 빌드 검증 완료.
  - `DetoxyCoachRecommender.kt`: Domain Layer의 순수성 유지를 위해 `UiText` Wrapper 패턴 도입 (`core/utils/UiText.kt`).
    - 도메인 로직은 `UiText.StringResource` 반환.
    - UI Layer (`ReportScreen`, `CoachRecommendationDialog` 등)에서 `asString()`으로 변환.
  - Phase 2 High Priority 8/8 파일 처리 완료.

**총 처리**: ~105개 하드코딩 문자열 → stringResource()

---

## 📁 변경된 파일

| 날짜 | 파일 | 설명 |
|------|------|------|
| 01-15 | `strings.xml` | 160개 문자열 추가 |
| 01-15 | `MainActivity.kt` | stringResource() 적용 |
| 01-17 | `DetoxyControlSettingsScreen.kt` | 40개 교체 |
| 01-17 | `ScheduleCreationDialog.kt` | 33개 교체 |
| 01-17 | `AddLocationAutoRunDialog.kt` | 32개 교체 |
| 01-17 | `strings.xml` | 위치/스케줄 관련 ~60개 추가 |

---

## ⚠️ 특이 사항

**도메인 레이어 파일**: `DetoxyCoachRecommender.kt` (39개)는 Compose가 아닌 도메인 로직 파일로 별도 처리 필요

---

### 2026-01-17: Phase 2 진행 (High 완료 / Medium 진행중)
- **High Priority (8/8 완료)**:
  - `AutoRunHistoryScreen.kt` 등 표기되지 않은 나머지 파일들도 이미 이전 세션에서 완료됨.
  - `DetoxyCoachRecommender.kt`: `UiText` 적용 완료.
- **Medium Priority (4/16 완료)**:
  - `ScheduleGroupViewModel.kt`: 에러 메시지 처리를 `UiText`로 리팩토링.
  - `TimeBasedAutoRunCard.kt`, `AutoRunHistoryItem.kt`: UI 문자열 추출 및 JSON 파싱 로직 분리.
  - `ScheduleGroupScreen.kt`: 화면 UI 텍스트 추출 및 `UiText` 핸들링 추가.
- **문자열 리소스 (strings.xml)**:
  - 스케줄 그룹, 자동실행, 히스토리 관련 약 80+개 추가.
- **빌드 검증**:
  - `UiText` 관련 컴파일 에러 및 Compose import 이슈 해결하여 빌드 성공 (`BUILD SUCCESSFUL`).

- **Medium Priority - Part 2 (4/16 추가 완료)**:
  - `ReportScreen.kt`: 통계 타이틀 및 빈 상태 메시지 리소스화.
  - `TimeBasedAutoRunScreen.kt`: 다이얼로그, 버튼, 배터리 정보 카드 등 리소스화.
  - `LocationPermissionDialogs.kt`: 위치 권한 관련 모든 텍스트 리소스화.
  - `TimerScreen.kt`: 타이머 관련 모든 텍스트 및 다이얼로그 전면 교체.

---

## 📁 변경된 파일

| 날짜 | 파일 | 설명 |
|------|------|------|
| 01-17 | `ScheduleGroupViewModel.kt` | UiText 적용 |
| 01-17 | `ScheduleGroupScreen.kt` | 리소스 적용, Import 수정 |
| 01-17 | `TimeBasedAutoRunCard.kt` | 리소스 적용, 로직 분리 |
| 01-17 | `AutoRunHistoryItem.kt` | 리소스 적용 |
| 01-17 | `strings.xml` | Medium Priority 관련 문자열 추가 |
| 01-17 | `ReportScreen.kt` | 리소스 적용 |
| 01-17 | `TimeBasedAutoRunScreen.kt` | 리소스 적용 |
| 01-17 | `LocationPermissionDialogs.kt` | 리소스 적용 |
| 01-17 | `TimerScreen.kt` | 리소스 적용 |

---

## 📋 남은 작업 (Medium Priority 주요 대상)

| 상태 | 파일 | 개수 |
|------|------|------|
| ✅ | `ReportScreen.kt` | 21 |
| ✅ | `TimeBasedAutoRunScreen.kt` | 21 |
| ✅ | `LocationPermissionDialogs.kt` | 21 |
| ✅ | `TimerScreen.kt` | 20 |
