# 작업 기록 2025-10-06_2.3

## 작업 정보
- **작업 번호**: 2.3
- **작업명**: Room 데이터베이스 (Day 9-10)
- **작업 일시**: 2025-10-06
- **작업 상태**: ✅ 완료
- **작업 시간**: 약 40분

## 작업 개요
집중 세션 기록과 사용자 설정을 저장하기 위한 Room 데이터베이스를 구현했습니다. `FocusSession`과 `UserSettings` 엔티티를 생성하고, 각각의 DAO 인터페이스를 구현하였으며, `DetoxyDatabase` 클래스를 통해 데이터베이스를 구성했습니다. Hilt를 통한 의존성 주입도 설정하여 앱 전체에서 데이터베이스를 사용할 수 있도록 했습니다.

## 완료된 작업

### 2.3.1 핵심 엔티티 (MVP 최소) ✅
- **FocusSession 엔티티** (`data/local/entity/FocusSession.kt`)
  - 집중 세션 기록 저장
  - UUID 기반 고유 ID
  - 시작/종료 시간 (Unix timestamp)
  - 목표 시간(분), 성공 여부 추적

- **UserSettings 엔티티** (`data/local/entity/UserSettings.kt`)
  - 사용자 게임화 정보 저장
  - 단일 레코드(ID=1) 관리
  - 총 포인트, 현재 스트릭, 마지막 성공 날짜

### 2.3.2 DAO 인터페이스 ✅
- **FocusSessionDao** (`data/local/dao/FocusSessionDao.kt`)
  - `insert()`: 새 세션 추가
  - `update()`: 세션 업데이트
  - `getTodaySessions()`: 오늘 날짜 세션 조회 (Flow)
  - `getAllSessions()`: 모든 세션 조회 (최신순)
  - `getSessionById()`: 특정 ID 세션 조회
  - `getSuccessfulSessions()`: 성공한 세션만 조회

- **UserSettingsDao** (`data/local/dao/UserSettingsDao.kt`)
  - `getSettings()`: 설정 조회 (Flow)
  - `insert()`: 설정 추가 (REPLACE 전략)
  - `update()`: 설정 업데이트
  - `addPoints()`: 포인트 증가
  - `updateStreak()`: 스트릭 업데이트
  - `resetStreak()`: 스트릭 초기화

### 2.3.3 데이터베이스 구성 ✅
- **DetoxyDatabase** (`data/local/DetoxyDatabase.kt`)
  - FocusSession, UserSettings 엔티티 포함
  - Room Database 추상 클래스
  - sessionDao(), settingsDao() 제공
  - version 1, exportSchema false

### 2.3.4 Hilt 의존성 주입 ✅
- **DatabaseModule 업데이트** (`core/di/DatabaseModule.kt`)
  - `provideDetoxyDatabase()`: Database 싱글톤 인스턴스 제공
  - `provideFocusSessionDao()`: FocusSessionDao 제공
  - `provideUserSettingsDao()`: UserSettingsDao 제공
  - @Singleton으로 앱 전체 생명주기 동안 단일 인스턴스 유지

## 기술적 구현 세부사항

### 1. FocusSession 엔티티
```kotlin
package com.allday.detoxy.data.local.entity

import androidx.room.Entity
import androidx.room.PrimaryKey
import java.util.UUID

@Entity(tableName = "focus_sessions")
data class FocusSession(
    @PrimaryKey
    val id: String = UUID.randomUUID().toString(),

    val startTime: Long,

    val endTime: Long? = null,

    val durationMinutes: Int,

    val success: Boolean = false
)
```

**특징**:
- UUID 기반 고유 ID로 분산 환경에서도 충돌 없음
- Unix timestamp(milliseconds) 사용으로 시간대 문제 해결
- nullable endTime으로 진행 중 세션 표현 가능

### 2. UserSettings 엔티티
```kotlin
package com.allday.detoxy.data.local.entity

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "user_settings")
data class UserSettings(
    @PrimaryKey
    val id: Int = 1,

    val totalPoints: Int = 0,

    val currentStreak: Int = 0,

    val lastSuccessDate: String? = null
)
```

**특징**:
- 단일 레코드(ID=1)로 관리하여 간단한 구조
- YYYY-MM-DD 형식의 날짜 문자열로 스트릭 계산
- 기본값 제공으로 초기화 간소화

### 3. FocusSessionDao
```kotlin
package com.allday.detoxy.data.local.dao

import androidx.room.Dao
import androidx.room.Insert
import androidx.room.Query
import androidx.room.Update
import com.allday.detoxy.data.local.entity.FocusSession
import kotlinx.coroutines.flow.Flow

@Dao
interface FocusSessionDao {

    @Insert
    suspend fun insert(session: FocusSession)

    @Update
    suspend fun update(session: FocusSession)

    @Query("SELECT * FROM focus_sessions WHERE DATE(startTime/1000, 'unixepoch') = DATE('now') ORDER BY startTime DESC")
    fun getTodaySessions(): Flow<List<FocusSession>>

    @Query("SELECT * FROM focus_sessions ORDER BY startTime DESC")
    fun getAllSessions(): Flow<List<FocusSession>>

    @Query("SELECT * FROM focus_sessions WHERE id = :sessionId")
    fun getSessionById(sessionId: String): Flow<FocusSession?>

    @Query("SELECT * FROM focus_sessions WHERE success = 1 ORDER BY startTime DESC")
    fun getSuccessfulSessions(): Flow<List<FocusSession>>
}
```

**핵심 쿼리 로직**:
- `DATE(startTime/1000, 'unixepoch')`: 밀리초를 초로 변환 후 날짜 추출
- `DATE('now')`: SQLite 함수로 현재 날짜 가져오기
- Flow 사용으로 실시간 데이터 변경 감지 및 UI 자동 업데이트

### 4. UserSettingsDao
```kotlin
package com.allday.detoxy.data.local.dao

import androidx.room.Dao
import androidx.room.Insert
import androidx.room.OnConflictStrategy
import androidx.room.Query
import androidx.room.Update
import com.allday.detoxy.data.local.entity.UserSettings
import kotlinx.coroutines.flow.Flow

@Dao
interface UserSettingsDao {

    @Query("SELECT * FROM user_settings WHERE id = 1")
    fun getSettings(): Flow<UserSettings?>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(settings: UserSettings)

    @Update
    suspend fun update(settings: UserSettings)

    @Query("UPDATE user_settings SET totalPoints = totalPoints + :points WHERE id = 1")
    suspend fun addPoints(points: Int)

    @Query("UPDATE user_settings SET currentStreak = :streak, lastSuccessDate = :date WHERE id = 1")
    suspend fun updateStreak(streak: Int, date: String)

    @Query("UPDATE user_settings SET currentStreak = 0 WHERE id = 1")
    suspend fun resetStreak()
}
```

**특징**:
- `OnConflictStrategy.REPLACE`로 초기 설정 생성 시 충돌 방지
- 트랜잭션 기반 포인트 증가로 동시성 문제 해결
- 편의 메소드(addPoints, updateStreak)로 반복 코드 제거

### 5. DetoxyDatabase
```kotlin
package com.allday.detoxy.data.local

import androidx.room.Database
import androidx.room.RoomDatabase
import com.allday.detoxy.data.local.dao.FocusSessionDao
import com.allday.detoxy.data.local.dao.UserSettingsDao
import com.allday.detoxy.data.local.entity.FocusSession
import com.allday.detoxy.data.local.entity.UserSettings

@Database(
    entities = [
        FocusSession::class,
        UserSettings::class
    ],
    version = 1,
    exportSchema = false
)
abstract class DetoxyDatabase : RoomDatabase() {

    abstract fun sessionDao(): FocusSessionDao

    abstract fun settingsDao(): UserSettingsDao
}
```

**설정**:
- version 1: 첫 버전
- exportSchema false: 스키마 파일 생성 비활성화 (MVP 간소화)

### 6. DatabaseModule (Hilt 의존성 주입)
```kotlin
package com.allday.detoxy.core.di

import android.content.Context
import androidx.room.Room
import com.allday.detoxy.data.local.DetoxyDatabase
import com.allday.detoxy.data.local.dao.FocusSessionDao
import com.allday.detoxy.data.local.dao.UserSettingsDao
import dagger.Module
import dagger.Provides
import dagger.hilt.InstallIn
import dagger.hilt.android.qualifiers.ApplicationContext
import dagger.hilt.components.SingletonComponent
import javax.inject.Singleton

@Module
@InstallIn(SingletonComponent::class)
object DatabaseModule {

    @Provides
    @Singleton
    fun provideDetoxyDatabase(
        @ApplicationContext context: Context
    ): DetoxyDatabase {
        return Room.databaseBuilder(
            context,
            DetoxyDatabase::class.java,
            "detoxy_database"
        ).build()
    }

    @Provides
    fun provideFocusSessionDao(database: DetoxyDatabase): FocusSessionDao {
        return database.sessionDao()
    }

    @Provides
    fun provideUserSettingsDao(database: DetoxyDatabase): UserSettingsDao {
        return database.settingsDao()
    }
}
```

**의존성 주입 구조**:
- `@Singleton` + `SingletonComponent`: 앱 전체에서 단일 DB 인스턴스
- `@ApplicationContext`: 애플리케이션 컨텍스트 주입
- DAO는 Database에서 직접 가져와 제공

## 빌드 결과
```bash
$ ./gradlew assembleDebug

BUILD SUCCESSFUL in 9s
39 actionable tasks: 16 executed, 23 up-to-date
```

**경고**:
- 일부 미사용 변수 경고 발생 (기능 동작에는 영향 없음)
- Week 3에서 세션 저장 기능 통합 시 해결 예정

## 생성된 파일
1. **FocusSession.kt** (`data/local/entity/FocusSession.kt`)
   - 집중 세션 엔티티

2. **UserSettings.kt** (`data/local/entity/UserSettings.kt`)
   - 사용자 설정 엔티티

3. **FocusSessionDao.kt** (`data/local/dao/FocusSessionDao.kt`)
   - FocusSession DAO 인터페이스

4. **UserSettingsDao.kt** (`data/local/dao/UserSettingsDao.kt`)
   - UserSettings DAO 인터페이스

5. **DetoxyDatabase.kt** (`data/local/DetoxyDatabase.kt`)
   - Room 데이터베이스 클래스

## 수정된 파일
1. **DatabaseModule.kt** (`core/di/DatabaseModule.kt`)
   - TODO 주석 제거 및 실제 구현체 추가
   - Database, DAO 의존성 주입 메소드 구현

## 다음 단계
- **Week 3 (3.1)**: GamificationManager 구현 및 세션 완료 시 포인트/스트릭 업데이트
- **TimerViewModel 통합**: 타이머 종료 시 FocusSession 저장 로직 추가
- **Repository 패턴**: DAO를 감싸는 Repository 계층 추가 (선택적)

## 커밋 정보
```
[Week 2] 2.3 Room 데이터베이스 구현

- FocusSession 엔티티 구현
  - UUID 기반 고유 ID
  - Unix timestamp 기반 시간 저장
  - 세션 성공 여부 추적

- UserSettings 엔티티 구현
  - 단일 레코드(ID=1) 관리
  - 포인트, 스트릭, 마지막 성공 날짜 저장

- FocusSessionDao 구현
  - insert, update 메소드
  - 오늘 세션, 전체 세션, 성공 세션 조회
  - Flow 기반 반응형 데이터 스트림

- UserSettingsDao 구현
  - 설정 조회 및 업데이트
  - 포인트 증가, 스트릭 관리 편의 메소드
  - OnConflictStrategy.REPLACE로 초기화 간소화

- DetoxyDatabase 구성
  - 2개 엔티티 포함 (FocusSession, UserSettings)
  - version 1, exportSchema false

- DatabaseModule 완성
  - Database 싱글톤 인스턴스 제공
  - FocusSessionDao, UserSettingsDao 의존성 주입

- 빌드 성공 확인
  - BUILD SUCCESSFUL in 9s
  - 39 actionable tasks: 16 executed, 23 up-to-date

관련 작업: docs/00_mvp_allday_detoxy_todolist.md #2.3
```

## 커밋 ID
- 커밋 ID: [여기에 커밋 후 추가]
