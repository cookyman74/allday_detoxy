# 위치 기반 자동 실행 - LocationBasedAutoRunScreen 레이아웃 (Day 14)

## 📋 작업 정보

**작업 일자**: 2025-10-24  
**작업 범위**: [02_advanced_autosetting_todolist.md](../docs/02_advanced_autosetting_todolist.md) § 4.1.1 LocationBasedAutoRunScreen 레이아웃 (Day 14)  
**이전 작업**: [2025-10-23_2nd_advanced_3.3.md](./2025-10-23_2nd_advanced_3.3.md) - 시간 기반 자동 실행 알림 및 액션

---

## 🎯 목표

위치 기반 자동 실행 설정 화면의 UI 레이아웃을 구현합니다.

**주요 기능**:
1. Play Services 미지원 경고 표시
2. 위치 권한 안내 및 요청
3. 등록된 위치 리스트 표시
4. 배터리 영향 안내
5. Empty State 및 최대 개수 제한 안내

---

## ✅ 완료된 작업

### 1. LocationBasedAutoRunRepository 구현

**파일**: `app/src/main/java/com/allday/detoxy/data/repository/LocationBasedAutoRunRepository.kt` (신규, ~95줄)

```kotlin
/**
 * 위치 기반 자동 실행 Repository
 *
 * LocationBasedAutoRun 데이터에 대한 비즈니스 로직과 데이터 액세스를 담당합니다.
 */
@Singleton
class LocationBasedAutoRunRepository @Inject constructor(
    private val dao: LocationBasedAutoRunDao
) {
    fun getAll(): Flow<List<LocationBasedAutoRun>>
    fun getById(id: String): Flow<LocationBasedAutoRun?>
    fun getEnabled(): Flow<List<LocationBasedAutoRun>>
    suspend fun insert(autoRun: LocationBasedAutoRun)
    suspend fun update(autoRun: LocationBasedAutoRun)
    suspend fun deleteById(id: String)
    suspend fun toggleEnabled(id: String, isEnabled: Boolean)
    suspend fun getCount(): Int
    suspend fun getEnabledCount(): Int
}
```

**특징**:
- Singleton 스코프로 앱 전체에서 단일 인스턴스 사용
- DAO를 통한 데이터 액세스 추상화
- Flow를 통한 반응형 데이터 스트림 제공

---

### 2. LocationBasedAutoRunViewModel 구현

**파일**: `app/src/main/java/com/allday/detoxy/presentation/viewmodel/LocationBasedAutoRunViewModel.kt` (신규, ~190줄)

#### 2.1 StateFlow 정의

```kotlin
@HiltViewModel
class LocationBasedAutoRunViewModel @Inject constructor(
    private val repository: LocationBasedAutoRunRepository,
    private val geofenceManager: AutoRunGeofenceManager,
    @ApplicationContext private val context: Context
) : ViewModel() {

    // 위치 기반 자동 실행 리스트
    val locations: StateFlow<List<LocationBasedAutoRun>>

    // Google Play Services 사용 가능 여부
    val playServicesAvailable: StateFlow<Boolean>

    // 위치 권한 상태
    val locationPermissionGranted: StateFlow<Boolean>
    val backgroundLocationPermissionGranted: StateFlow<Boolean>

    // 위치 서비스 활성화 여부
    val locationServiceEnabled: StateFlow<Boolean>

    // 전체 위치 권한 부여 여부
    val hasFullLocationPermission: StateFlow<Boolean>
}
```

#### 2.2 주요 메서드

```kotlin
// 권한 상태 확인
fun checkPermissions()

// 위치 기반 자동 실행 추가 (Geofence 자동 등록)
fun addLocation(location: LocationBasedAutoRun)

// 위치 기반 자동 실행 업데이트 (Geofence 재등록)
fun updateLocation(location: LocationBasedAutoRun)

// 위치 기반 자동 실행 삭제 (Geofence 자동 해제)
fun deleteLocation(locationId: String)

// 활성화/비활성화 토글 (Geofence 등록/해제)
fun toggleLocation(locationId: String, isEnabled: Boolean)
```

**특징**:
- Geofence 등록/해제 자동 처리
- 권한 상태 실시간 반영
- Play Services 사용 가능 여부 체크

---

### 3. UI 컴포넌트 구현

#### 3.1 PlayServicesWarningCard

**파일**: `app/src/main/java/com/allday/detoxy/presentation/ui/autorun/components/PlayServicesWarningCard.kt` (신규, ~80줄)

```kotlin
/**
 * Play Services 미지원 경고 카드
 *
 * Google Play Services가 미지원되는 기기에서 표시됩니다.
 * 위치 기반 자동 실행 기능을 사용할 수 없음을 안내하고 대체 방법을 제시합니다.
 */
@Composable
fun PlayServicesWarningCard(
    onNavigateToTimeBased: () -> Unit
)
```

**UI 구성**:
- ⚠️ 아이콘 + 경고 메시지
- 대체 방법 안내 (시간 기반 자동 실행)
- "시간 기반 설정으로 이동" 버튼 (FilledTonalButton)
- ErrorContainer 색상 사용 (시각적 경고)

---

#### 3.2 LocationPermissionCard

**파일**: `app/src/main/java/com/allday/detoxy/presentation/ui/autorun/components/LocationPermissionCard.kt` (신규, ~140줄)

```kotlin
/**
 * 위치 권한 안내 카드
 *
 * 위치 권한이 없을 때 표시되는 카드입니다.
 * 권한의 필요성을 설명하고 권한 설정 화면으로 이동하는 버튼을 제공합니다.
 */
@Composable
fun LocationPermissionCard(
    hasLocationPermission: Boolean,
    hasBackgroundPermission: Boolean,
    onRequestPermission: () -> Unit,
    onNavigateToTimeBased: () -> Unit
)
```

**UI 구성**:
- 📍 아이콘 + 권한 필요성 설명
- 현재 권한 상태 표시:
  - ✅ 정확한 위치: 허용됨
  - ❌ 백그라운드 위치: 거부됨
- 버튼 2개 (가로 배치):
  - "시간 기반 사용" (FilledTonalButton)
  - "권한 설정하기" (Button)

**권한 상태에 따른 표시**:
- 정확한 위치 없음: "📍 위치 권한이 필요해요"
- 백그라운드 위치 없음: "📍 백그라운드 위치 권한 필요" + "항상 허용" 안내
- 모든 권한 있음: 카드 표시 안 함

---

#### 3.3 LocationBasedAutoRunCard

**파일**: `app/src/main/java/com/allday/detoxy/presentation/ui/autorun/components/LocationBasedAutoRunCard.kt` (신규, ~190줄)

```kotlin
/**
 * 위치 기반 자동 실행 카드
 *
 * 등록된 위치 기반 자동 실행 정보를 표시하는 카드입니다.
 */
@Composable
fun LocationBasedAutoRunCard(
    location: LocationBasedAutoRun,
    successRate: Float? = null,
    gpsAccuracy: String? = null,
    onToggle: (Boolean) -> Unit,
    onEdit: () -> Unit,
    onDelete: () -> Unit
)
```

**UI 구성**:
1. **헤더**: 위치 아이콘 + 라벨 + 활성화 토글
   - 위치 아이콘 자동 선택 (회사 🏢, 학교 🏫, 도서관 📚, 카페 ☕, 집 🏠, 기본 📍)
2. **주소**: 축약 표시 (1줄, ellipsis)
3. **정보**: 반경 + 타이머 시간 (예: "반경 100m • 45분")
4. **성공률** (옵션): "성공률: 85% 🟢" (70% 이상 🟢, 50~70% 🟡, 50% 미만 🔴)
5. **GPS 정확도** (옵션): "GPS 정확도: 높음"
6. **액션 버튼**: 편집 / 삭제 (OutlinedButton)

**카드 색상**:
- 활성화: PrimaryContainer
- 비활성화: SurfaceVariant

---

#### 3.4 LocationBatteryImpactCard

**파일**: `app/src/main/java/com/allday/detoxy/presentation/ui/autorun/components/LocationBatteryImpactCard.kt` (신규, ~160줄)

```kotlin
/**
 * 위치 기반 배터리 영향 안내 카드
 *
 * 위치 기반 자동 실행의 배터리 영향을 안내하고, 최적화 팁을 제공합니다.
 */
@Composable
fun LocationBatteryImpactCard(
    enabledCount: Int
)
```

**배터리 소모 예상** (활성화된 위치 개수에 따라):
- 0개: 0%/일
- 1개: 2~3%/일
- 2개: 3~4%/일
- 3개: 4~5%/일
- 4개: 5~6%/일
- 5개: 6~7%/일

**최적화 팁** (확장 시):
- 📍 위치 기반 자동 실행을 2개 이하로 유지하세요
- 📏 반경을 너무 작게 설정하지 마세요 (100m 이상 권장)
- 🔕 사용하지 않는 위치는 비활성화하세요
- 🔋 체류 시간을 1분 이상으로 설정하면 더 효율적입니다

---

#### 3.5 LocationBasedAutoRunScreen

**파일**: `app/src/main/java/com/allday/detoxy/presentation/ui/autorun/LocationBasedAutoRunScreen.kt` (신규, ~280줄)

```kotlin
/**
 * 위치 기반 자동 실행 설정 화면
 *
 * 위치 기반 자동 실행 설정을 관리하는 메인 화면입니다.
 */
@OptIn(ExperimentalMaterial3Api::class)
@Composable
fun LocationBasedAutoRunScreen(
    onBack: () -> Unit = {},
    onNavigateToTimeBased: () -> Unit = {},
    viewModel: LocationBasedAutoRunViewModel = hiltViewModel()
)
```

**화면 구성**:
1. **TopAppBar**: "위치 기반 자동 실행" + 뒤로가기 버튼
2. **FloatingActionButton**: "위치 추가" (권한 있고 5개 미만일 때만)
3. **LazyColumn 콘텐츠**:
   - Play Services 미지원 경고 (미지원 시)
   - 위치 권한 안내 (권한 없을 때)
   - Empty State (권한 있지만 위치 없을 때)
   - 등록된 위치 리스트
   - 최대 개수 초과 경고 (5개 도달 시)
   - 배터리 영향 안내 (권한 있고 위치 있을 때)

**권한 요청 플로우**:
1. **정확한 위치** 요청 (`ACCESS_FINE_LOCATION`)
2. 승인 후 **백그라운드 위치** 요청 (`ACCESS_BACKGROUND_LOCATION`)
3. 거부 시 설정 화면으로 이동 안내

**Empty State**:
```
📍
등록된 위치가 없습니다
특정 장소 도착 시 자동으로 집중 모드를 시작하세요
```

**최대 개수 경고**:
```
⚠️ 최대 5개까지 등록 가능합니다.
배터리 효율을 위해 개수가 제한됩니다.
```

---

## 📊 빌드 검증

```bash
$ ./gradlew compileDebugKotlin
```

**결과**: ✅ **BUILD SUCCESSFUL in 23s**

**경고**:
- 30개 warnings (unused parameters, deprecated APIs)
- 모두 기존 코드 또는 향후 사용 예정 (무시 가능)

**에러**: 0개

---

## 🎯 개선 효과

### 1. 완전한 권한 상태 관리 ✅

**구현 내용**:
- Play Services 사용 가능 여부 체크
- 정확한 위치 권한 체크
- 백그라운드 위치 권한 체크
- 위치 서비스 활성화 여부 체크
- 전체 위치 권한 부여 여부 (combine)

**사용자 경험**:
- ✅ 권한 상태에 따라 적절한 UI 표시
- ✅ 단계별 권한 요청 (정확한 위치 → 백그라운드 위치)
- ✅ 권한 없을 때 대체 방법 제시 (시간 기반 자동 실행)

---

### 2. Play Services 미지원 기기 대응 ✅

**문제 상황**:
- Huawei 등 일부 기기는 Google Play Services 미탑재
- Geofencing API를 사용할 수 없음

**해결 방법**:
- `GoogleApiAvailability.isGooglePlayServicesAvailable()` 체크
- 미지원 시 PlayServicesWarningCard 표시
- 시간 기반 자동 실행으로 즉시 이동 가능

**효과**:
- ✅ 미지원 기기에서도 명확한 안내 제공
- ✅ 대체 기능으로 즉시 전환 가능
- ✅ 사용자 혼란 방지

---

### 3. 배터리 영향 투명성 ✅

**구현 내용**:
- 활성화된 위치 개수에 따른 예상 배터리 소모 표시
- 최적화 팁 제공 (확장 가능 카드)
- 실제 소모량 확인 방법 안내

**배터리 소모 예상**:
- 1개: 2~3%/일
- 2개: 3~4%/일
- 5개: 6~7%/일

**효과**:
- ✅ 사용자가 배터리 영향을 미리 인지
- ✅ 최적화 방법 제공으로 배터리 절약 유도
- ✅ 신뢰도 향상 (투명한 정보 제공)

---

### 4. 최대 개수 제한 명확화 ✅

**제한 이유**:
- 시스템 제한: Geofence 100개
- 배터리 효율: 5개로 제한 (`AutoRunGeofenceManager.MAX_GEOFENCES`)

**UI 처리**:
- 5개 도달 시 경고 카드 표시
- FloatingActionButton 숨김
- 추가 등록 불가 안내

**효과**:
- ✅ 사용자가 제한 사유를 명확히 이해
- ✅ 배터리 효율 유지
- ✅ 혼란 방지 (추가 버튼 미노출)

---

### 5. Geofence 자동 관리 ✅

**ViewModel 통합**:
- `addLocation()`: DB 저장 + Geofence 등록
- `updateLocation()`: DB 업데이트 + Geofence 재등록
- `deleteLocation()`: Geofence 해제 + DB 삭제
- `toggleLocation()`: Geofence 등록/해제 + DB 토글

**효과**:
- ✅ 사용자는 DB 저장만 신경 쓰면 됨
- ✅ Geofence 등록/해제 자동 처리
- ✅ 데이터 일관성 보장

---

## 📝 변경된 파일

| 파일 | 상태 | 설명 | 라인 수 |
|------|------|------|---------|
| `LocationBasedAutoRunRepository.kt` | ✅ 신규 | 위치 기반 자동 실행 Repository | ~95줄 |
| `LocationBasedAutoRunViewModel.kt` | ✅ 신규 | 위치 기반 자동 실행 ViewModel (권한, Geofence 관리) | ~190줄 |
| `PlayServicesWarningCard.kt` | ✅ 신규 | Play Services 미지원 경고 카드 | ~80줄 |
| `LocationPermissionCard.kt` | ✅ 신규 | 위치 권한 안내 카드 | ~140줄 |
| `LocationBasedAutoRunCard.kt` | ✅ 신규 | 위치 기반 자동 실행 카드 | ~190줄 |
| `LocationBatteryImpactCard.kt` | ✅ 신규 | 위치 기반 배터리 영향 안내 카드 | ~160줄 |
| `LocationBasedAutoRunScreen.kt` | ✅ 신규 | 위치 기반 자동 실행 메인 화면 | ~280줄 |
| `02_advanced_autosetting_todolist.md` | ✅ 수정 | 체크리스트 업데이트 (4.1.1 완료) | +5줄 |

**총 7개 파일 신규 생성, 1개 파일 수정**  
**총 코드 추가**: ~+1,135줄

---

## 🚀 다음 작업 (4.1.2 AddLocationAutoRunDialog, Day 15)

### 4.1.2 AddLocationAutoRunDialog 확장

#### 위치 검색 단계
- [ ] 주소 입력 TextField
- [ ] Google Places API (또는 Geocoder) 연동
- [ ] 검색 결과 리스트

#### 위치 확인 단계 (선택: 지도 표시)
- [ ] 선택한 위치 표시
- [ ] 반경 원 표시
- [ ] 위치 미세 조정 (드래그)

#### 설정 단계
- [ ] 라벨 입력 (예: "회사", "도서관")
- [ ] 반경 선택 (50m, 100m, 200m, 500m)
- [ ] 타이머 시간 선택
- [ ] 차단 프리셋 선택
- [ ] 트리거 타입 선택 (도착 시 / 주기적)
- [ ] 체류 시간 설정 (0분/1분/3분/5분)
- [ ] 도착 후 확인 옵션
- [ ] "저장" 버튼

**예상 작업량**: 1일 (Day 15), ~400줄

---

## 📋 체크리스트 업데이트

- [x] § 4.1.1 LocationBasedAutoRunScreen 레이아웃 - 오류 상태 UI 추가 ✅
- [x] § 4.1.1 LocationBasedAutoRunScreen 레이아웃 - 메인 화면 Compose ✅
- [x] § 4.1.1 LocationBasedAutoRunScreen 레이아웃 - LocationBasedAutoRunCard 컴포넌트 ✅
- [x] § 4.1.1 LocationBasedAutoRunScreen 레이아웃 - LocationPermissionCard 컴포넌트 ✅
- [x] § 4.1.1 LocationBasedAutoRunScreen 레이아웃 - 배터리 영향 안내 카드 ✅

---

## 📦 커밋 정보

| 단계 | 커밋 ID | 설명 |
|------|---------|------|
| **코드 구현** | (다음 단계) | LocationBasedAutoRunScreen 레이아웃 구현 (Repository, ViewModel, UI 컴포넌트) |
| **문서 작성** | (다음 단계) | 작업 기록 및 체크리스트 업데이트 |

---

**작업 완료일**: 2025-10-24  
**상태**: ✅ 완료, 빌드 성공, 다음 작업(4.1.2) 준비 완료

---

## 🎊 주요 성과

### 완전한 권한 관리 시스템 구축

**구현 내용**:
- ✅ Play Services 사용 가능 여부 체크
- ✅ 정확한 위치 권한 체크
- ✅ 백그라운드 위치 권한 체크
- ✅ 위치 서비스 활성화 여부 체크
- ✅ 단계별 권한 요청 플로우

**효과**:
- 사용자가 권한 상태를 명확히 인지
- 단계별 안내로 권한 승인율 향상 예상
- 대체 기능 제시로 이탈 방지

### Play Services 미지원 기기 대응

**문제 해결**:
- Huawei 등 미지원 기기에서 명확한 안내 제공
- 시간 기반 자동 실행으로 즉시 전환 가능
- 사용자 혼란 방지 및 신뢰도 유지

### 배터리 영향 투명성

**구현 효과**:
- 활성화된 위치 개수에 따른 예상 배터리 소모 표시
- 최적화 팁 제공으로 배터리 절약 유도
- 투명한 정보 제공으로 사용자 신뢰도 향상

### Geofence 자동 관리

**시스템 통합**:
- ViewModel에서 DB 저장과 Geofence 등록을 자동으로 처리
- 사용자는 UI 조작만으로 모든 작업 완료
- 데이터 일관성 보장

---

> **4.1.1 작업의 의의**: 위치 기반 자동 실행의 기반이 되는 UI 레이아웃을 완성했습니다. Play Services 미지원, 권한 부족 등 다양한 예외 상황을 모두 고려한 견고한 화면 구조를 구축했으며, 사용자에게 명확한 안내와 대체 방법을 제공하여 신뢰도를 높였습니다. 배터리 영향을 투명하게 공개하고 최적화 팁을 제공하여 사용자가 안심하고 기능을 사용할 수 있도록 했습니다. 다음 작업에서는 위치 추가 다이얼로그를 구현하여 실제로 위치를 등록할 수 있도록 완성할 예정입니다. 🎉

