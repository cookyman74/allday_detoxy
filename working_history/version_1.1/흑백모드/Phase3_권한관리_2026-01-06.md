# Grayscale Mode Phase 3 - 권한 관리 및 토글 롤백

**작업일**: 2026-01-06  
**버전**: v1.1  
**Phase**: Phase 3 (권한 관리 및 토글 롤백)

---

## 📋 작업 요약

권한 관리 및 토글 롤백 기능을 검토하고, 필요한 유틸리티 메서드를 추가했습니다.

---

## ✅ 완료된 작업

### TASK-001: shouldApplyGrayscale() 유틸리티
- **파일**: `core/manager/GrayscaleManager.kt`
- **메서드**:
  ```kotlin
  /**
   * 그레이스케일 적용 가능 여부 확인
   * 
   * 다음 조건을 모두 만족해야 true:
   * 1. Android 15+ (API 35)
   * 2. ACCESS_NOTIFICATION_POLICY 권한 부여됨
   */
  fun shouldApplyGrayscale(): Boolean {
      return isSupported() && hasPermission()
  }
  ```

### TASK-002: 토글 롤백 로직 (ViewModel) - Phase 1에서 완료
- **파일**: `FocusSettingsViewModel.kt`
- **이미 구현됨**:
  - `toggleGrayscaleMode(enabled: Boolean)` - API 레벨 게이트 + 권한 체크
  - `onGrayscalePermissionResult(granted: Boolean)` - 권한 결과 처리
  - 권한 미승인 시 `grayscaleModeEnabled = false` 명시적 설정

### TASK-003: UiState에 다이얼로그 상태 추가 - Phase 1에서 완료
- **파일**: `FocusSettingsViewModel.kt`
- **이미 구현됨**: `showGrayscalePermissionDialog: Boolean = false`

---

## 🔍 Phase 1에서 구현된 권한 플로우

```kotlin
fun toggleGrayscaleMode(enabled: Boolean) {
    // 토글 OFF 시: 다이얼로그도 닫기
    if (!enabled) {
        _uiState.update { 
            it.copy(
                grayscaleModeEnabled = false, 
                showGrayscalePermissionDialog = false
            ) 
        }
        saveGrayscaleSetting(false)
        return
    }
    
    // 토글 ON 시: API 레벨 게이트 확인
    if (Build.VERSION.SDK_INT < Build.VERSION_CODES.VANILLA_ICE_CREAM) {
        _uiState.update { it.copy(grayscaleModeEnabled = true) }
        saveGrayscaleSetting(true)
        return
    }
    
    // Android 15+: 권한 확인
    val nm = getApplication<Application>()
        .getSystemService(NotificationManager::class.java)
    if (!nm.isNotificationPolicyAccessGranted) {
        // 권한 미승인: grayscaleModeEnabled = false 명시적 설정
        _uiState.update { 
            it.copy(
                grayscaleModeEnabled = false,
                showGrayscalePermissionDialog = true
            ) 
        }
        saveGrayscaleSetting(false)
        return
    }
    
    // 권한 있음: 정상 토글 ON
    _uiState.update { it.copy(grayscaleModeEnabled = true) }
    saveGrayscaleSetting(true)
}
```

---

## 🧪 검증

### 빌드 검증
```bash
./gradlew compileDebugKotlin
# BUILD SUCCESSFUL in 8s
```

---

## 📁 수정된 파일

| 파일 | 변경 내용 |
|------|----------|
| `GrayscaleManager.kt` | `shouldApplyGrayscale()` 유틸리티 메서드 추가 |

---

## 📝 Phase 3 작업 참고사항

Phase 3의 대부분 작업은 Phase 1에서 이미 완료되었습니다:
- `toggleGrayscaleMode()` - Phase 1에서 구현 및 리뷰 반영 완료
- `onGrayscalePermissionResult()` - Phase 1에서 구현 완료
- `showGrayscalePermissionDialog` UiState - Phase 1에서 추가 완료

Phase 3에서는 `shouldApplyGrayscale()` 유틸리티 메서드만 추가하여 Phase 4에서 사용할 수 있도록 준비했습니다.

---

## 📝 다음 단계

- **Phase 4**: FocusTimerService 연동 (타이머 시작/종료 시 흑백 모드 연동)
