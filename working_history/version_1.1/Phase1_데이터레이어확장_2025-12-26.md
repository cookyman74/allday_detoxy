# 작업 기록: Phase 1 데이터 레이어 확장

**작업 일시**: 2025-12-26  
**작업 범위**: 할 일(To-Do) 결합 스케줄 기능을 위한 Room DB 스키마 확장

## 문제 상황 / 작업 배경

v1.1 기능 추가를 위해 기존 스케줄 엔티티에 목표/할일 정보를 저장할 수 있도록 DB 스키마를 확장해야 함.

**Phase 0 확인 결과**:
- 현재 DB 버전: v8
- 확장 대상 엔티티: TimeBasedAutoRun, LocationBasedAutoRun

## 수정 사항

### 1. 도메인 모델 생성

**파일**: `app/src/main/java/com/allday/detoxy/domain/model/ScheduleInfo.kt`

```kotlin
data class ScheduleInfo(
    val title: String = "",
    val description: String = "",  // MVP: 빈 문자열
    val memo: String = "",         // MVP: 빈 문자열
    val todos: List<ScheduleTodo> = emptyList()
)

data class ScheduleTodo(
    val id: String,
    val content: String,
    val isRequired: Boolean = false,
    val orderIndex: Int = 0
)

data class TodoStatus(...)         // 세션 결과 저장용
enum class TodoCompletionStatus { COMPLETED, NOT_COMPLETED, NO_RESPONSE }
enum class ScheduleType { TIME_BASED, LOCATION_BASED }
```

### 2. 엔티티 확장

**TimeBasedAutoRun.kt**:
```kotlin
val scheduleInfoJson: String? = null  // v9 추가
```

**LocationBasedAutoRun.kt**:
```kotlin
val scheduleInfoJson: String? = null  // v9 추가
```

### 3. TypeConverter 구현

| 파일 | 설명 |
|------|------|
| `ScheduleInfoConverter.kt` | ScheduleInfo ↔ JSON (org.json 사용) |
| `TodoEnumConverters.kt` | ScheduleType, TodoCompletionStatus enum 변환 |

### 4. 새 엔티티 및 DAO

**FocusSessionTodoResultEntity.kt**:
- 테이블명: `focus_session_todo_result`
- PK: sessionId
- 주요 필드: scheduleId, scheduleType, scheduleTitleSnapshot, todoResultsJson, completedAt, lastModifiedAt

**FocusSessionTodoResultDao.kt**:
- `getBySessionId()`, `getByScheduleId()`, `getAllFlow()`
- `getModifiableResultsFlow()` - NO_RESPONSE/NOT_COMPLETED 항목 조회
- `insert()`, `updateResults()`

### 5. Migration

**Migration_8_9.kt** (v8 → v9):
1. `ALTER TABLE time_based_auto_run ADD COLUMN scheduleInfoJson TEXT`
2. `ALTER TABLE location_based_auto_run ADD COLUMN scheduleInfoJson TEXT`
3. `CREATE TABLE focus_session_todo_result (...)`

### 6. Database 수정

**DetoxyDatabase.kt**:
- version = 9
- FocusSessionTodoResultEntity 엔티티 추가
- @TypeConverters 어노테이션 추가
- focusSessionTodoResultDao() 추가

**DatabaseModule.kt**:
- MIGRATION_8_9 등록
- provideFocusSessionTodoResultDao() 추가

## 검증 방법

```bash
./gradlew clean assembleDebug
# BUILD SUCCESSFUL in 45s
```

- [x] 컴파일 오류 없음
- [x] Room 스키마 생성 확인 (kapt 성공)
- [x] Hilt DI 그래프 정상

## 관련 파일

**신규 생성**:
- `domain/model/ScheduleInfo.kt`
- `data/local/converter/ScheduleInfoConverter.kt`
- `data/local/converter/TodoEnumConverters.kt`
- `data/local/entity/FocusSessionTodoResultEntity.kt`
- `data/local/dao/FocusSessionTodoResultDao.kt`
- `data/local/migration/Migration_8_9.kt`

**수정**:
- `data/local/entity/TimeBasedAutoRun.kt`
- `data/local/entity/LocationBasedAutoRun.kt`
- `data/local/DetoxyDatabase.kt`
- `core/di/DatabaseModule.kt`

## 참고 사항

### Phase 2 준비 사항

| 항목 | 값 |
|------|-----|
| DB 버전 | v9 |
| ScheduleInfo 경로 | `domain/model/ScheduleInfo.kt` |
| FocusSessionTodoResultEntity 경로 | `data/local/entity/FocusSessionTodoResultEntity.kt` |
| FocusSessionTodoResultDao 경로 | `data/local/dao/FocusSessionTodoResultDao.kt` |

### 다음 작업 (Phase 2)

1. `ScheduleInfoValidation` 검증 로직 구현
2. `buildTodoResult()` 함수 구현
3. `deriveGoalStatusFromTodos()` 목표 상태 자동 도출 함수
4. UseCase 파라미터 확장

### 주의사항

- org.json 라이브러리 사용 (Gson 미포함 프로젝트)
- scheduleInfoJson은 nullable로 유지 (기존 데이터 호환)

---

**작업 완료일**: 2025-12-26  
**작업자**: AI Assistant
