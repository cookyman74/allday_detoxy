# Phase 4+ UI/UX 리팩토링 - 작업 완료 보고서

> **작업일**: 2026-01-02
> **Phase**: Phase 4 확장 - 목록 화면 간소화 및 네비게이션 개선
> **상태**: ✅ 완료

---

## 📋 작업 요약

| 항목 | 내용 |
|------|------|
| 목표 | 스케줄 그룹 목록 화면 UI 간소화 및 상세 페이지 분리 |
| 결과 | ✅ 빌드 성공, 커밋 완료 |
| 배경 | 기존 카드형 UI가 너무 무거워 사용자 경험 저하, 뒤로가기 시 복잡한 UI 반복 표시 |

---

## 🎯 해결한 문제

### 1. 목록 화면 복잡성
- **기존**: `ScheduleGroupCard`에 히트맵, 시간 목록, 위치 정보, 제어 버튼 등 모든 정보 표시
- **변경**: `SimpleScheduleGroupItem`으로 교체하여 아이콘, 이름, 시간 스케줄 개수, 토글만 표시

### 2. 히트맵 미표시 버그
- **원인**: `WeeklyHeatmapCalculator.calculate()`에서 `isEnabled=false`인 스케줄 필터링
- **수정**: 필터 제거하여 비활성화된 스케줄도 히트맵에 표시

### 3. 네비게이션 연결 문제
- **기존**: 스케줄 탭에서 카드 클릭 시 목록 화면으로 이동
- **수정**: 상세 페이지(`ScheduleGroupDetailScreen`)로 직접 이동

---

## 📁 수정된 파일

### 1. ScheduleGroupScreen.kt (대규모 수정)

| 변경 내용 | 설명 |
|----------|------|
| `SimpleScheduleGroupItem` 추가 | 목록용 경량 아이템 Composable 신규 정의 |
| `ScheduleGroupCard` 호출 제거 | 기존의 복잡한 카드 UI 사용 중단 |
| 미사용 State 정리 | `linkedLocations`, `linkedLocationCounts`, `groupHeatmaps` 주석 처리 |
| Import 추가 | `DateRange`, `clickable`, `background` 등 |

```kotlin
@Composable
private fun SimpleScheduleGroupItem(
    group: ScheduleGroup,
    controlState: ScheduleGroupControlState,
    timeCount: Int,
    onStateChange: (ScheduleGroupControlState) -> Unit,
    onClick: () -> Unit
) {
    val cardTint = when (controlState) { ... }
    
    SimpleGlassSurface(
        modifier = Modifier.fillMaxWidth().clickable { onClick() },
        backgroundColor = cardTint,
        alpha = 0.3f
    ) {
        Row(...) {
            // 아이콘 + 이름 + 시간 스케줄 개수
            // 토글 스위치
        }
    }
}
```

### 2. WeeklyHeatmapCalculator.kt (버그 수정)

| 변경 전 | 변경 후 |
|--------|--------|
| `val enabledAutoRuns = autoRuns.filter { it.isEnabled }` | `val targetAutoRuns = autoRuns` |

- **효과**: 비활성화된 스케줄도 히트맵에 표시

### 3. MainActivity.kt (네비게이션 수정)

| 변경 내용 | 설명 |
|----------|------|
| `SCHEDULE_GROUP_DETAIL` 타입 추가 | 스케줄 탭에서 상세 페이지로 직접 이동 |

---

## 🖼️ UI 변경 비교

### 목록 화면 (ScheduleGroupScreen)

| 변경 전 | 변경 후 |
|--------|--------|
| 대형 카드 (히트맵 + 시간 목록 + 위치 + 버튼) | 경량 리스트 아이템 (아이콘 + 이름 + 개수 + 토글) |
| 카드당 약 200dp 높이 | 아이템당 약 80dp 높이 |
| 클릭 시 별도 동작 없음 | 클릭 시 상세 페이지 이동 |

### 상세 화면 (ScheduleGroupDetailScreen)

- 히트맵 전체 표시 (항상 펼침)
- 시간 목록 표시
- 위치 정보 표시
- 수정/삭제 버튼

---

## 📊 성능 개선 효과

| 항목 | 변경 전 | 변경 후 |
|------|--------|--------|
| 목록 렌더링 부하 | 높음 (모든 히트맵 동시 렌더링) | 낮음 (경량 아이템만 렌더링) |
| 초기 로딩 | 모든 그룹의 히트맵 계산 | 상세 페이지 진입 시 개별 계산 |
| 메모리 사용 | 높음 | 낮음 |

---

## 📊 빌드 결과

```
./gradlew :app:compileDebugKotlin

BUILD SUCCESSFUL in 22s
```

---

## 🔗 관련 커밋

```
fda3998 [UI] 스케줄 그룹 목록 간소화 및 히트맵 로직 수정 (Build Fixed)
519c202 [Fix] 히트맵 미표시 디버깅 로그 추가 및 네비게이션 수정
```

---

## 🔜 다음 단계

- **Phase 5**: 접근성 및 최종 검증
  - 스크린 리더 테스트
  - 다크 모드 대응 확인
  - 최종 QA

---

## 📝 참고 사항

### 사용자 피드백 반영

> "기존 UI는 필요 없어진 것 아닌가? 뒤로가기 버튼을 누르면 이전에 구성된 SCHEDULE_GROUP 페이지가 출력된다. 해당 페이지는 더 이상 사용하지 않아야 한다."

- ✅ 목록 화면을 심플하게 변경하여 "경량 진입점" 역할로 전환
- ✅ 상세 페이지에서 뒤로가기 시 가벼운 목록 화면 표시

---

## 🔄 추가 작업: ScheduleGroupScreen 완전 제거 (2026-01-02 19:35)

### 배경

스케줄 탭(메인 화면)에서 이미 스케줄 그룹 목록이 표시되고 있기 때문에, 별도의 `ScheduleGroupScreen`이 중복됨.

### 제거된 항목

| 항목 | 설명 |
|------|------|
| `AutoRunScreenType.SCHEDULE_GROUP` | enum 타입 주석 처리 |
| `TimeBasedAutoRunScreen.onNavigateToScheduleGroup` | 콜백 제거 |
| `LocationBasedAutoRunScreen.onNavigateToScheduleGroup` | 콜백 제거 |
| `TimeBasedAutoRunScreen.onBack` 분기 로직 | SCHEDULE_GROUP 분기 제거, 항상 NONE(메인)으로 이동 |
| `SCHEDULE_GROUP` when 분기 | 전체 주석 처리 |

### 수정 파일

- `MainActivity.kt`: 19줄 삭제, 10줄 추가 (순감 9줄)

### 변경된 네비게이션 흐름

```
[변경 전]
스케줄 탭 → ScheduleGroupScreen → ScheduleGroupDetailScreen → 뒤로가기 → ScheduleGroupScreen → ...

[변경 후]
스케줄 탭 → ScheduleGroupDetailScreen → 뒤로가기 → 스케줄 탭 ✅
```

### 커밋

```
542858b [Fix] 상세 페이지 백버튼이 스케줄 탭(메인)으로 직접 돌아가도록 수정
7bdc907 [Refactor] 불필요한 SCHEDULE_GROUP 화면 제거 - 스케줄 탭에서 상세 페이지로 직접 이동
```

