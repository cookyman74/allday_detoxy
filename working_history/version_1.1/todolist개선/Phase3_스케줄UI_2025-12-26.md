# 작업 기록: Phase 3 스케줄 생성/편집 UI

**작업 일시**: 2025-12-26  
**작업 범위**: 할 일(To-Do) 기능을 위한 스케줄 생성/편집 UI 구현

## 문제 상황 / 작업 배경

Phase 2에서 생성된 검증 로직과 핵심 함수를 기반으로 스케줄 생성/편집 다이얼로그에 목표/할일 입력 UI를 추가해야 함.

**Phase 2 확인 결과**:
- 검증 함수: `validateScheduleInfo(info: ScheduleInfo): ValidationResult`
- 결과 빌더: `TodoResultBuilder.buildTodoResult(scheduleId, info, responses)`

## 수정 사항

### 1. 신규 Composable 생성

| 파일 | 설명 |
|------|------|
| `GoalInputSection.kt` | 목표 입력 섹션 (50자 제한, 글자 수 표시) |
| `TodoListSection.kt` | 할일 목록 섹션 (최대 5개, 필수 토글, 삭제) |
| `TodoItemRow` | 개별 할일 항목 Row (TodoListSection 내부) |

**주요 기능**:
- 글자 수 제한 표시 (목표 50자, 할일 100자)
- 제한 근접 시 색상 경고
- 세부 할일 추가/접기 토글
- 필수 항목 FilterChip 토글

### 2. 다이얼로그 통합

#### AddTimeBasedAutoRunDialog.kt

**수정 내용**:
- `ScheduleInfoConverter`, `ScheduleInfo`, `ScheduleTodo` import 추가
- 로컬 상태 추가: `scheduleInfo`, `showTodoSection`
- 라벨 입력 후 GoalInputSection, TodoListSection 배치
- 저장 시 `scheduleInfoJson` 필드에 JSON 직렬화하여 저장

**코드 위치**: 라벨 입력 섹션 ~ 시간표 연결 섹션 사이

#### AddLocationAutoRunDialog.kt

**수정 내용**:
- 동일한 import 및 상태 추가
- `LocationSettingsStep` 파라미터 확장 (scheduleInfo, showTodoSection)
- 저장 시 `scheduleInfoJson` 필드에 JSON 직렬화하여 저장

**코드 위치**: 차단 강도 섹션 ~ 진입 조건 섹션 사이

### 3. 프로젝트 구조 노트

**ViewModel 확장 불필요**:
- 다이얼로그 내 로컬 상태(remember)로 scheduleInfo 관리
- 저장 시 Entity에 scheduleInfoJson 포함하여 onSave 콜백 전달
- Repository/DAO에서 자동 저장됨

## 검증 방법

```bash
./gradlew assembleDebug
# BUILD SUCCESSFUL in 7s
```

- [x] 컴파일 오류 없음
- [x] GoalInputSection Composable 생성 완료
- [x] TodoListSection Composable 생성 완료
- [x] AddTimeBasedAutoRunDialog 통합 완료
- [x] AddLocationAutoRunDialog 통합 완료

## 관련 파일

**신규 생성**:
- `presentation/ui/autorun/components/GoalInputSection.kt`
- `presentation/ui/autorun/components/TodoListSection.kt`

**수정**:
- `presentation/ui/autorun/components/AddTimeBasedAutoRunDialog.kt`
- `presentation/ui/autorun/components/AddLocationAutoRunDialog.kt`

## Phase 4 준비 사항

| 항목 | 값 |
|------|-----|
| 목표/할일 UI | GoalInputSection, TodoListSection |
| 저장 위치 | TimeBasedAutoRun.scheduleInfoJson, LocationBasedAutoRun.scheduleInfoJson |
| ScheduleInfo 조회 | ScheduleInfoConverter().toScheduleInfo(scheduleInfoJson) |

### 다음 작업 (Phase 4)

1. 차단 오버레이에서 목표/할일 표시
2. OverlayDisplayRules 구현
3. 목표/할일 표시 로직 구현

### 리뷰 피드백 반영 (2025-12-26)

**수정 내용**:

| 이슈 | 심각도 | 해결 방법 |
|------|--------|----------|
| 저장 시 검증 누락 | Medium | `validateScheduleInfo()` 호출 및 빈 할일 필터링 추가 |
| orderIndex 중복 가능 | Low | 삭제 후 `mapIndexed`로 재정렬, 추가 시 `maxOfOrNull + 1` 사용 |

**코드 변경**:
- import 추가: `validateScheduleInfo`, `ValidationResult`
- 저장 전 빈 할일(`content.isNotBlank()`) 필터링
- 검증 실패 시 `return@Button`으로 저장 중단
- 삭제 후 orderIndex 재정렬: `newTodos.mapIndexed { i, todo -> todo.copy(orderIndex = i) }`
- 추가 시 orderIndex: `maxOfOrNull { it.orderIndex } + 1`

---

**작업 완료일**: 2025-12-26  
**작업자**: AI Assistant
