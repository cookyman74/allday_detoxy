# 3차 고도화 작업 기록 - Day 1: 데이터 모델 확장 (v5→v6)

## 작업 일자
- 2025-10-30 (수) 오후

## 작업 범위
- Day 1-2: 데이터 모델 확장 (Room v5 → v6)
  - ScheduleGroup UI 필드 추가
  - LocationBasedAutoRun 위치-시간표 연동 옵션 추가
  - TimeBasedAutoRun 그룹 내 우선순위 추가
  - Migration_5_6 작성 및 테스트

---

## 1. 작업 개요

### 1.1 목표
- **엔티티 확장**: ScheduleGroup, LocationBasedAutoRun, TimeBasedAutoRun에 v6 필드 추가
- **마이그레이션**: Migration_5_6.kt 작성 (v5 → v6)
- **DAO 확장**: ScheduleGroupDao, LocationBasedAutoRunDao에 v6 메서드 추가
- **테스트**: MigrationTest_5_6.kt 작성

### 1.2 참조 문서
- `docs/03_complex_time&location_todolist.md` §1 (데이터 모델 확장)
- `working_history/2025-10-29_2.5nd_advanced_3.2.md` (v4→v5 마이그레이션 참조)
- `docs/02_advanced_room_migration_strategy.md` (마이그레이션 전략)

### 1.3 작업 방식
- Clean Architecture 원칙 준수 (domain 인터페이스 + data 구현)
- 기존 패턴 유지 (Soft Reference 전략)
- 기존 데이터 영향 없음 (nullable 필드 또는 DEFAULT 값 설정)

---

## 2. 엔티티 확장 (v6 필드 추가)

### 2.1 ScheduleGroup.kt

**추가된 필드**:
- `iconType: String = "WORK"` - UI 표시용 아이콘 타입
- `colorHex: String = "#4CAF50"` - UI 표시용 색상 코드
- `lastActivatedAt: Long? = null` - 마지막 활성화 시각 (통계용)

**파일**: `app/src/main/java/com/allday/detoxy/data/local/entity/ScheduleGroup.kt`

**변경 내용**:
```kotlin
// v6 추가: UI 및 통계 필드
val iconType: String = "WORK",
val colorHex: String = "#4CAF50",
val lastActivatedAt: Long? = null
```

### 2.2 LocationBasedAutoRun.kt

**추가된 필드**:
- `activateScheduleOnEnter: Boolean = false` - 위치 진입 시 시간표 자동 활성화
- `deactivateScheduleOnExit: Boolean = false` - 위치 이탈 시 시간표 자동 비활성화
- `exitActionType: String = "DEACTIVATE"` - 위치 이탈 시 액션 타입

**파일**: `app/src/main/java/com/allday/detoxy/data/local/entity/LocationBasedAutoRun.kt`

**변경 내용**:
```kotlin
// v6 추가: 위치-시간표 연동 옵션
val activateScheduleOnEnter: Boolean = false,
val deactivateScheduleOnExit: Boolean = false,
val exitActionType: String = "DEACTIVATE"
```

### 2.3 TimeBasedAutoRun.kt

**추가된 필드**:
- `groupPriority: Int = 0` - 그룹 내 우선순위 (정렬용, 선택적)

**파일**: `app/src/main/java/com/allday/detoxy/data/local/entity/TimeBasedAutoRun.kt`

**변경 내용**:
```kotlin
// v6 추가: 그룹 내 우선순위
val groupPriority: Int = 0
```

---

## 3. 마이그레이션 스크립트 작성

### 3.1 Migration_5_6.kt 생성

**파일**: `app/src/main/java/com/allday/detoxy/data/local/migrations/Migration_5_6.kt`

**마이그레이션 내용**:
1. **ScheduleGroup 테이블 확장**
   - `iconType TEXT NOT NULL DEFAULT 'WORK'`
   - `colorHex TEXT NOT NULL DEFAULT '#4CAF50'`
   - `lastActivatedAt INTEGER` (nullable)

2. **LocationBasedAutoRun 테이블 확장**
   - `activateScheduleOnEnter INTEGER NOT NULL DEFAULT 0`
   - `deactivateScheduleOnExit INTEGER NOT NULL DEFAULT 0`
   - `exitActionType TEXT NOT NULL DEFAULT 'DEACTIVATE'`

3. **TimeBasedAutoRun 테이블 확장**
   - `groupPriority INTEGER NOT NULL DEFAULT 0`

**예상 실행 시간**: < 70ms

### 3.2 DetoxyDatabase.kt 버전 업데이트

**변경 내용**:
- 버전: 5 → 6
- 버전 히스토리 업데이트

**파일**: `app/src/main/java/com/allday/detoxy/data/local/DetoxyDatabase.kt`

### 3.3 DatabaseModule.kt에 MIGRATION_5_6 추가

**변경 내용**:
- `MIGRATION_5_6` import 추가
- `.addMigrations()`에 `MIGRATION_5_6` 추가
- 마이그레이션 히스토리 주석 업데이트

**파일**: `app/src/main/java/com/allday/detoxy/core/di/DatabaseModule.kt`

---

## 4. DAO 확장

### 4.1 ScheduleGroupDao.kt

**추가된 메서드**:
- `setActiveWithTimestamp(id: String, isActive: Boolean, timestamp: Long? = null)`
  - 활성화/비활성화 및 마지막 활성화 시각 업데이트용 (v6+)

**파일**: `app/src/main/java/com/allday/detoxy/data/local/dao/ScheduleGroupDao.kt`

**변경 내용**:
```kotlin
@Query("UPDATE schedule_group SET isActive = :isActive, lastActivatedAt = :timestamp WHERE id = :id")
suspend fun setActiveWithTimestamp(id: String, isActive: Boolean, timestamp: Long? = null)
```

### 4.2 LocationBasedAutoRunDao.kt

**추가된 메서드**:
- `getAutoActivateLocations(): Flow<List<LocationBasedAutoRun>>`
  - 진입 시 시간표 자동 활성화 옵션이 설정된 위치 조회 (v6+)
- `getAutoDeactivateLocations(): Flow<List<LocationBasedAutoRun>>`
  - 이탈 시 시간표 자동 비활성화 옵션이 설정된 위치 조회 (v6+)

**파일**: `app/src/main/java/com/allday/detoxy/data/local/dao/LocationBasedAutoRunDao.kt`

**변경 내용**:
```kotlin
@Query("SELECT * FROM location_based_auto_run WHERE activateScheduleOnEnter = 1 AND isEnabled = 1")
fun getAutoActivateLocations(): Flow<List<LocationBasedAutoRun>>

@Query("SELECT * FROM location_based_auto_run WHERE deactivateScheduleOnExit = 1 AND isEnabled = 1")
fun getAutoDeactivateLocations(): Flow<List<LocationBasedAutoRun>>
```

---

## 5. 마이그레이션 테스트 작성

### 5.1 MigrationTest_5_6.kt 생성

**파일**: `app/src/androidTest/java/com/allday/detoxy/data/local/migrations/MigrationTest_5_6.kt`

**테스트 케이스**:

1. **migrate5To6_addsNewFields()**
   - v5 DB 생성 및 샘플 데이터 삽입
   - v5 → v6 마이그레이션 실행
   - 새 컬럼 존재 확인 (PRAGMA table_info)
   - 기본값 검증 (iconType="WORK", colorHex="#4CAF50", activateScheduleOnEnter=0, 등)
   - 기존 데이터 무손실 확인

2. **migrate5To6_insertsNewFields()**
   - v5 → v6 마이그레이션 실행
   - v6 필드 포함 데이터 삽입 테스트
   - v6 필드 포함 데이터 확인

**검증 내용**:
- ScheduleGroup: iconType, colorHex, lastActivatedAt 컬럼 확인
- LocationBasedAutoRun: activateScheduleOnEnter, deactivateScheduleOnExit, exitActionType 컬럼 확인
- TimeBasedAutoRun: groupPriority 컬럼 확인
- 기본값 검증: 모든 필드 기본값 정확성 확인
- 기존 데이터 무손실: 모든 기존 레코드 보존 확인

---

## 6. 검증 결과

### 6.1 빌드 검증
- ✅ Kotlin 컴파일 성공
- ✅ Linter 오류 없음
- ✅ 모든 파일 정상 컴파일

### 6.2 코드 품질
- ✅ Clean Architecture 패턴 유지
- ✅ 주석 및 문서화 완료
- ✅ 기존 코드 스타일 준수

### 6.3 마이그레이션 안정성
- ✅ 기존 데이터 영향 없음 (nullable 또는 DEFAULT 값)
- ✅ Soft Reference 전략 유지
- ✅ 테스트 케이스 작성 완료

---

## 7. 다음 작업 단계

### 7.1 Day 2 (예정)
- Repository 확장 (필요 시)
- 단위 테스트 작성
- 통합 테스트 검증

### 7.2 Day 3-5 (예정)
- ScheduleGroupManager 구현
- GeofenceTransitionsReceiver 확장
- AlarmManager 연동

---

## 8. 변경된 파일 목록

### 8.1 엔티티
1. `app/src/main/java/com/allday/detoxy/data/local/entity/ScheduleGroup.kt`
2. `app/src/main/java/com/allday/detoxy/data/local/entity/LocationBasedAutoRun.kt`
3. `app/src/main/java/com/allday/detoxy/data/local/entity/TimeBasedAutoRun.kt`

### 8.2 마이그레이션
4. `app/src/main/java/com/allday/detoxy/data/local/migrations/Migration_5_6.kt` (신규)
5. `app/src/main/java/com/allday/detoxy/data/local/DetoxyDatabase.kt`
6. `app/src/main/java/com/allday/detoxy/core/di/DatabaseModule.kt`

### 8.3 DAO
7. `app/src/main/java/com/allday/detoxy/data/local/dao/ScheduleGroupDao.kt`
8. `app/src/main/java/com/allday/detoxy/data/local/dao/LocationBasedAutoRunDao.kt`

### 8.4 테스트
9. `app/src/androidTest/java/com/allday/detoxy/data/local/migrations/MigrationTest_5_6.kt` (신규)

---

## 9. 커밋 정보

**커밋 메시지**:
```
feat(db): Room v5→v6 마이그레이션 및 데이터 모델 확장

- ScheduleGroup: iconType, colorHex, lastActivatedAt 필드 추가 (v6)
- LocationBasedAutoRun: activateScheduleOnEnter, deactivateScheduleOnExit, exitActionType 필드 추가 (v6)
- TimeBasedAutoRun: groupPriority 필드 추가 (v6)
- Migration_5_6.kt 작성 (v5 → v6)
- ScheduleGroupDao.setActiveWithTimestamp() 메서드 추가
- LocationBasedAutoRunDao.getAutoActivateLocations(), getAutoDeactivateLocations() 메서드 추가
- MigrationTest_5_6.kt 작성 (통합 테스트)
- DetoxyDatabase 버전 업데이트 (5 → 6)
- DatabaseModule에 MIGRATION_5_6 추가

참조: docs/03_complex_time&location_todolist.md §1
```

---

## 10. 참고 사항

### 10.1 마이그레이션 전략
- 기존 데이터 영향 없음 (nullable 필드 또는 DEFAULT 값)
- 모든 기존 레코드는 기본값으로 설정됨
- Soft Reference 관계 유지 (Foreign Key 없음)

### 10.2 다음 단계
- Day 2에서 Repository 확장 및 추가 테스트 진행
- Day 3-5에서 ScheduleGroupManager 및 GeofenceTransitionsReceiver 확장 예정

---

**작업 완료 시간**: 2025-10-30 오후
**작업자**: AI Assistant
**검증 상태**: ✅ 완료

