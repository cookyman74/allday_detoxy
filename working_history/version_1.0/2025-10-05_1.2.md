# 작업 이력 - 2025-10-05

## 작업 단위: 1.2 AccessibilityService 구현

### 📋 작업 개요
- **작업 번호**: 1.2
- **작업명**: AccessibilityService 구현 (Day 2-3)
- **작업 일자**: 2025-10-05
- **작업 상태**: 부분 완료 (1.2.1-1.2.3 완료, 1.2.4 대기 중)
- **소요 시간**: 약 20분

### 🎯 작업 목표
앱 차단 기능의 핵심인 AccessibilityService를 구현하여 타이머 실행 중 특정 앱(Instagram, TikTok, YouTube, Facebook) 실행 시 홈 화면으로 이동시키는 기능을 개발합니다.

### ✅ 완료된 작업

#### 1.2.1 AccessibilityService 기본 구조

**완료 항목**:
1. ✅ **FocusAccessibilityService 클래스 생성**
   - `service/accessibility/FocusAccessibilityService.kt` 생성
   - `AccessibilityService` 상속
   - `@AndroidEntryPoint` 어노테이션 적용 (Hilt DI 지원)
   - `onAccessibilityEvent()` 구현
   - `onInterrupt()` 구현
   - `navigateToHome()` 홈 화면 이동 메서드 구현

**주요 기능**:
- **이벤트 감지**: `TYPE_WINDOW_STATE_CHANGED` 이벤트 처리
- **패키지명 확인**: 실행된 앱의 패키지명 추출
- **차단 앱 체크**: 차단 앱 리스트와 비교
- **홈 이동**: `Intent.ACTION_MAIN` + `Intent.CATEGORY_HOME`으로 홈 화면 이동
- **로깅**: 디버그 로그로 동작 추적

#### 1.2.2 앱 차단 로직 (MVP 간소화)

**완료 항목**:
1. ✅ **하드코딩된 차단 앱 리스트**
   ```kotlin
   private val BLOCKED_APPS = setOf(
       "com.instagram.android",           // Instagram
       "com.zhiliaoapp.musically",        // TikTok
       "com.google.android.youtube",      // YouTube
       "com.facebook.katana"              // Facebook
   )
   ```

2. ✅ **타이머 상태 관리**
   - `isTimerRunning` 정적 변수로 타이머 실행 상태 관리
   - `@Volatile` 어노테이션으로 스레드 안전성 보장
   - TODO 주석: Week 2에서 StateFlow로 리팩토링 예정

**MVP 결정**:
- 사용자 커스텀 차단 목록 기능은 1차 릴리스로 연기
- 4개 앱만 하드코딩으로 차단
- 성능 목표: 차단 반응 시간 < 500ms

#### 1.2.3 접근성 서비스 설정

**완료 항목**:
1. ✅ **accessibility_service_config.xml 생성**
   - 위치: `app/src/main/res/xml/accessibility_service_config.xml`
   - 이벤트 타입: `typeWindowStateChanged`
   - 피드백 타입: `feedbackGeneric`
   - 패키지명 필터링: 차단 앱 4개 명시

2. ✅ **AndroidManifest.xml 수정**
   - AccessibilityService 등록
   - `android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"` 설정
   - `android:exported="false"` 설정 (보안)
   - 메타데이터로 설정 파일 연결

3. ✅ **strings.xml 업데이트**
   - 앱 이름: "Allday Detoxy"로 변경
   - 접근성 서비스 설명 문자열 추가
   - 사용자에게 명확한 서비스 목적 설명

### 🔧 기술적 세부사항

#### FocusAccessibilityService.kt 핵심 코드

```kotlin
@AndroidEntryPoint
class FocusAccessibilityService : AccessibilityService() {

    companion object {
        private const val TAG = "FocusAccessibilityService"

        private val BLOCKED_APPS = setOf(
            "com.instagram.android",
            "com.zhiliaoapp.musically",
            "com.google.android.youtube",
            "com.facebook.katana"
        )

        @Volatile
        var isTimerRunning: Boolean = false
    }

    override fun onAccessibilityEvent(event: AccessibilityEvent?) {
        if (event == null) return
        if (!isTimerRunning) return

        if (event.eventType != AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED) return

        val packageName = event.packageName?.toString() ?: return

        if (packageName in BLOCKED_APPS) {
            Log.d(TAG, "Blocked app detected: $packageName")
            navigateToHome()
        }
    }

    private fun navigateToHome() {
        val homeIntent = Intent(Intent.ACTION_MAIN).apply {
            addCategory(Intent.CATEGORY_HOME)
            flags = Intent.FLAG_ACTIVITY_NEW_TASK
        }

        try {
            startActivity(homeIntent)
            Log.d(TAG, "Navigated to home screen")
        } catch (e: Exception) {
            Log.e(TAG, "Failed to navigate to home: ${e.message}", e)
        }
    }
}
```

#### accessibility_service_config.xml

```xml
<?xml version="1.0" encoding="utf-8"?>
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:packageNames="com.instagram.android,com.zhiliaoapp.musically,com.google.android.youtube,com.facebook.katana" />
```

#### AndroidManifest.xml 추가 내용

```xml
<!-- AccessibilityService for app blocking -->
<service
    android:name=".service.accessibility.FocusAccessibilityService"
    android:exported="false"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>
```

### 🏗 생성된 파일 목록

**Kotlin 소스 파일**:
1. `app/src/main/java/com/allday/detoxy/service/accessibility/FocusAccessibilityService.kt`

**XML 리소스 파일**:
1. `app/src/main/res/xml/accessibility_service_config.xml` (신규 생성)
2. `app/src/main/res/values/strings.xml` (수정)

**설정 파일**:
1. `app/src/main/AndroidManifest.xml` (수정)

### ✅ 빌드 검증

**빌드 명령어**:
```bash
./gradlew assembleDebug
```

**빌드 결과**:
```
BUILD SUCCESSFUL in 18s
39 actionable tasks: 21 executed, 18 up-to-date
```

**검증 사항**:
- ✅ Hilt 코드 생성 성공
- ✅ AccessibilityService 컴파일 성공
- ✅ 매니페스트 병합 성공
- ✅ APK 생성 성공

### ⚠️ 주의사항 및 제한사항

1. **권한 설정 필요**
   - 앱 설치 후 사용자가 수동으로 접근성 권한 활성화 필요
   - 설정 → 접근성 → Allday Detoxy → 활성화

2. **타이머 연동 대기**
   - 현재 `isTimerRunning`은 정적 변수로 관리
   - Week 1 Day 4-5에서 타이머 기능 구현 후 연동 필요
   - Week 2에서 StateFlow로 리팩토링 예정

3. **테스트 제한**
   - 실제 기기 또는 에뮬레이터에서만 테스트 가능
   - 접근성 권한이 필요하므로 권한 부여 후 테스트 필요

4. **성능 고려사항**
   - `TYPE_WINDOW_STATE_CHANGED` 이벤트는 빈번하게 발생
   - `isTimerRunning` 체크로 불필요한 처리 최소화
   - 패키지명 필터링으로 이벤트 처리 범위 제한

### 📝 참조 문서
- [MVP 개발 계획](../docs/00_mvp_allday_detoxy_todolist.md) - 전체 작업 계획
- [PRD 문서](../docs/prd.md) - 제품 요구사항 정의서
- [기술 설계](../docs/00_android_allday_detoxy_plan.md) - 상세 기술 설계

### 🚀 다음 작업

#### 후속 작업 (Day 4-5)
1. **1.3 기본 타이머 기능**
   - FocusTimer 클래스 구현
   - 타이머 UI (TimerScreen Compose)
   - 타이머-서비스 연동
   - `FocusAccessibilityService.isTimerRunning` 상태 업데이트

#### 1.2.4 권한 요청 UI (간소화) - 추가 완료

**완료 항목**:
1. ✅ **PermissionUtils 유틸리티 클래스 생성**
   - 위치: `core/utils/PermissionUtils.kt`
   - `isAccessibilityServiceEnabled()`: 접근성 서비스 활성화 여부 확인
   - `openAccessibilitySettings()`: 접근성 설정 화면으로 이동
   - `canDrawOverlays()`: 오버레이 권한 확인 (Week 2 사용 예정)
   - `openOverlaySettings()`: 오버레이 설정 화면으로 이동 (Week 2 사용 예정)

2. ✅ **권한 안내 UI 컴포넌트 생성**
   - 위치: `presentation/ui/permission/PermissionDialog.kt`
   - `AccessibilityPermissionDialog`: 접근성 권한 안내 다이얼로그
   - `PermissionGuideCard`: 간단한 인라인 권한 안내 카드 (MVP 버전)

3. ✅ **MainActivity에 권한 체크 UI 통합**
   - `MainScreen` Composable 생성
   - 권한 상태 체크 로직 추가 (TODO: ViewModel로 이동 예정)
   - 권한 안내 카드 표시
   - 권한 안내 다이얼로그 표시

**주요 기능**:
- **권한 상태 확인**: Settings.Secure API를 통한 접근성 서비스 활성화 확인
- **설정 화면 이동**: Intent를 통한 접근성 설정 화면 열기
- **사용자 안내**: Material3 디자인의 직관적인 권한 안내 UI
- **MVP 간소화**: 복잡한 튜토리얼 없이 간단한 안내와 버튼만 제공

### 📊 작업 진행률
- **1.2.1**: 100% 완료 ✅
- **1.2.2**: 100% 완료 ✅
- **1.2.3**: 100% 완료 ✅
- **1.2.4**: 100% 완료 ✅
- **전체 1.2**: 100% 완료 ✅

### 🏗 추가 생성된 파일 목록 (1.2.4)

**Kotlin 소스 파일**:
1. `core/utils/PermissionUtils.kt` - 권한 체크 및 설정 유틸리티
2. `presentation/ui/permission/PermissionDialog.kt` - 권한 안내 UI 컴포넌트

**수정된 파일**:
1. `MainActivity.kt` - 권한 체크 UI 통합

### ✅ 빌드 검증 (1.2.4)

**빌드 명령어**:
```bash
./gradlew assembleDebug
```

**빌드 결과**:
```
BUILD SUCCESSFUL in 5s
39 actionable tasks: 11 executed, 28 up-to-date
```

**검증 사항**:
- ✅ PermissionUtils 컴파일 성공
- ✅ PermissionDialog 컴파일 성공
- ✅ MainActivity UI 통합 성공
- ✅ Compose 의존성 정상 작동
- ✅ APK 생성 성공

### 🔗 권장 커밋 메시지

프로젝트를 Git에 커밋할 때 사용할 수 있는 커밋 메시지:

```bash
git add .
git commit -m "feat: Add accessibility permission UI and utilities

- Create PermissionUtils for permission check and settings navigation
- Add AccessibilityPermissionDialog for user guidance
- Add PermissionGuideCard for inline permission status display
- Integrate permission check UI in MainActivity
- Implement Settings.Secure API for accessibility service status check
- Add Intent navigation to accessibility settings
- Apply Material3 design for permission UI components
- Verify build success with Gradle

Features:
- isAccessibilityServiceEnabled() - Check accessibility service status
- openAccessibilitySettings() - Navigate to accessibility settings
- AccessibilityPermissionDialog - Full screen permission guide dialog
- PermissionGuideCard - Inline permission status card (MVP version)

TODO: Week 1 Day 4-5 - Connect timer state to isTimerRunning
TODO: Week 2 - Refactor isTimerRunning to StateFlow
TODO: Week 3 - Move permission logic to ViewModel

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

**작업 기록자**: Claude (AI Assistant)
**작업 승인자**: Jung Ho Jang
**마지막 업데이트**: 2025-10-05
