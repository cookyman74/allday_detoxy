# 작업 기록 2025-10-06_3.1

## 작업 정보
- **작업 번호**: 3.1
- **작업명**: 포인트/스트릭 시스템 (Day 11-12)
- **작업 일시**: 2025-10-06
- **작업 상태**: ✅ 완료
- **작업 시간**: 약 45분

## 작업 개요
MVP의 핵심 게임화 시스템인 포인트/스트릭 시스템을 구현했습니다. `GamificationManager` 클래스를 통해 간단한 포인트 계산(1분=1포인트)과 연속 성공일 기반 스트릭 업데이트 로직을 구현했습니다. `FocusRepository` 인터페이스와 구현체를 생성하여 Clean Architecture의 의존성 역전 원칙을 적용했으며, `TimerViewModel`에 세션 저장 및 보상 지급 로직을 통합했습니다.

## 완료된 작업

### 3.1.1 GamificationManager (MVP 간소화) ✅
- **GamificationManager 클래스** (`domain/manager/GamificationManager.kt`)
  - 간단한 포인트 계산: 1분 = 1포인트
  - 스트릭 업데이트 로직:
    - 성공 시: 어제 성공했으면 스트릭 +1, 아니면 스트릭 1로 초기화
    - 실패 시: 스트릭 0으로 초기화
  - LocalDate API 사용으로 날짜 비교
  - Hilt @Singleton으로 의존성 주입 가능

### 3.1.2 세션 완료 시 보상 지급 ✅
- **FocusRepository 인터페이스** (`domain/repository/FocusRepository.kt`)
  - Clean Architecture의 domain 계층 인터페이스
  - 세션 관리: startSession, endSession, getSession, getTodaySessions, getAllSessions
  - 사용자 설정 관리: getSettings, initializeSettings, addPoints, updateStreak, updateSettings
  - Flow 기반 반응형 데이터 스트림

- **FocusRepositoryImpl 구현체** (`data/repository/FocusRepositoryImpl.kt`)
  - Room DAO를 사용하여 실제 데이터베이스 작업 수행
  - FocusSessionDao, UserSettingsDao 의존성 주입
  - @Singleton으로 앱 전체에서 단일 인스턴스 사용

- **RepositoryModule 업데이트** (`core/di/RepositoryModule.kt`)
  - @Binds를 사용하여 인터페이스와 구현체 매핑
  - Clean Architecture의 의존성 역전 원칙(DIP) 구현
  - domain 계층이 data 계층을 직접 의존하지 않도록 추상화

- **TimerViewModel 통합** (`presentation/viewmodel/TimerViewModel.kt`)
  - repository, gamificationManager 의존성 주입
  - currentSessionId 프로퍼티로 현재 세션 추적
  - init 블록에서 UserSettings 초기화 (첫 실행 시)
  - startTimer(): FocusSession 생성 및 저장
  - giveUpTimer(): 세션 실패 처리
  - resetTimer(): 세션 ID 초기화
  - onTimerFinish(): 세션 종료, 포인트 지급, 스트릭 업데이트

## 기술적 구현 세부사항

### 1. GamificationManager
```kotlin
package com.allday.detoxy.domain.manager

import com.allday.detoxy.data.local.entity.UserSettings
import java.time.LocalDate
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class GamificationManager @Inject constructor() {

    fun calculatePoints(durationMinutes: Int): Int {
        return durationMinutes
    }

    fun updateStreak(settings: UserSettings, success: Boolean): UserSettings {
        val today = LocalDate.now().toString()

        return if (success) {
            val yesterday = LocalDate.now().minusDays(1).toString()

            if (settings.lastSuccessDate == yesterday) {
                settings.copy(
                    currentStreak = settings.currentStreak + 1,
                    lastSuccessDate = today
                )
            } else {
                settings.copy(
                    currentStreak = 1,
                    lastSuccessDate = today
                )
            }
        } else {
            settings.copy(currentStreak = 0)
        }
    }
}
```

**특징**:
- MVP 간소화: 1분 = 1포인트 단순 공식
- 연속 성공일 기반 스트릭 계산
- LocalDate로 날짜 비교 정확성 확보
- Immutable 데이터 클래스 copy() 활용

### 2. FocusRepository 인터페이스
```kotlin
package com.allday.detoxy.domain.repository

import com.allday.detoxy.data.local.entity.FocusSession
import com.allday.detoxy.data.local.entity.UserSettings
import kotlinx.coroutines.flow.Flow

interface FocusRepository {

    // FocusSession 관련
    suspend fun startSession(session: FocusSession)
    suspend fun endSession(sessionId: String, success: Boolean, endTime: Long)
    fun getSession(sessionId: String): Flow<FocusSession?>
    fun getTodaySessions(): Flow<List<FocusSession>>
    fun getAllSessions(): Flow<List<FocusSession>>

    // UserSettings 관련
    fun getSettings(): Flow<UserSettings?>
    suspend fun initializeSettings(settings: UserSettings)
    suspend fun addPoints(points: Int)
    suspend fun updateStreak(streak: Int, date: String)
    suspend fun updateSettings(settings: UserSettings)
}
```

**특징**:
- Clean Architecture의 domain 계층 인터페이스
- 세션 및 설정 관리를 위한 추상화된 데이터 접근 계층
- Flow 기반 반응형 데이터 스트림 (Room과 호환)
- suspend 함수로 코루틴 지원

### 3. FocusRepositoryImpl
```kotlin
package com.allday.detoxy.data.repository

import com.allday.detoxy.data.local.dao.FocusSessionDao
import com.allday.detoxy.data.local.dao.UserSettingsDao
import com.allday.detoxy.data.local.entity.FocusSession
import com.allday.detoxy.data.local.entity.UserSettings
import com.allday.detoxy.domain.repository.FocusRepository
import kotlinx.coroutines.flow.Flow
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class FocusRepositoryImpl @Inject constructor(
    private val sessionDao: FocusSessionDao,
    private val settingsDao: UserSettingsDao
) : FocusRepository {

    override suspend fun startSession(session: FocusSession) {
        sessionDao.insert(session)
    }

    override suspend fun endSession(sessionId: String, success: Boolean, endTime: Long) {
        val session = sessionDao.getSessionById(sessionId)
        session.collect { existingSession ->
            existingSession?.let {
                val updatedSession = it.copy(
                    endTime = endTime,
                    success = success
                )
                sessionDao.update(updatedSession)
            }
        }
    }

    override fun getSession(sessionId: String): Flow<FocusSession?> {
        return sessionDao.getSessionById(sessionId)
    }

    override fun getTodaySessions(): Flow<List<FocusSession>> {
        return sessionDao.getTodaySessions()
    }

    override fun getAllSessions(): Flow<List<FocusSession>> {
        return sessionDao.getAllSessions()
    }

    override fun getSettings(): Flow<UserSettings?> {
        return settingsDao.getSettings()
    }

    override suspend fun initializeSettings(settings: UserSettings) {
        settingsDao.insert(settings)
    }

    override suspend fun addPoints(points: Int) {
        settingsDao.addPoints(points)
    }

    override suspend fun updateStreak(streak: Int, date: String) {
        settingsDao.updateStreak(streak, date)
    }

    override suspend fun updateSettings(settings: UserSettings) {
        settingsDao.update(settings)
    }
}
```

**특징**:
- DAO를 사용하여 실제 데이터베이스 작업 수행
- @Singleton으로 앱 전체에서 단일 인스턴스 보장
- Flow 반환으로 실시간 데이터 업데이트 감지
- 세션 종료 시 기존 세션 조회 후 업데이트

### 4. RepositoryModule
```kotlin
package com.allday.detoxy.core.di

import com.allday.detoxy.data.repository.FocusRepositoryImpl
import com.allday.detoxy.domain.repository.FocusRepository
import dagger.Binds
import dagger.Module
import dagger.hilt.InstallIn
import dagger.hilt.components.SingletonComponent
import javax.inject.Singleton

@Module
@InstallIn(SingletonComponent::class)
abstract class RepositoryModule {

    @Binds
    @Singleton
    abstract fun bindFocusRepository(
        impl: FocusRepositoryImpl
    ): FocusRepository
}
```

**특징**:
- @Binds를 사용하여 인터페이스를 구현체로 매핑
- @Provides보다 효율적 (추상 메서드 사용)
- Clean Architecture의 의존성 역전 원칙(DIP) 구현
- domain 계층은 추상화된 인터페이스에만 의존

### 5. TimerViewModel 통합
주요 변경 사항:

**의존성 주입**:
```kotlin
@HiltViewModel
class TimerViewModel @Inject constructor(
    private val application: Application,
    private val repository: FocusRepository,
    private val gamificationManager: GamificationManager
) : ViewModel()
```

**초기화**:
```kotlin
private var currentSessionId: String? = null

init {
    viewModelScope.launch {
        val settings = repository.getSettings().first()
        if (settings == null) {
            repository.initializeSettings(
                UserSettings(
                    id = 1,
                    totalPoints = 0,
                    currentStreak = 0,
                    lastSuccessDate = null
                )
            )
        }
    }
}
```

**타이머 시작 (세션 생성 및 저장)**:
```kotlin
fun startTimer(durationMinutes: Int) {
    // 세션 생성 및 저장
    val sessionId = UUID.randomUUID().toString()
    currentSessionId = sessionId
    viewModelScope.launch {
        repository.startSession(
            FocusSession(
                id = sessionId,
                startTime = System.currentTimeMillis(),
                endTime = null,
                durationMinutes = durationMinutes,
                success = false
            )
        )
    }

    // ... 기존 로직 (AccessibilityService, LockOverlay, DND, Timer 시작)
}
```

**타이머 포기 (세션 실패 처리)**:
```kotlin
fun giveUpTimer() {
    // 세션 종료 처리
    currentSessionId?.let { sessionId ->
        viewModelScope.launch {
            repository.endSession(
                sessionId = sessionId,
                success = false,
                endTime = System.currentTimeMillis()
            )
            currentSessionId = null
        }
    }

    // ... 기존 로직
}
```

**타이머 완료 (포인트 지급 및 스트릭 업데이트)**:
```kotlin
private fun onTimerFinish(success: Boolean) {
    // ... 기존 로직 (AccessibilityService, LockOverlay, DND 비활성화)

    // 세션 종료 및 포인트/스트릭 업데이트
    currentSessionId?.let { sessionId ->
        viewModelScope.launch {
            // 세션 종료
            repository.endSession(
                sessionId = sessionId,
                success = success,
                endTime = System.currentTimeMillis()
            )

            // 성공 시 포인트 지급 및 스트릭 업데이트
            if (success) {
                val settings = repository.getSettings().first()
                settings?.let {
                    // 포인트 계산 및 추가
                    val session = repository.getSession(sessionId).first()
                    session?.let { focusSession ->
                        val points = gamificationManager.calculatePoints(focusSession.durationMinutes)
                        repository.addPoints(points)

                        // 스트릭 업데이트
                        val updatedSettings = gamificationManager.updateStreak(it, success)
                        repository.updateStreak(updatedSettings.currentStreak, updatedSettings.lastSuccessDate ?: "")
                    }
                }
            }

            currentSessionId = null
        }
    }
}
```

**특징**:
- UUID 기반 세션 ID 생성
- viewModelScope.launch로 코루틴 생명주기 관리
- Flow.first()로 최신 데이터 조회
- 성공 시에만 포인트 지급 및 스트릭 업데이트
- 세션 완료 후 currentSessionId null 처리

## 빌드 및 테스트

### 빌드 결과
```bash
./gradlew assembleDebug
```

**초기 빌드 실패**: GamificationManager에 Hilt 주입 어노테이션 누락
```
error: [Dagger/MissingBinding] com.allday.detoxy.domain.manager.GamificationManager cannot be provided without an @Inject constructor or an @Provides-annotated method.
```

**해결**: GamificationManager에 @Singleton, @Inject constructor 추가

**최종 빌드 성공**: ✅ BUILD SUCCESSFUL in 4s

### 정적 분석
- Kotlin 컴파일 경고: 없음 (TimerScreen의 미사용 변수는 기존 코드)
- Hilt 컴파일: 성공
- 의존성 그래프: 정상

## 구현 패턴 및 Best Practices

### 1. Clean Architecture
- **Domain Layer**: FocusRepository 인터페이스, GamificationManager (비즈니스 로직)
- **Data Layer**: FocusRepositoryImpl (데이터 접근 구현)
- **Presentation Layer**: TimerViewModel (UI 상태 관리)
- **의존성 방향**: Presentation → Domain ← Data (DIP 준수)

### 2. Dependency Injection (Hilt)
- @Singleton으로 앱 생명주기 동안 단일 인스턴스
- @Inject constructor로 생성자 주입
- @Binds로 인터페이스-구현체 바인딩 (효율적)
- ViewModel 주입으로 생명주기 안전성 확보

### 3. Reactive Programming (Flow)
- Room DAO의 Flow 반환으로 실시간 데이터 업데이트
- first()로 단일 값 조회 (suspend)
- collect로 스트림 구독 (세션 종료 시)

### 4. 코루틴 활용
- viewModelScope.launch로 ViewModel 생명주기 연동
- suspend 함수로 비동기 작업 명확화
- 메인 스레드 블로킹 없이 DB 작업 수행

### 5. MVP 간소화 전략
- 복잡한 포인트 공식 제외 (1분=1포인트 단순화)
- 뱃지, 레벨, 가중치 등은 1차 릴리스 이후로 연기
- 핵심 기능(세션 저장, 포인트, 스트릭)에 집중

## 다음 단계

### 3.2 일일 리포트 (Day 13)
- [ ] ReportScreen Compose UI
- [ ] ReportViewModel 구현
- [ ] 오늘 세션 통계 표시
- [ ] 현재 스트릭 및 총 포인트 표시

### 향후 개선 사항
- 실제 디바이스 테스트 (세션 생성/종료 플로우 검증)
- UI 테스트 (포인트/스트릭 표시 확인)
- Edge Case 테스트 (앱 종료 시 세션 처리, 날짜 변경 시 스트릭 등)
- 복잡한 포인트 계산 알고리즘 추가 (1차 릴리스 이후)

## 추가 작업 - LockOverlayService 타이머 수정

### 문제 상황
- **증상**: Service 환경에서 Compose UI의 타이머가 업데이트되지 않음
- **원인**: ComposeView가 Service 컨텍스트에서 recomposition을 제대로 트리거하지 못함
- **영향**: 오버레이 화면의 남은 시간이 초기값에서 고정되어 카운트다운되지 않음

### 시도한 해결 방법들
1. **LaunchedEffect with State** - 실패: State 변경이 recomposition을 트리거하지 못함
2. **produceState** - 실패: Service 컨텍스트에서 작동하지 않음
3. **DisposableEffect** - 실패: 동일한 recomposition 문제
4. **InfiniteTransition Animation** - 실패: Service에서 애니메이션이 시작되지 않음
5. **LifecycleService + SavedStateRegistryOwner** - 실패: 여전히 recomposition 안됨
6. **ViewCompositionStrategy** - 실패: Service의 근본적인 한계 해결 못함
7. **View 재생성 방식** - 부분 성공: 작동하지만 화면 깜빡임으로 UX 저하

### 최종 해결책: XML 레이아웃 사용

#### 1. XML 레이아웃 생성 (`res/layout/lock_overlay_layout.xml`)
```xml
<LinearLayout>
    <TextView android:id="@+id/timerText" />
    <ProgressBar android:id="@+id/progressBar" />
    <Button android:id="@+id/giveUpButton" />
</LinearLayout>
```

#### 2. LockOverlayService 수정
- **변경 전**: ComposeView 사용
- **변경 후**: XML 레이아웃 inflate 및 TextView 직접 업데이트

```kotlin
// XML 레이아웃 inflate
val inflater = getSystemService(Context.LAYOUT_INFLATER_SERVICE) as LayoutInflater
overlayView = inflater.inflate(R.layout.lock_overlay_layout, null)

// View 참조
timerTextView = overlayView?.findViewById(R.id.timerText)
progressBar = overlayView?.findViewById(R.id.progressBar)

// 타이머 업데이트 (coroutine)
lifecycleScope.launch {
    while (isActive && currentRemainingSeconds > 0) {
        delay(1000)
        withContext(Dispatchers.Main) {
            currentRemainingSeconds--
            updateTimerDisplay() // TextView.text 직접 업데이트
        }
    }
}
```

#### 3. LockOverlayScreen.kt 파일 유지
- Compose UI 파일은 향후 Activity 기반 오버레이 전환 시 재사용을 위해 유지
- 현재는 사용하지 않음

### 기술적 교훈
1. **Service + Compose 한계**: Service 컨텍스트는 Compose recomposition에 필요한 생명주기를 제공하지 못함
2. **전통적 View 시스템의 안정성**: Service 오버레이에서는 XML 기반 View가 더 안정적
3. **UI 업데이트 방식**: TextView의 직접적인 text 속성 변경은 Service에서도 정상 작동

## 커밋 정보
- **커밋 해시**: 2977c5c
- **커밋 메시지**: 포인트/스트릭 시스템 구현 (3.1)
- **추가 커밋 예정**: LockOverlayService 타이머 수정 (XML 레이아웃 전환)

## 참고 자료
- [00_mvp_allday_detoxy_todolist.md](../docs/00_mvp_allday_detoxy_todolist.md) - Week 3.1
- [2025-10-06_2.3.md](./2025-10-06_2.3.md) - Room 데이터베이스 구현
- Clean Architecture 패턴
- Hilt Dependency Injection
- Kotlin Coroutines & Flow
