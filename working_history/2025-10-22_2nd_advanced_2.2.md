# 2025-10-22: 2차 고도화 - 2.2 AlarmManager 래퍼 클래스

## 📋 작업 개요

**작업 범위**: [02_advanced_autosetting_todolist.md](../docs/02_advanced_autosetting_todolist.md) § 2.2.1 AutoRunAlarmManager 클래스  
**작업 기간**: 2025-10-22  
**담당자**: AI Assistant  
**상태**: ✅ 완료

---

## 🎯 목표

시간 기반 자동 실행을 위한 AlarmManager 래퍼 클래스 구현:
1. **AutoRunAlarmManager 클래스 구현** (정확 알람 스케줄링, 권한 체크)
2. **BroadcastReceiver 구현** (알람 트리거 처리)
3. **AndroidManifest 업데이트** (Receiver 등록, 권한 추가)
4. **Hilt 의존성 주입 설정** (AlarmModule)
5. **빌드 검증** (compileDebugKotlin, assembleDebug)

---

## ✅ 완료된 작업

### 1. AutoRunAlarmManager 클래스 구현

**파일**: `app/src/main/java/com/allday/detoxy/core/manager/AutoRunAlarmManager.kt` (신규, ~350줄)

#### 1.1 주요 기능

**정확 알람 권한 체크 (Android 12+)**:
```kotlin
fun canScheduleExactAlarms(): Boolean {
    return if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S) {
        alarmManager.canScheduleExactAlarms()
    } else {
        true // Android 12 미만은 권한 불필요
    }
}
```
- StateFlow로 권한 상태 노출 (`_canScheduleExactAlarms`)
- UI에서 실시간으로 권한 변화 감지 가능

**알람 스케줄링 (Doze 모드 대응)**:
```kotlin
fun scheduleTimeBasedAutoRun(autoRun: TimeBasedAutoRun): Boolean {
    val nextTriggerTime = calculateNextTriggerTime(autoRun)
    
    if (canScheduleExactAlarms()) {
        // 정확 알람 (±2분 정확도)
        alarmManager.setExactAndAllowWhileIdle(
            AlarmManager.RTC_WAKEUP,
            nextTriggerTime,
            pendingIntent
        )
    } else {
        // 일반 알람 (±15분 정확도)
        alarmManager.setAndAllowWhileIdle(
            AlarmManager.RTC_WAKEUP,
            nextTriggerTime,
            pendingIntent
        )
    }
}
```
- `setExactAndAllowWhileIdle()`: 절전 모드에서도 정확히 실행
- Fallback: 권한 없을 시 `setAndAllowWhileIdle()` 사용

**요일별 다음 트리거 시각 계산**:
```kotlin
private fun calculateNextTriggerTime(autoRun: TimeBasedAutoRun): Long? {
    val enabledDays = parseEnabledDays(autoRun.enabledDays)
    
    // 오늘 또는 내일부터 최대 7일 검색
    for (daysToAdd in 0..6) {
        val checkCalendar = targetCalendar.clone() as Calendar
        checkCalendar.add(Calendar.DAY_OF_YEAR, daysToAdd)
        
        // 요일 확인
        val dayOfWeek = getDayOfWeekString(checkCalendar)
        if (enabledDays.contains(dayOfWeek)) {
            return checkCalendar.timeInMillis
        }
    }
    
    return null
}
```
- enabledDays JSON 파싱: `["MON","TUE","WED"]` → `Set<String>`
- 활성화된 요일 중 가장 가까운 시각 반환
- 과거 시각 스킵

**PendingIntent 생성 (Android 12+ 대응)**:
```kotlin
private fun createPendingIntent(autoRun: TimeBasedAutoRun): PendingIntent {
    val intent = Intent(context, AutoRunAlarmReceiver::class.java).apply {
        action = ACTION_AUTO_RUN_ALARM
        putExtra(EXTRA_AUTO_RUN_ID, autoRun.id)
        putExtra(EXTRA_DURATION_MINUTES, autoRun.durationMinutes)
        putExtra(EXTRA_PRESET_TYPE, autoRun.presetType)
        putExtra(EXTRA_LABEL, autoRun.label)
    }

    val requestCode = autoRun.id.hashCode() % 10000 + REQUEST_CODE_BASE
    
    return PendingIntent.getBroadcast(
        context,
        requestCode,
        intent,
        PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE
    )
}
```
- FLAG_IMMUTABLE 필수 (Android 12+)
- requestCode: ID 해시로 고유값 생성

#### 1.2 Public API

| 메서드 | 설명 | 반환값 |
|--------|------|--------|
| `canScheduleExactAlarms()` | 정확 알람 권한 체크 | Boolean |
| `scheduleTimeBasedAutoRun(autoRun)` | 알람 스케줄링 | Boolean (성공/실패) |
| `cancelTimeBasedAutoRun(autoRunId)` | 알람 취소 | Unit |
| `rescheduleAll(autoRuns)` | 전체 재스케줄링 | Unit |

#### 1.3 StateFlow (반응형 상태)

```kotlin
val canScheduleExactAlarms: StateFlow<Boolean>
```
- UI에서 권한 상태 실시간 관찰 가능
- 권한 변경 시 자동으로 UI 업데이트

---

### 2. AutoRunAlarmReceiver 구현

**파일**: `app/src/main/java/com/allday/detoxy/receiver/AutoRunAlarmReceiver.kt` (신규, ~60줄)

#### 2.1 알람 트리거 처리

```kotlin
class AutoRunAlarmReceiver : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent) {
        if (intent.action != AutoRunAlarmManager.ACTION_AUTO_RUN_ALARM) {
            return
        }

        // Intent에서 autoRun 정보 추출
        val autoRunId = intent.getStringExtra(AutoRunAlarmManager.EXTRA_AUTO_RUN_ID)
        val durationMinutes = intent.getIntExtra(AutoRunAlarmManager.EXTRA_DURATION_MINUTES, 0)
        val presetType = intent.getStringExtra(AutoRunAlarmManager.EXTRA_PRESET_TYPE)
        val label = intent.getStringExtra(AutoRunAlarmManager.EXTRA_LABEL)
        
        // TODO: AutoRunNotificationManager 연동 (Week 2 작업)
        // TODO: AutoRunLog 기록 (Week 2 작업)
        // TODO: 다음 알람 자동 스케줄링
    }
}
```

#### 2.2 향후 구현 예정 (Week 2)

- **AutoRunNotificationManager**: 알림 표시 로직
- **AutoRunLog**: 자동 실행 이력 기록
- **다음 알람 스케줄링**: DB에서 autoRun 다시 가져와 재스케줄링
- **타이머 실행 중 확인**: 중복 실행 방지

---

### 3. AndroidManifest 업데이트

**파일**: `app/src/main/AndroidManifest.xml` (수정)

#### 3.1 권한 추가

```xml
<!-- 정확 알람 권한 (Android 12+, 시간 기반 자동 실행) -->
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

<!-- 부팅 완료 감지 (알람 재등록) -->
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
```

**권한 설명**:
- `SCHEDULE_EXACT_ALARM`: Android 12+ 정확 알람 권한
- `RECEIVE_BOOT_COMPLETED`: 재부팅 시 알람 재등록 (향후 구현)

#### 3.2 Receiver 등록

```xml
<!-- AutoRunAlarmReceiver for time-based auto-run -->
<receiver
    android:name=".receiver.AutoRunAlarmReceiver"
    android:exported="false"
    android:enabled="true">
    <intent-filter>
        <action android:name="com.allday.detoxy.ACTION_AUTO_RUN_ALARM" />
    </intent-filter>
</receiver>
```

**설정 설명**:
- `exported="false"`: 외부 앱에서 호출 불가 (보안)
- `enabled="true"`: 기본 활성화
- Intent Action: `com.allday.detoxy.ACTION_AUTO_RUN_ALARM`

---

### 4. Hilt 의존성 주입 모듈

**파일**: `app/src/main/java/com/allday/detoxy/core/di/AlarmModule.kt` (신규, ~30줄)

```kotlin
@Module
@InstallIn(SingletonComponent::class)
object AlarmModule {

    @Provides
    @Singleton
    fun provideAlarmManager(@ApplicationContext context: Context): AlarmManager {
        return context.getSystemService(Context.ALARM_SERVICE) as AlarmManager
    }
}
```

**의존성 주입**:
- `AlarmManager` → `AutoRunAlarmManager`로 자동 주입
- Singleton 스코프: 앱 전체에서 단일 인스턴스 공유

---

### 5. 빌드 검증

#### 5.1 compileDebugKotlin

```bash
./gradlew compileDebugKotlin
```

**결과**:
```
BUILD SUCCESSFUL in 2s
18 actionable tasks: 2 executed, 16 up-to-date
```
- ✅ Kotlin 컴파일 성공 (0 errors)

#### 5.2 assembleDebug

```bash
./gradlew assembleDebug
```

**결과**:
```
BUILD SUCCESSFUL in 4s
41 actionable tasks: 9 executed, 32 up-to-date
```
- ✅ 전체 빌드 성공 (0 errors)
- ✅ APK 생성 완료

---

## 📊 최종 산출물 요약

### 신규 파일

| 구분 | 파일 | 라인 수 | 상태 |
|------|------|---------|------|
| **Manager** | AutoRunAlarmManager.kt | ~350 | ✅ |
| **Receiver** | AutoRunAlarmReceiver.kt | ~60 | ✅ |
| **Module** | AlarmModule.kt | ~30 | ✅ |
| **작업 기록** | 2025-10-22_2nd_advanced_2.2.md | (현재 파일) | ✅ |
| **합계** | **4개 파일** | **~440줄** | ✅ |

### 수정 파일

| 파일 | 변경 사항 | 상태 |
|------|-----------|------|
| AndroidManifest.xml | 권한 2개 추가, Receiver 1개 등록 | ✅ |
| 02_advanced_autosetting_todolist.md | 2.2.1 섹션 체크리스트 완료 표시 | ✅ |

---

## 🔍 기술적 하이라이트

### 1. Doze 모드 대응

**문제**: Android 6.0+ Doze 모드에서 일반 알람이 지연됨  
**해결**: `setExactAndAllowWhileIdle()` 사용

```kotlin
alarmManager.setExactAndAllowWhileIdle(
    AlarmManager.RTC_WAKEUP,  // 기기 깨우기
    nextTriggerTime,
    pendingIntent
)
```

**효과**:
- 절전 모드에서도 ±2분 정확도 보장
- 사용자 경험 향상 (정확한 시간에 알림)

### 2. 권한 Fallback 전략

**정확 알람 권한 없음**:
```kotlin
if (canScheduleExactAlarms()) {
    alarmManager.setExactAndAllowWhileIdle()  // ±2분
} else {
    alarmManager.setAndAllowWhileIdle()       // ±15분
    Log.w(TAG, "Inexact alarm scheduled (no permission)")
}
```

**장점**:
- 권한 거부 시에도 기능 동작 (degraded mode)
- UI에서 경고 배너 표시 가능 (StateFlow 활용)
- 사용자에게 권한 재요청 유도

### 3. 요일별 알람 계산 로직

**enabledDays JSON 파싱**:
```kotlin
val enabledDays = parseEnabledDays("[\"MON\",\"TUE\",\"WED\"]")
// Result: setOf("MON", "TUE", "WED")
```

**다음 트리거 시각 계산**:
- 오늘부터 최대 7일 검색
- 활성화된 요일만 고려
- 과거 시각 스킵

**예시**:
- 오늘: 금요일 오후 3시
- 설정: 월/수/금 오전 9시
- 다음 트리거: 다음 주 월요일 오전 9시

### 4. PendingIntent 고유 RequestCode

```kotlin
val requestCode = autoRun.id.hashCode() % 10000 + REQUEST_CODE_BASE
```

**설계 이유**:
- autoRunId는 UUID (문자열) → hashCode로 Int 변환
- REQUEST_CODE_BASE (10000): 다른 PendingIntent와 충돌 방지
- % 10000: overflow 방지 (0~9999 범위)

---

## 🎯 다음 단계 (Week 1, Day 6 후반 ~ Day 8)

### 2.2.2 WorkManager 백업 로직 (선택)
- [ ] AutoRunWorker 클래스 구현
- [ ] OneTimeWorkRequest 스케줄링
- [ ] 전략 결정 로직 (AlarmManager vs WorkManager)
- [ ] 정확도 안내 UI

### 2.2.3 정확 알람 권한 UI (Week 2)
- [ ] 권한 요청 다이얼로그
- [ ] 권한 상태 카드 (TimeBasedAutoRunScreen)
- [ ] 설정 화면 이동 Intent

### 2.3 Geofencing 래퍼 클래스 (Day 7-8)
- [ ] AutoRunGeofenceManager 클래스 설계
- [ ] Google Play Services 체크
- [ ] Broadcast Receiver 생성
- [ ] 위치 권한 관리

---

## 🔥 긴급 수정 (리뷰 피드백)

### 리뷰 일자: 2025-10-22 (작업 직후)
**리뷰어 의견**: P0 Critical Bug 발견

#### P0 - Critical: calculateNextTriggerTime() 버그

**문제점**:
```kotlin
// 수정 전 (BUGGY)
for (daysToAdd in 0..6) {  // 총 7일만 확인
    // 오늘 같은 요일인데 시간이 지난 경우
    // → 다음 주 같은 요일(7일 후)을 찾지 못함!
}
```

**시나리오**:
- 오늘: 월요일 오후 3시
- 설정: 월요일 오전 9시
- 재스케줄 시도:
  - 0일 후 (오늘 월요일 09:00): 과거 → skip
  - 1~6일 후 (화~일): 월요일 아님 → 매칭 실패
  - 7일 후 (다음 주 월요일 09:00): **검색 안 함!** → null 반환
- **결과**: 알람 소실, 기능 멈춤

**수정 내역**:
```kotlin
// 수정 후 (FIXED)
for (daysToAdd in 0..13) {  // 최대 13일(2주) 확인
    val checkCalendar = targetCalendar.clone() as Calendar
    checkCalendar.add(Calendar.DAY_OF_YEAR, daysToAdd)
    
    // 현재 시각 이후만 허용 (재스케줄 시 현재 시각은 제외)
    if (checkCalendar.timeInMillis <= now.timeInMillis) {
        continue
    }
    
    // 요일 확인
    val dayOfWeek = getDayOfWeekString(checkCalendar)
    if (enabledDays.contains(dayOfWeek)) {
        Log.d(TAG, "✅ Next trigger found: ${formatTime(checkCalendar.timeInMillis)} ($dayOfWeek)")
        return checkCalendar.timeInMillis
    }
}
```

**개선 사항**:
- ✅ 검색 범위: `0..6` → `0..13` (2주, 충분한 안전 마진)
- ✅ 로그 추가: 다음 트리거 시각 확인 가능
- ✅ 주석 추가: 버그 발생 이유와 해결 방법 명시

#### 추가 개선 사항

**1. parseEnabledDays() 견고성 강화**:
```kotlin
// 수정 전
private fun parseEnabledDays(enabledDaysJson: String): Set<String> {
    return try {
        val regex = Regex(""""(\w+)"""")
        regex.findAll(enabledDaysJson).map { it.groupValues[1] }.toSet()
    } catch (e: Exception) {
        emptySet()
    }
}

// 수정 후
private fun parseEnabledDays(enabledDaysJson: String): Set<String> {
    if (enabledDaysJson.isBlank() || enabledDaysJson == "[]") {
        Log.w(TAG, "⚠️ Empty enabledDays JSON")
        return emptySet()
    }
    
    return try {
        val regex = Regex(""""(\w+)"""")
        regex.findAll(enabledDaysJson)
            .map { it.groupValues[1].uppercase() } // 🆕 대소문자 정규화
            .filter { it in VALID_DAY_CODES }      // 🆕 유효성 검증
            .toSet()
    } catch (e: Exception) {
        Log.e(TAG, "❌ Failed to parse enabledDays: $enabledDaysJson", e)
        emptySet()
    }
}
```

**개선 사항**:
- ✅ 빈 문자열/빈 배열 체크
- ✅ 대소문자 정규화 (`["mon", "MON"]` → `["MON"]`)
- ✅ 유효한 요일만 필터링 (`VALID_DAY_CODES` 상수)
- ✅ 예외 로깅 개선

**2. requestCode 안전성 강화**:
```kotlin
// 수정 전
val requestCode = autoRun.id.hashCode() % 10000 + REQUEST_CODE_BASE
// 문제: UUID hashCode가 음수일 수 있음

// 수정 후
val requestCode = kotlin.math.abs(autoRun.id.hashCode()) % 10000 + REQUEST_CODE_BASE
// 해결: abs()로 항상 양수 보장
```

**개선 사항**:
- ✅ `kotlin.math.abs()` 사용으로 음수 hashCode 대응
- ✅ requestCode 범위: 10000~19999 (충돌 방지)

**3. VALID_DAY_CODES 상수 추가**:
```kotlin
companion object {
    // ...
    private val VALID_DAY_CODES = setOf("MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN")
}
```

---

### 최종 빌드 검증 (수정 후)

```bash
./gradlew compileDebugKotlin
```

**결과**:
```
BUILD SUCCESSFUL in 3s
18 actionable tasks: 3 executed, 15 up-to-date
```
- ✅ Kotlin 컴파일 성공 (0 errors)

---

## 📦 커밋 정보

**커밋 ID**: `c9677a2`  
**브랜치**: `feat/v0.8`  
**커밋 메시지**:
```
feat(alarm): AlarmManager 래퍼 클래스 구현 (시간 기반 자동 실행)

신규 클래스:
- AutoRunAlarmManager: AlarmManager 래퍼, 정확 알람 스케줄링
  - canScheduleExactAlarms(): Android 12+ 권한 체크
  - scheduleTimeBasedAutoRun(): 요일별 알람 스케줄링
  - calculateNextTriggerTime(): 다음 트리거 시각 계산
  - StateFlow 권한 상태 노출
  
- AutoRunAlarmReceiver: BroadcastReceiver
  - Intent에서 autoRun 정보 추출
  - TODO: AutoRunNotificationManager 연동 (Week 2)
  
- AlarmModule: Hilt 의존성 주입
  - AlarmManager 시스템 서비스 제공

AndroidManifest:
- SCHEDULE_EXACT_ALARM 권한 추가 (Android 12+)
- RECEIVE_BOOT_COMPLETED 권한 추가
- AutoRunAlarmReceiver 등록

Doze 모드 대응:
- setExactAndAllowWhileIdle() (±2분 정확도)
- Fallback: setAndAllowWhileIdle() (±15분 정확도)

PendingIntent:
- FLAG_IMMUTABLE (Android 12+ 필수)
- 고유 RequestCode (ID hashCode)

빌드 검증:
- compileDebugKotlin: ✅ SUCCESS (0 errors)
- assembleDebug: ✅ SUCCESS (0 errors)

Week 1 Day 6 완료
작업 기록: working_history/2025-10-22_2nd_advanced_2.2.md
```

---

## ✅ 최종 체크리스트

- [x] AutoRunAlarmManager 클래스 구현 (~350줄)
- [x] 정확 알람 권한 체크 (Android 12+)
- [x] AlarmManager 설정 (setExactAndAllowWhileIdle)
- [x] 요일별 알람 계산 로직
- [x] PendingIntent 생성 (FLAG_IMMUTABLE)
- [x] AutoRunAlarmReceiver BroadcastReceiver 구현 (~60줄)
- [x] AndroidManifest 권한 추가 (2개)
- [x] AndroidManifest Receiver 등록
- [x] AlarmModule Hilt 모듈 생성 (~30줄)
- [x] 빌드 검증 (compileDebugKotlin SUCCESS)
- [x] 빌드 검증 (assembleDebug SUCCESS)
- [x] 작업 기록 작성

---

## 🎓 참고 문서

- [02_advanced_autosetting_prd.md](../docs/02_advanced_autosetting_prd.md) § 4.1 시간 기반 자동 실행
- [02_advanced_autosetting_todolist.md](../docs/02_advanced_autosetting_todolist.md) § 2.2
- [2025-10-21_2nd_advanced_1.2.md](./2025-10-21_2nd_advanced_1.2.md) - AlarmManager vs WorkManager 기술 조사
- [2025-10-22_2nd_advanced_2.1.md](./2025-10-22_2nd_advanced_2.1.md) - Room 마이그레이션 v3→v4
- [Android AlarmManager 문서](https://developer.android.com/training/scheduling/alarms)
- [Exact Alarm Permission](https://developer.android.com/about/versions/12/behavior-changes-12#exact-alarm-permission)

---

**작성자**: AI Assistant  
**작성 일자**: 2025-10-22  
**브랜치**: feat/v0.8  
**버전**: v0.6 (2차 고도화)  
**상태**: ✅ 완료, 빌드 검증 통과


