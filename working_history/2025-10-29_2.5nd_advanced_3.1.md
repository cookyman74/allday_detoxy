# 2.5차 고도화 작업 기록 - Week 3, Day 15-16

## 작업 일자
- 2025-10-29 (화) 오후

## 작업 범위
- Week 3, Day 15-16: ScheduleGroup 사전 연구 (Spike)

---

## 1. 작업 개요

### 1.1 목표
- **v4→v5 마이그레이션 복잡도 평가**: 기존 데이터 무결성 검증, 롤백 전략, 성능 영향도
- **ScheduleGroup 엔티티 설계 검증**: 데이터 모델 및 관계 설계
- **기존 엔티티 확장 설계**: LocationBasedAutoRun, TimeBasedAutoRun 확장 방안

### 1.2 참조 문서
- `docs/02.5_autosetting_todolist.md` §3.1
- `docs/02_advanced_room_migration_strategy.md` (v3→v4 마이그레이션 참조)
- `working_history/2025-10-28_2.5nd_advanced_2.3.md` (이전 작업)

### 1.3 작업 방식
- **Spike(사전 연구)**: 실제 코드 구현 전 설계 검증 및 위험 요소 분석
- **산출물**: 설계 검증 문서, 마이그레이션 전략, 위험 요소 목록

---

## 2. 현재 상태 분석 (v4 기준)

### 2.1 현재 데이터베이스 버전
- **버전**: v4
- **엔티티 수**: 10개
  - FocusSession, UserSettings (v1)
  - FocusInterruption (v2)
  - FocusDistraction, DetoxyRoutineLog, FocusSettings (v3)
  - TimeBasedAutoRun, LocationBasedAutoRun, CustomTimerPreset, AutoRunLog (v4)

### 2.2 자동 실행 관련 엔티티 구조

#### 2.2.1 TimeBasedAutoRun (시간 기반 자동 실행)
```kotlin
@Entity(
    tableName = "time_based_auto_run",
    indices = [
        Index(value = ["hour", "minute"]),
        Index(value = ["isEnabled"])
    ]
)
data class TimeBasedAutoRun(
    @PrimaryKey val id: String,
    val hour: Int,                  // 0-23
    val minute: Int,                // 0-59
    val durationMinutes: Int,
    val presetType: String,
    val enabledDays: String,        // JSON: ["MON", "TUE", ...]
    val label: String?,
    val isEnabled: Boolean,
    val createdAt: Long
)
```

**특징**:
- 요일별 활성화/비활성화 가능 (`enabledDays` JSON 배열)
- 최대 10개까지 등록 가능
- AlarmManager로 예약 관리

#### 2.2.2 LocationBasedAutoRun (위치 기반 자동 실행)
```kotlin
@Entity(
    tableName = "location_based_auto_run",
    indices = [Index(value = ["isEnabled"])]
)
data class LocationBasedAutoRun(
    @PrimaryKey val id: String,
    val label: String,
    val address: String?,
    val latitude: Double,
    val longitude: Double,
    val radiusMeters: Int,
    val durationMinutes: Int,
    val presetType: String,
    val triggerType: String,
    val periodicIntervalMinutes: Int?,
    val dwellTimeMinutes: Int,
    val requiresUserConfirmation: Boolean,
    val isEnabled: Boolean,
    val createdAt: Long
)
```

**특징**:
- Geofencing 기반 자동 실행
- 최대 5개까지 등록 가능 (Android Geofencing API 제한)
- 1회성 트리거 (ENTER) 또는 주기적 트리거 (PERIODIC)

### 2.3 현재 구조의 제한 사항

#### 2.3.1 위치-시간 복합 시나리오 부재
**문제점**:
- 위치 기반은 1회성 또는 주기적 트리거만 가능
- 특정 위치 진입 시 자동으로 여러 시간대에 타이머를 시작하는 시나리오 불가능

**사용자 요구사항** (사용자 리뷰 반영 §12.2):
- "회사에 도착하면 업무 시간표 자동 활성화" (예: 오전 10시, 오후 2시, 오후 4시)
- "위치 이탈 시 시간표 자동 비활성화"
- 반복 진입 시 동일 시간표 자동 재적용

**해결 방안**: ScheduleGroup 도입

---

## 3. ScheduleGroup 엔티티 설계 검증

### 3.1 ScheduleGroup 개념

**목적**: 여러 TimeBasedAutoRun을 그룹으로 묶어 LocationBasedAutoRun과 연계

**핵심 기능**:
1. **시간표 그룹화**: 여러 시간 기반 자동 실행을 하나의 그룹으로 관리
2. **위치 기반 활성화**: 특정 위치 진입 시 그룹 전체 활성화
3. **위치 이탈 비활성화**: 위치 이탈 시 그룹 전체 비활성화
4. **독립 실행 모드**: 시간 기반 자동 실행의 독립 실행 또는 그룹 종속 선택

### 3.2 ScheduleGroup 엔티티 설계

```kotlin
/**
 * 스케줄 그룹 엔티티
 *
 * 여러 TimeBasedAutoRun을 그룹으로 묶어 위치 기반으로 활성화/비활성화합니다.
 *
 * ## 사용 예시
 * 1. "업무 시간표" 그룹 생성
 * 2. 오전 10시, 오후 2시, 오후 4시 TimeBasedAutoRun 추가 (scheduleGroupId 설정)
 * 3. "회사" LocationBasedAutoRun에 linkedScheduleGroupId 설정
 * 4. 회사 진입 → "업무 시간표" 그룹 활성화 → 3개 알람 등록
 * 5. 회사 이탈 → "업무 시간표" 그룹 비활성화 → 3개 알람 취소
 *
 * @property id 고유 ID (UUID)
 * @property name 그룹 이름 (예: "업무 시간표", "공부 루틴")
 * @property description 그룹 설명 (옵션)
 * @property isActive 활성화 여부
 * @property createdAt 생성 시간 (timestamp)
 */
@Entity(
    tableName = "schedule_group",
    indices = [Index(value = ["isActive"])]
)
data class ScheduleGroup(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val name: String,
    val description: String? = null,
    val isActive: Boolean = true,
    val createdAt: Long = System.currentTimeMillis()
)
```

**설계 결정사항**:
- **최대 개수 제한 없음**: 사용자가 원하는 만큼 그룹 생성 가능
- **단순 구조**: 그룹 자체는 최소 정보만 보유 (name, description, isActive)
- **인덱스**: `isActive` 필드 인덱스 (활성화된 그룹만 빠르게 조회)

### 3.3 기존 엔티티 확장 설계

#### 3.3.1 TimeBasedAutoRun 확장
```kotlin
data class TimeBasedAutoRun(
    // ... 기존 필드 ...
    
    /**
     * 연결된 ScheduleGroup ID (v5+)
     *
     * null: 독립 실행 모드 (기본값)
     * UUID: 해당 그룹에 종속
     */
    val scheduleGroupId: String? = null,
    
    /**
     * 독립 실행 여부 (v5+)
     *
     * true: scheduleGroupId와 무관하게 항상 실행 (기본값)
     * false: scheduleGroupId 그룹 활성화 시에만 실행
     *
     * 참고: scheduleGroupId가 null이면 이 필드는 무시됨 (항상 독립 실행)
     */
    val isIndependent: Boolean = true
)
```

**설계 의도**:
- `scheduleGroupId == null`: 기존 동작 유지 (항상 활성화)
- `scheduleGroupId != null && isIndependent == true`: 그룹에 속하지만 독립 실행
- `scheduleGroupId != null && isIndependent == false`: 그룹 활성화 시에만 실행

**마이그레이션 전략**:
- 기존 TimeBasedAutoRun 레코드는 `scheduleGroupId = null, isIndependent = true`로 설정
- 기존 동작에 영향 없음

#### 3.3.2 LocationBasedAutoRun 확장
```kotlin
data class LocationBasedAutoRun(
    // ... 기존 필드 ...
    
    /**
     * 연결된 ScheduleGroup ID (v5+)
     *
     * null: 1회성/주기적 트리거 (기존 동작)
     * UUID: 해당 그룹의 시간표 활성화/비활성화
     */
    val linkedScheduleGroupId: String? = null
)
```

**설계 의도**:
- `linkedScheduleGroupId == null`: 기존 동작 유지 (단순 트리거)
- `linkedScheduleGroupId != null`: 진입 시 그룹 활성화, 이탈 시 그룹 비활성화

**마이그레이션 전략**:
- 기존 LocationBasedAutoRun 레코드는 `linkedScheduleGroupId = null`로 설정
- 기존 동작에 영향 없음

### 3.4 관계 다이어그램

```
ScheduleGroup (1) ─────┐
                       │
                       │ linkedScheduleGroupId
                       ▼
LocationBasedAutoRun (N)
                       
                       
ScheduleGroup (1) ─────┐
                       │
                       │ scheduleGroupId
                       ▼
TimeBasedAutoRun (N)
```

**관계 타입**:
- `ScheduleGroup : LocationBasedAutoRun` = 1:N (하나의 그룹에 여러 위치 연결 가능)
- `ScheduleGroup : TimeBasedAutoRun` = 1:N (하나의 그룹에 여러 시간표 포함 가능)

**참조 무결성**:
- **Soft Reference**: FK 없음 (원본 그룹 삭제 시 자동 실행 설정은 독립 모드로 전환)
- **이유**: 사용자가 그룹을 삭제해도 개별 자동 실행 설정은 유지되어야 함

---

## 4. v4→v5 마이그레이션 복잡도 평가

### 4.1 마이그레이션 범위

#### 4.1.1 신규 테이블
- `schedule_group` 테이블 생성
- 인덱스: `(isActive)`

#### 4.1.2 기존 테이블 확장
- `time_based_auto_run` 테이블:
  - `scheduleGroupId TEXT` 추가 (nullable)
  - `isIndependent INTEGER NOT NULL DEFAULT 1` 추가
  - 인덱스: `(scheduleGroupId)` 추가
  
- `location_based_auto_run` 테이블:
  - `linkedScheduleGroupId TEXT` 추가 (nullable)
  - 인덱스: `(linkedScheduleGroupId)` 추가

### 4.2 복잡도 평가

#### 4.2.1 낮은 복잡도 요소 ✅
1. **신규 테이블 생성**: ScheduleGroup 테이블은 독립적으로 생성 가능
2. **nullable 필드 추가**: 기존 데이터에 영향 없음 (기본값 NULL)
3. **NOT NULL 필드 추가**: `isIndependent`는 DEFAULT 1로 설정 가능
4. **인덱스 생성**: 성능 향상, 데이터 영향 없음

#### 4.2.2 중간 복잡도 요소 ⚠️
1. **Soft Reference 관계**: FK 없이 애플리케이션 레벨에서 관리
   - **장점**: 마이그레이션 단순, 롤백 쉬움
   - **단점**: 참조 무결성은 애플리케이션이 보장해야 함

2. **기존 데이터 마이그레이션**: 모든 기존 레코드에 새 필드 추가
   - **예상 레코드 수**: TimeBasedAutoRun ≤ 100개, LocationBasedAutoRun ≤ 50개 (사용자당)
   - **영향도**: 낮음 (ALTER TABLE 자동 처리)

#### 4.2.3 위험 요소 ⚠️
1. **테스트 복잡도**: GeofenceTransitionsReceiver 확장 로직 테스트 필요
2. **동시성 문제**: 위치 진입/이탈 시 여러 알람 등록/취소 처리
3. **AlarmManager 제한**: Android 13+ 정확한 알람 권한 필요

### 4.3 마이그레이션 SQL 미리보기

```sql
-- 1. ScheduleGroup 테이블 생성
CREATE TABLE IF NOT EXISTS schedule_group (
    id TEXT PRIMARY KEY NOT NULL,
    name TEXT NOT NULL,
    description TEXT,
    isActive INTEGER NOT NULL DEFAULT 1,
    createdAt INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS index_schedule_group_isActive 
ON schedule_group(isActive);

-- 2. LocationBasedAutoRun 확장
ALTER TABLE location_based_auto_run 
ADD COLUMN linkedScheduleGroupId TEXT;

CREATE INDEX IF NOT EXISTS index_location_based_auto_run_linkedScheduleGroupId 
ON location_based_auto_run(linkedScheduleGroupId);

-- 3. TimeBasedAutoRun 확장
ALTER TABLE time_based_auto_run 
ADD COLUMN scheduleGroupId TEXT;

ALTER TABLE time_based_auto_run 
ADD COLUMN isIndependent INTEGER NOT NULL DEFAULT 1;

CREATE INDEX IF NOT EXISTS index_time_based_auto_run_scheduleGroupId 
ON time_based_auto_run(scheduleGroupId);
```

**예상 실행 시간**: < 100ms (사용자당 레코드 수가 적음)

---

## 5. 기존 데이터 무결성 검증

### 5.1 기존 데이터 영향도 분석

#### 5.1.1 TimeBasedAutoRun
- **기존 레코드**: `scheduleGroupId = NULL, isIndependent = 1`
- **영향**: 없음 (기존 동작 유지)
- **검증 방법**: 
  - 마이그레이션 전후 `SELECT COUNT(*)` 동일 확인
  - 기존 레코드의 `isEnabled` 상태 유지 확인

#### 5.1.2 LocationBasedAutoRun
- **기존 레코드**: `linkedScheduleGroupId = NULL`
- **영향**: 없음 (기존 동작 유지)
- **검증 방법**: 
  - 마이그레이션 전후 `SELECT COUNT(*)` 동일 확인
  - 기존 레코드의 `isEnabled` 상태 유지 확인

#### 5.1.3 UserSettings
- **영향**: 없음 (변경 없음)

#### 5.1.4 AutoRunLog
- **영향**: 없음 (변경 없음)

### 5.2 무결성 검증 체크리스트

- [ ] 마이그레이션 전 `time_based_auto_run` 레코드 수
- [ ] 마이그레이션 후 `time_based_auto_run` 레코드 수 (동일 확인)
- [ ] 마이그레이션 전 `location_based_auto_run` 레코드 수
- [ ] 마이그레이션 후 `location_based_auto_run` 레코드 수 (동일 확인)
- [ ] 신규 필드 `scheduleGroupId`, `linkedScheduleGroupId`가 모두 NULL인지 확인
- [ ] 신규 필드 `isIndependent`가 모두 1인지 확인
- [ ] 인덱스 3개 생성 확인
- [ ] `schedule_group` 테이블 생성 확인 (레코드 수 0)

---

## 6. 마이그레이션 롤백 전략

### 6.1 자동 롤백 (Room 기본 동작)

**Room의 마이그레이션 실패 처리**:
```kotlin
Room.databaseBuilder(context, DetoxyDatabase::class.java, "detoxy_database")
    .addMigrations(MIGRATION_4_5)
    // .fallbackToDestructiveMigration() ❌ 사용하지 않음 (데이터 보존)
    .build()
```

- **마이그레이션 실패 시**: Room이 예외 발생 → 앱 크래시 → 사용자에게 안내
- **데이터 보존**: v4 DB 백업 자동 유지 (Room 내부 기능)

### 6.2 수동 롤백 시나리오

#### 6.2.1 시나리오 1: 마이그레이션 성공, 런타임 오류 발견
**증상**: 위치 진입 시 알람 등록 실패, 그룹 활성화 로직 오류

**롤백 방법**:
1. 앱 버전 롤백 (v0.6으로 다운그레이드)
2. Room은 v4 스키마 사용 (v5 필드 무시)
3. **문제점**: v5에서 생성한 ScheduleGroup 데이터는 v4에서 보이지 않음
   - **영향도**: 낮음 (신규 기능이므로 사용자가 적음)

#### 6.2.2 시나리오 2: 마이그레이션 중 실패
**증상**: 앱 실행 불가, DB 오픈 실패

**롤백 방법**:
1. Crashlytics로 마이그레이션 오류 로그 수집
2. 사용자에게 앱 재설치 안내 (데이터 백업 후)
3. **데이터 손실 방지**: v4 DB 백업 파일 복원 (Room 자동 백업)

### 6.3 롤백 방지 전략

#### 6.3.1 마이그레이션 테스트 강화
- [ ] MigrationTest_4_5.kt 작성 (Day 19-20)
- [ ] 다양한 데이터 조합 테스트 (빈 DB, 최대 개수 DB 등)
- [ ] 인덱스 존재 확인
- [ ] 기본값 확인

#### 6.3.2 단계적 배포
1. **알파 테스트**: 내부 테스터 50명 (1주일)
2. **베타 테스트**: 외부 테스터 500명 (1주일)
3. **점진적 배포**: 10% → 50% → 100% (각 3일)

#### 6.3.3 모니터링
- Crashlytics: `migration_4_to_5` 커스텀 키 로깅
- Firebase Analytics: `db_migration_success`, `db_migration_failed` 이벤트

---

## 7. 성능 영향도 측정

### 7.1 마이그레이션 성능

#### 7.1.1 예상 실행 시간
- **ScheduleGroup 테이블 생성**: < 10ms
- **ALTER TABLE 3회**: < 30ms (각 10ms)
- **인덱스 생성 3개**: < 30ms (각 10ms)
- **총 예상 시간**: < 100ms

**근거**:
- SQLite ALTER TABLE은 테이블 크기에 비례
- 사용자당 TimeBasedAutoRun ≤ 10개, LocationBasedAutoRun ≤ 5개
- 인덱스 생성은 레코드 수에 비례 (15개 이하 → 빠름)

#### 7.1.2 메모리 사용량
- **추가 메모리**: 약 1KB 미만
  - ScheduleGroup: 200 bytes/개 × 5개 = 1KB
  - 신규 인덱스: 100 bytes/개 × 3개 = 300 bytes

### 7.2 런타임 성능

#### 7.2.1 조회 성능
**기존 쿼리** (영향 없음):
```sql
SELECT * FROM time_based_auto_run WHERE isEnabled = 1 
ORDER BY hour, minute
```
- **인덱스**: `(isEnabled)`, `(hour, minute)` 사용
- **영향**: 없음 (신규 필드는 조회 조건 아님)

**신규 쿼리** (추가):
```sql
-- 그룹에 속한 시간표 조회
SELECT * FROM time_based_auto_run 
WHERE scheduleGroupId = ? 
ORDER BY hour, minute
```
- **인덱스**: `(scheduleGroupId)` 사용
- **예상 속도**: < 5ms (레코드 ≤ 10개)

```sql
-- 위치에 연결된 그룹 조회
SELECT sg.* FROM schedule_group sg
INNER JOIN location_based_auto_run lbar 
ON lbar.linkedScheduleGroupId = sg.id
WHERE lbar.id = ?
```
- **인덱스**: `(linkedScheduleGroupId)` 사용
- **예상 속도**: < 5ms (1개 레코드)

#### 7.2.2 쓰기 성능
**그룹 생성**:
```sql
INSERT INTO schedule_group (id, name, description, isActive, createdAt)
VALUES (?, ?, ?, 1, ?)
```
- **예상 속도**: < 5ms

**그룹 활성화/비활성화**:
```sql
UPDATE time_based_auto_run 
SET isEnabled = ? 
WHERE scheduleGroupId = ? AND isIndependent = 0
```
- **영향 레코드**: ≤ 10개
- **예상 속도**: < 10ms

### 7.3 AlarmManager 성능

#### 7.3.1 위치 진입 시 알람 등록
**시나리오**: "회사" 위치 진입 → "업무 시간표" 그룹 활성화 → 5개 알람 등록

**예상 처리 시간**:
- 그룹 조회: < 5ms
- 시간표 조회: < 5ms
- AlarmManager 등록 5회: < 50ms (각 10ms)
- **총**: < 100ms

**위험 요소**:
- Android 13+ 정확한 알람 권한 필요
- 권한 없으면 `setExact()` → `set()`로 대체 (정확도 낮음)

#### 7.3.2 위치 이탈 시 알람 취소
**시나리오**: "회사" 위치 이탈 → "업무 시간표" 그룹 비활성화 → 5개 알람 취소

**예상 처리 시간**:
- 그룹 조회: < 5ms
- 시간표 조회: < 5ms
- AlarmManager 취소 5회: < 50ms (각 10ms)
- **총**: < 100ms

### 7.4 배터리 영향도

#### 7.4.1 추가 배터리 소모
- **Geofencing**: 변화 없음 (기존과 동일)
- **AlarmManager**: 증가 (위치 진입 시 추가 알람 등록)
  - 기존: LocationBasedAutoRun 1회 트리거
  - 신규: LocationBasedAutoRun 1회 + 그룹 내 TimeBasedAutoRun N회
  - **예상 증가**: < 1% (알람은 저전력)

#### 7.4.2 배터리 최적화 전략
- **Wake Lock 최소화**: AlarmManager 콜백에서 빠르게 처리 (< 100ms)
- **배치 처리**: 위치 진입 시 알람 5개를 순차 등록 (병렬 아님)
- **불필요한 쿼리 제거**: 그룹 활성화 시 `isIndependent = 0`인 항목만 조회

---

## 8. 설계 결정사항 요약

### 8.1 핵심 결정사항

#### 8.1.1 Soft Reference 관계 (FK 없음)
**결정**: ScheduleGroup 삭제 시 TimeBasedAutoRun/LocationBasedAutoRun은 독립 모드로 전환

**이유**:
- 사용자가 그룹을 삭제해도 개별 자동 실행 설정은 유지되어야 함
- 마이그레이션 롤백이 쉬움
- 애플리케이션 레벨에서 참조 무결성 보장 (ViewModel)

**트레이드오프**:
- 장점: 마이그레이션 단순, 롤백 쉬움
- 단점: 참조 무결성은 코드로 관리 (테스트 필요)

#### 8.1.2 isIndependent 플래그
**결정**: TimeBasedAutoRun에 `isIndependent` 필드 추가

**이유**:
- 그룹에 속하면서도 독립 실행하는 시나리오 지원
- 예: "매일 오전 9시"는 항상 실행 + "업무 시간표"에도 포함

**사용 예시**:
- `scheduleGroupId != null && isIndependent == true`: 항상 실행 (그룹 무관)
- `scheduleGroupId != null && isIndependent == false`: 그룹 활성화 시에만 실행

#### 8.1.3 nullable 필드 사용
**결정**: `scheduleGroupId`, `linkedScheduleGroupId` nullable

**이유**:
- 기존 레코드에 영향 없음 (NULL = 기존 동작)
- 신규 레코드만 그룹 ID 설정
- 마이그레이션 간단 (ALTER TABLE ADD COLUMN)

### 8.2 대안 검토 및 기각

#### 8.2.1 대안 1: Many-to-Many 관계 (중간 테이블)
```sql
CREATE TABLE schedule_group_time_based (
    scheduleGroupId TEXT,
    timeBasedAutoRunId TEXT,
    PRIMARY KEY (scheduleGroupId, timeBasedAutoRunId)
)
```

**기각 이유**:
- 복잡도 증가 (조인 쿼리 필요)
- 마이그레이션 복잡
- 현재 요구사항에는 과도한 설계

#### 8.2.2 대안 2: Foreign Key 사용
```sql
ALTER TABLE time_based_auto_run 
ADD COLUMN scheduleGroupId TEXT,
FOREIGN KEY (scheduleGroupId) REFERENCES schedule_group(id) ON DELETE SET NULL
```

**기각 이유**:
- SQLite는 ALTER TABLE에서 FK 추가 불가
- 테이블 재생성 필요 (복잡한 마이그레이션)
- Soft Reference로 충분

---

## 9. 다음 작업 단계 (Week 3, Day 17-18)

### 9.1 Room 마이그레이션 v4→v5 구현
- [ ] `Migration_4_5.kt` 작성
- [ ] `ScheduleGroup` 엔티티 작성
- [ ] `ScheduleGroupDao` 인터페이스 작성
- [ ] `ScheduleGroupRepository` 구현
- [ ] `LocationBasedAutoRun` 엔티티 확장
- [ ] `TimeBasedAutoRun` 엔티티 확장
- [ ] `DetoxyDatabase` 버전 업데이트 (v4 → v5)

### 9.2 빌드 검증
- [ ] `./gradlew compileDebugKotlin` 성공 확인
- [ ] 컴파일 경고 확인 및 해결

---

## 10. 체크리스트 업데이트

### 10.1 완료된 항목 (§3.1)
- ✅ **v4→v5 마이그레이션 복잡도 평가**
  - 신규 테이블: 1개 (schedule_group)
  - 기존 테이블 확장: 2개 (time_based_auto_run, location_based_auto_run)
  - 복잡도: 낮음 (nullable 필드 추가, 인덱스 생성)
  - 예상 실행 시간: < 100ms
  
- ✅ **기존 데이터 무결성 검증**
  - 기존 레코드 영향 없음 (새 필드 NULL 또는 DEFAULT 1)
  - 검증 체크리스트 작성 (8개 항목)
  
- ✅ **마이그레이션 롤백 전략 수립**
  - 자동 롤백: Room 기본 동작 (마이그레이션 실패 시)
  - 수동 롤백: 앱 버전 다운그레이드 (v0.6)
  - 롤백 방지: 단계적 배포, 모니터링 강화
  
- ✅ **성능 영향도 측정**
  - 마이그레이션: < 100ms (사용자당)
  - 조회 성능: < 5ms (인덱스 활용)
  - 쓰기 성능: < 10ms
  - 배터리 증가: < 1% (알람 추가 등록)

- ✅ **ScheduleGroup 엔티티 설계 검증**
  - 엔티티 설계 완료 (name, description, isActive, createdAt)
  - 관계 다이어그램 작성 (1:N 관계)
  - Soft Reference 관계 (FK 없음)
  
- ✅ **LocationBasedAutoRun 확장 설계**
  - `linkedScheduleGroupId TEXT` 추가 (nullable)
  - 인덱스: `(linkedScheduleGroupId)`
  - 기존 레코드: NULL (기존 동작 유지)
  
- ✅ **TimeBasedAutoRun 확장 설계**
  - `scheduleGroupId TEXT` 추가 (nullable)
  - `isIndependent INTEGER NOT NULL DEFAULT 1` 추가
  - 인덱스: `(scheduleGroupId)`
  - 기존 레코드: NULL + 1 (기존 동작 유지)

### 10.2 다음 단계
- [ ] Migration_4_5.kt 작성 (Day 17)
- [ ] DAO 및 Repository 구현 (Day 17-18)

---

## 11. 기술적 학습 내용

### 11.1 Room 마이그레이션 Best Practices
1. **nullable 필드 사용**: 기존 데이터 영향 최소화
2. **DEFAULT 값 설정**: NOT NULL 필드는 DEFAULT 필수
3. **인덱스 전략**: 조회 조건이 될 필드에만 인덱스 생성
4. **Soft Reference**: 복잡한 관계는 FK 없이 애플리케이션 레벨 관리

### 11.2 SQLite ALTER TABLE 제약사항
- **지원**: ADD COLUMN (nullable or DEFAULT)
- **미지원**: ADD COLUMN with FK, DROP COLUMN, RENAME COLUMN (v3.25.0 이전)
- **대안**: 테이블 재생성 (복잡함, 피하는 것이 좋음)

### 11.3 Android AlarmManager 제한사항
- Android 13+ (API 33): `SCHEDULE_EXACT_ALARM` 권한 필요
- 권한 없으면: `setExact()` → `set()` (부정확)
- 위치 진입 시 여러 알람 등록 → 권한 확인 중요

---

## 12. 작업 시간
- 총 작업 시간: 약 2.5시간
  - 현재 상태 분석: 30분
  - ScheduleGroup 설계 검증: 45분
  - 마이그레이션 복잡도 평가: 30분
  - 무결성 검증 및 롤백 전략: 30분
  - 성능 영향도 측정: 30분
  - 문서화: 15분

---

## 13. 산출물

### 13.1 새로 생성된 파일
1. ✅ `working_history/2025-10-29_2.5nd_advanced_3.1.md` (작업 기록)

### 13.2 수정 예정 파일 (Day 17-18)
1. ⏳ `app/src/main/java/com/allday/detoxy/data/local/entity/ScheduleGroup.kt` (신규)
2. ⏳ `app/src/main/java/com/allday/detoxy/data/local/dao/ScheduleGroupDao.kt` (신규)
3. ⏳ `app/src/main/java/com/allday/detoxy/data/local/migrations/Migration_4_5.kt` (신규)
4. ⏳ `app/src/main/java/com/allday/detoxy/data/local/entity/LocationBasedAutoRun.kt` (확장)
5. ⏳ `app/src/main/java/com/allday/detoxy/data/local/entity/TimeBasedAutoRun.kt` (확장)
6. ⏳ `app/src/main/java/com/allday/detoxy/data/local/DetoxyDatabase.kt` (버전 업데이트)

### 13.3 체크리스트 업데이트 예정
- ⏳ `docs/02.5_autosetting_todolist.md` §3.1 완료 표시

---

## 14. 리뷰 피드백 반영 (2025-10-30 추가)

### 14.1 리뷰 의견 분석

#### 14.1.1 의견 1: Clean Architecture 레이어 정합성 부족
**리뷰 의견**:
- 계획서에 data 레이어(엔티티/DAO)만 언급되어 있음
- domain 레이어 인터페이스(Repository 등)와 data 레이어 구현을 함께 갱신해야 함
- 다음 단계 계획에 "domain repository 인터페이스/사용처 업데이트" 명시 필요

**검토 결과**: ✅ **실제 문제 확인**

**현재 문서 상태**:
- §9.1에 `ScheduleGroupRepository` 구현만 언급
- domain 레이어 인터페이스 정의 누락
- 기존 Repository 인터페이스 확장 방안 누락

#### 14.1.2 의견 2: Soft Reference 참조 무결성 검증 로직 부족
**리뷰 의견**:
- Soft Reference로 관리하기로 결정했지만, 참조 무결성 검증 로직이 명확하지 않음
- ScheduleGroup 삭제 시 연결된 자동 실행 처리 방법 불명확
- 자동 비활성화? groupId null 세팅?

**검토 결과**: ✅ **실제 문제 확인**

**현재 문서 상태**:
- §8.1.1에서 "Soft Reference: FK 없음" 결정만 기술
- 실제 삭제/비활성화 시 처리 로직 누락

#### 14.1.3 의견 3: 이후 구현 시 확인 사항
**리뷰 의견**:
- TimeBasedAutoRun/LocationBasedAutoRun 변경이 기존 테스트·서비스 흐름과 충돌 확인 필요
- v5 DB 버전 업 초기화 시나리오 검증 필요

**검토 결과**: ✅ **실제 문제 확인**

**현재 문서 상태**:
- 기존 서비스(AlarmManager, Geofence) 영향도 분석 부족
- 초기화 시나리오 검증 계획 누락

### 14.2 추가 설계 사항

#### 14.2.1 Clean Architecture 레이어 구조

**domain 레이어 (인터페이스)**:
```kotlin
// domain/repository/ScheduleGroupRepository.kt (인터페이스)
interface ScheduleGroupRepository {
    fun getAll(): Flow<List<ScheduleGroup>>
    fun getById(id: String): Flow<ScheduleGroup?>
    suspend fun insert(scheduleGroup: ScheduleGroup)
    suspend fun update(scheduleGroup: ScheduleGroup)
    suspend fun delete(scheduleGroupId: String)
    suspend fun toggleActive(scheduleGroupId: String, isActive: Boolean)
    
    // 참조 무결성 관리
    suspend fun getLinkedTimeBasedAutoRuns(scheduleGroupId: String): List<TimeBasedAutoRun>
    suspend fun getLinkedLocations(scheduleGroupId: String): List<LocationBasedAutoRun>
    suspend fun unlinkAllAutoRuns(scheduleGroupId: String)
}
```

**data 레이어 (구현)**:
```kotlin
// data/repository/ScheduleGroupRepositoryImpl.kt
class ScheduleGroupRepositoryImpl @Inject constructor(
    private val scheduleGroupDao: ScheduleGroupDao,
    private val timeBasedAutoRunDao: TimeBasedAutoRunDao,
    private val locationBasedAutoRunDao: LocationBasedAutoRunDao
) : ScheduleGroupRepository {
    
    override suspend fun delete(scheduleGroupId: String) {
        // 1. 연결된 자동 실행 조회
        val linkedTimeBasedAutoRuns = getLinkedTimeBasedAutoRuns(scheduleGroupId)
        val linkedLocations = getLinkedLocations(scheduleGroupId)
        
        // 2. 참조 해제 (groupId를 null로 설정)
        unlinkAllAutoRuns(scheduleGroupId)
        
        // 3. 그룹 삭제
        scheduleGroupDao.deleteById(scheduleGroupId)
    }
    
    override suspend fun unlinkAllAutoRuns(scheduleGroupId: String) {
        // TimeBasedAutoRun의 scheduleGroupId를 null로 설정
        timeBasedAutoRunDao.unlinkFromGroup(scheduleGroupId)
        
        // LocationBasedAutoRun의 linkedScheduleGroupId를 null로 설정
        locationBasedAutoRunDao.unlinkFromGroup(scheduleGroupId)
    }
}
```

**기존 Repository 인터페이스 확장**:
```kotlin
// domain/repository/TimeBasedAutoRunRepository.kt (기존 인터페이스 확장)
interface TimeBasedAutoRunRepository {
    // ... 기존 메서드 ...
    
    // v5 추가 메서드
    suspend fun getByScheduleGroup(scheduleGroupId: String): List<TimeBasedAutoRun>
    suspend fun unlinkFromGroup(scheduleGroupId: String)  // scheduleGroupId를 null로 설정
    suspend fun setGroupActive(scheduleGroupId: String, isActive: Boolean)  // 그룹 활성화/비활성화
}

// domain/repository/LocationBasedAutoRunRepository.kt (기존 인터페이스 확장)
interface LocationBasedAutoRunRepository {
    // ... 기존 메서드 ...
    
    // v5 추가 메서드
    suspend fun getByLinkedGroup(scheduleGroupId: String): List<LocationBasedAutoRun>
    suspend fun unlinkFromGroup(scheduleGroupId: String)  // linkedScheduleGroupId를 null로 설정
}
```

#### 14.2.2 Soft Reference 참조 무결성 검증 로직

**ScheduleGroup 삭제 시나리오**:

| 상황 | 처리 방법 | 이유 |
|------|----------|------|
| ScheduleGroup 삭제 | 연결된 TimeBasedAutoRun/LocationBasedAutoRun의 groupId를 null로 설정 | 개별 자동 실행 설정은 유지 (독립 모드로 전환) |
| ScheduleGroup 비활성화 | 연결된 TimeBasedAutoRun의 isEnabled를 false로 설정 (isIndependent == false만) | 그룹 비활성화는 종속 자동 실행만 영향 |
| TimeBasedAutoRun 삭제 | scheduleGroupId 참조만 제거, ScheduleGroup은 유지 | 그룹 자체는 영향 없음 |
| LocationBasedAutoRun 삭제 | linkedScheduleGroupId 참조만 제거, ScheduleGroup은 유지 | 그룹 자체는 영향 없음 |

**DAO 확장 (참조 무결성 관리)**:
```kotlin
// TimeBasedAutoRunDao 확장
@Dao
interface TimeBasedAutoRunDao {
    // ... 기존 메서드 ...
    
    @Query("SELECT * FROM time_based_auto_run WHERE scheduleGroupId = :scheduleGroupId")
    suspend fun getByScheduleGroup(scheduleGroupId: String): List<TimeBasedAutoRun>
    
    @Query("UPDATE time_based_auto_run SET scheduleGroupId = NULL WHERE scheduleGroupId = :scheduleGroupId")
    suspend fun unlinkFromGroup(scheduleGroupId: String)
    
    @Query("""
        UPDATE time_based_auto_run 
        SET isEnabled = :isActive 
        WHERE scheduleGroupId = :scheduleGroupId AND isIndependent = 0
    """)
    suspend fun setGroupActive(scheduleGroupId: String, isActive: Boolean)
}

// LocationBasedAutoRunDao 확장
@Dao
interface LocationBasedAutoRunDao {
    // ... 기존 메서드 ...
    
    @Query("SELECT * FROM location_based_auto_run WHERE linkedScheduleGroupId = :scheduleGroupId")
    suspend fun getByLinkedGroup(scheduleGroupId: String): List<LocationBasedAutoRun>
    
    @Query("UPDATE location_based_auto_run SET linkedScheduleGroupId = NULL WHERE linkedScheduleGroupId = :scheduleGroupId")
    suspend fun unlinkFromGroup(scheduleGroupId: String)
}
```

**ViewModel에서 참조 무결성 보장**:
```kotlin
// ScheduleGroupViewModel
@HiltViewModel
class ScheduleGroupViewModel @Inject constructor(
    private val repository: ScheduleGroupRepository,
    private val alarmManager: AutoRunAlarmManager  // 알람 재스케줄링
) : ViewModel() {
    
    fun deleteScheduleGroup(scheduleGroupId: String) {
        viewModelScope.launch {
            try {
                // 1. 연결된 자동 실행 조회
                val linkedTimeBasedAutoRuns = repository.getLinkedTimeBasedAutoRuns(scheduleGroupId)
                val linkedLocations = repository.getLinkedLocations(scheduleGroupId)
                
                // 2. 참조 해제 (DB)
                repository.unlinkAllAutoRuns(scheduleGroupId)
                
                // 3. 알람 재스케줄링 (종속 자동 실행만)
                linkedTimeBasedAutoRuns
                    .filter { !it.isIndependent }
                    .forEach { alarmManager.cancelAlarm(it.id) }
                
                // 4. 그룹 삭제
                repository.delete(scheduleGroupId)
                
                Log.d(TAG, "✅ ScheduleGroup deleted (id: $scheduleGroupId)")
                
            } catch (e: Exception) {
                _errorState.value = ScheduleGroupError.DeleteError(e.message)
            }
        }
    }
}
```

#### 14.2.3 기존 서비스 영향도 분석

**AlarmManager 영향**:
- **TimeBasedAutoRun 확장**: `scheduleGroupId`, `isIndependent` 필드 추가
  - 기존 로직: `isEnabled == true`인 항목만 알람 등록
  - 변경 로직: `isEnabled == true && (scheduleGroupId == null || isIndependent == true)` 조건 추가
  - **영향**: 기존 알람 등록 로직 수정 필요

**Geofence 영향**:
- **LocationBasedAutoRun 확장**: `linkedScheduleGroupId` 필드 추가
  - 기존 로직: 위치 진입 시 단순 트리거
  - 변경 로직: `linkedScheduleGroupId != null`이면 그룹 활성화 로직 실행
  - **영향**: GeofenceTransitionsReceiver 확장 필요

**Repository 영향**:
- **기존 Repository 메서드**: `getAll()`, `getById()`, `insert()`, `update()`, `delete()` 등
  - **영향**: 없음 (신규 필드는 nullable 또는 DEFAULT 값)
- **신규 Repository 메서드**: `getByScheduleGroup()`, `unlinkFromGroup()` 등
  - **영향**: 인터페이스 확장 필요 (기존 구현에 추가)

### 14.3 업데이트된 다음 작업 단계 (Week 3, Day 17-18)

#### 14.3.1 domain 레이어 (Day 17 오전)
- [ ] **ScheduleGroupRepository 인터페이스** 작성
  - `domain/repository/ScheduleGroupRepository.kt`
  - CRUD 메서드 + 참조 무결성 관리 메서드

- [ ] **기존 Repository 인터페이스 확장**
  - `domain/repository/TimeBasedAutoRunRepository.kt`
    - `getByScheduleGroup()`, `unlinkFromGroup()`, `setGroupActive()` 추가
  - `domain/repository/LocationBasedAutoRunRepository.kt`
    - `getByLinkedGroup()`, `unlinkFromGroup()` 추가

#### 14.3.2 data 레이어 (Day 17 오후)
- [ ] **ScheduleGroup 엔티티** 작성
  - `data/local/entity/ScheduleGroup.kt`

- [ ] **ScheduleGroupDao 인터페이스** 작성
  - `data/local/dao/ScheduleGroupDao.kt`

- [ ] **기존 DAO 확장**
  - `TimeBasedAutoRunDao`: `getByScheduleGroup()`, `unlinkFromGroup()`, `setGroupActive()` 추가
  - `LocationBasedAutoRunDao`: `getByLinkedGroup()`, `unlinkFromGroup()` 추가

- [ ] **ScheduleGroupRepositoryImpl 구현**
  - `data/repository/ScheduleGroupRepositoryImpl.kt`
  - 참조 무결성 로직 포함

- [ ] **기존 Repository 구현 확장**
  - `TimeBasedAutoRunRepositoryImpl`: 신규 메서드 구현
  - `LocationBasedAutoRunRepositoryImpl`: 신규 메서드 구현

#### 14.3.3 data 레이어 - 마이그레이션 (Day 18 오전)
- [ ] **LocationBasedAutoRun 엔티티 확장**
  - `linkedScheduleGroupId TEXT` 추가

- [ ] **TimeBasedAutoRun 엔티티 확장**
  - `scheduleGroupId TEXT`, `isIndependent INTEGER NOT NULL DEFAULT 1` 추가

- [ ] **Migration_4_5.kt 작성**
  - v4→v5 마이그레이션 스크립트

- [ ] **DetoxyDatabase 버전 업데이트**
  - version = 5
  - MIGRATION_4_5 추가

#### 14.3.4 빌드 검증 및 영향도 확인 (Day 18 오후)
- [ ] **컴파일 검증**
  - `./gradlew compileDebugKotlin` 성공 확인
  - 컴파일 경고 확인 및 해결

- [ ] **기존 서비스 영향도 확인**
  - AutoRunAlarmManager: 알람 등록 로직 확인 (isIndependent 조건 추가 필요)
  - GeofenceTransitionsReceiver: 트리거 로직 확인 (linkedScheduleGroupId 처리 필요)
  - 기존 Repository 구현: 신규 메서드 컴파일 확인

- [ ] **초기화 시나리오 검증 계획**
  - v4 DB → v5 DB 마이그레이션 테스트 (Day 19-20)
  - 신규 설치 시 v5 DB 초기화 테스트 (Day 19-20)

### 14.4 설계 결정사항 업데이트

#### 14.4.1 Soft Reference 참조 무결성 정책 (확정)

| 작업 | ScheduleGroup | TimeBasedAutoRun | LocationBasedAutoRun | AlarmManager |
|------|--------------|------------------|----------------------|--------------|
| **ScheduleGroup 삭제** | 삭제 | scheduleGroupId → NULL | linkedScheduleGroupId → NULL | 종속 알람 취소 (isIndependent == false) |
| **ScheduleGroup 비활성화** | isActive → false | isEnabled → false (isIndependent == false만) | 영향 없음 | 종속 알람 취소 |
| **TimeBasedAutoRun 삭제** | 영향 없음 | 삭제 | 영향 없음 | 해당 알람 취소 |
| **LocationBasedAutoRun 삭제** | 영향 없음 | 영향 없음 | 삭제 | Geofence 해제 |

**설계 이유**:
- **사용자 데이터 보존 우선**: 그룹 삭제 시 개별 자동 실행 설정은 유지 (독립 모드로 전환)
- **명시적 참조 해제**: groupId를 null로 설정하여 "그룹 없음" 상태 표현
- **종속 알람만 취소**: 독립 실행 모드(`isIndependent == true`)는 그룹 삭제와 무관하게 계속 실행

#### 14.4.2 Clean Architecture 레이어 책임 분리

| 레이어 | 책임 | 구현 위치 |
|--------|------|----------|
| **domain** | 비즈니스 로직, 인터페이스 정의 | `domain/repository/ScheduleGroupRepository.kt` (인터페이스) |
| **data** | 데이터 저장/조회, Repository 구현 | `data/repository/ScheduleGroupRepositoryImpl.kt` (구현) |
| **presentation** | UI 상태 관리, 사용자 인터랙션 | `presentation/viewmodel/ScheduleGroupViewModel.kt` |

**인터페이스-구현 분리 이유**:
- **테스트 용이성**: domain 레이어 테스트 시 MockRepository 사용 가능
- **의존성 역전**: presentation → domain (인터페이스) ← data (구현)
- **확장성**: 향후 서버 동기화 시 RemoteRepository 구현 추가 가능

### 14.5 기술적 위험 요소 추가 분석

#### 14.5.1 AlarmManager 로직 변경 위험
**문제**: 기존 알람 등록 로직이 `isEnabled == true`만 확인
**변경**: `isEnabled == true && (scheduleGroupId == null || isIndependent == true)` 조건 추가

**영향 범위**:
- `AutoRunAlarmManager.scheduleAlarm()` 메서드
- `AutoRunAlarmManager.cancelAlarm()` 메서드
- `AutoRunAlarmManager.rescheduleAll()` 메서드

**테스트 필요**:
- 기존 자동 실행 (scheduleGroupId == null) 정상 동작 확인
- 독립 실행 모드 (isIndependent == true) 정상 동작 확인
- 종속 모드 (isIndependent == false) 그룹 활성화 시에만 동작 확인

#### 14.5.2 GeofenceTransitionsReceiver 확장 위험
**문제**: 기존 Geofence 트리거 로직이 단순 트리거만 처리
**변경**: `linkedScheduleGroupId != null`이면 그룹 활성화 로직 추가

**영향 범위**:
- `GeofenceTransitionsReceiver.handleGeofenceTrigger()` 메서드
- `GeofenceTransitionsReceiver.handleGeofenceExit()` 메서드 (신규 추가)

**테스트 필요**:
- 기존 위치 기반 (linkedScheduleGroupId == null) 정상 동작 확인
- 그룹 연결 (linkedScheduleGroupId != null) 위치 진입 시 알람 등록 확인
- 그룹 연결 위치 이탈 시 알람 취소 확인

#### 14.5.3 v5 DB 초기화 시나리오
**시나리오 1**: 기존 사용자 (v4 → v5 마이그레이션)
- 기존 TimeBasedAutoRun: `scheduleGroupId = NULL, isIndependent = 1`
- 기존 LocationBasedAutoRun: `linkedScheduleGroupId = NULL`
- **검증**: 마이그레이션 후 기존 동작 유지 확인

**시나리오 2**: 신규 사용자 (v5 신규 설치)
- ScheduleGroup 테이블: 빈 상태
- TimeBasedAutoRun/LocationBasedAutoRun: 기본값으로 생성
- **검증**: 신규 설치 후 정상 동작 확인

**시나리오 3**: 마이그레이션 실패 (v4 유지)
- Room 마이그레이션 예외 발생
- **검증**: v4 DB 백업 복원, 사용자 안내 메시지 표시

### 14.6 체크리스트 업데이트

#### 14.6.1 추가 완료 항목
- ✅ **Clean Architecture 레이어 구조 설계**
  - domain 레이어 인터페이스 정의
  - data 레이어 구현 설계
  - 기존 Repository 인터페이스 확장 방안

- ✅ **Soft Reference 참조 무결성 정책 확정**
  - ScheduleGroup 삭제 시 처리 방법 명확화
  - 연결된 자동 실행 참조 해제 로직 설계
  - ViewModel에서 참조 무결성 보장 방법

- ✅ **기존 서비스 영향도 분석**
  - AlarmManager 로직 변경 필요 사항
  - GeofenceTransitionsReceiver 확장 필요 사항
  - Repository 영향 범위 분석

- ✅ **v5 DB 초기화 시나리오 검증 계획**
  - 기존 사용자 마이그레이션 시나리오
  - 신규 사용자 설치 시나리오
  - 마이그레이션 실패 시나리오

#### 14.6.2 업데이트된 다음 단계
- [ ] domain 레이어 인터페이스 작성 (Day 17 오전)
- [ ] data 레이어 구현 (Day 17 오후 ~ Day 18 오전)
- [ ] 빌드 검증 및 영향도 확인 (Day 18 오후)

### 14.7 참조 문서 업데이트 필요

#### 14.7.1 업데이트 대상
- [ ] `docs/02.5_autosetting_todolist.md` §3.2 상세화
  - domain 레이어 작업 추가
  - 기존 Repository 확장 작업 추가

- [ ] `docs/02_advanced_room_migration_strategy.md` v5 섹션 추가
  - ScheduleGroup 마이그레이션 전략
  - Soft Reference 참조 무결성 정책

---

## 15. 3.2 작업 결과 리뷰 반영 (2025-10-30 추가)

### 15.1 리뷰 의견 분석

#### 15.1.1 주요 이슈: RepositoryModule 바인딩 누락

**리뷰 의견**:
- `RepositoryModule.kt`에 ScheduleGroupRepository에 대한 @Binds/@Provides 바인딩이 없음
- ViewModel에서 ScheduleGroupRepository를 주입하려고 하면 Hilt가 구현체를 찾지 못해 런타임 크래시 발생

**검토 결과**: ✅ **실제 문제 확인**

**문제 원인**:
- ScheduleGroupRepositoryImpl 구현체를 작성했지만, Hilt DI 바인딩을 누락
- RepositoryModule에 @Binds 선언이 없어서 의존성 주입 시 크래시 발생 위험

**해결 방법**:
```kotlin
// app/src/main/java/com/allday/detoxy/core/di/RepositoryModule.kt
@Binds
@Singleton
abstract fun bindScheduleGroupRepository(
    impl: ScheduleGroupRepositoryImpl
): ScheduleGroupRepository
```

**변경 사항**:
1. import 추가:
   - `com.allday.detoxy.data.repository.ScheduleGroupRepositoryImpl`
   - `com.allday.detoxy.domain.repository.ScheduleGroupRepository`

2. @Binds 메서드 추가:
   - `bindScheduleGroupRepository()` 메서드 추가
   - @Singleton 스코프 지정
   - 인터페이스 → 구현체 바인딩

**검증**:
- ✅ `./gradlew compileDebugKotlin` 성공 (6초)
- ✅ `./gradlew test` 성공 (34초, 69 tasks)

#### 15.1.2 추가 관찰 1: domain 인터페이스가 data 엔티티 노출

**리뷰 의견**:
- domain 인터페이스(ScheduleGroupRepository.kt)가 data 계층 엔티티(TimeBasedAutoRun, LocationBasedAutoRun, ScheduleGroup)를 그대로 노출
- Clean Architecture 경계를 더 엄격히 적용하려면 도메인 모델을 별도로 두는 방안 검토 필요

**검토 결과**: ✅ **기존 패턴과 일치 (문제 없음)**

**현재 프로젝트 패턴 확인**:
```kotlin
// 기존 Repository 인터페이스들도 data 엔티티를 직접 사용
interface IAutoRunLogRepository {
    fun getAll(): Flow<List<AutoRunLog>>  // data 엔티티 직접 노출
}

interface IFocusSessionRepository {
    fun getAll(): Flow<List<FocusSession>>  // data 엔티티 직접 노출
}

interface AutoRunSettingsRepository {
    val excludeWeekendsFlow: Flow<Boolean>  // 프리미티브 타입
}
```

**설계 결정**:
- 현재 프로젝트는 **Pragmatic Clean Architecture** 접근 방식 사용
- Room 엔티티를 domain/data 계층 모두에서 공유
- 별도 domain 모델 변환 오버헤드 제거 (중소규모 앱에 적합)

**트레이드오프**:
| 접근 방식 | 장점 | 단점 |
|----------|------|------|
| **현재 방식** (엔티티 공유) | - 보일러플레이트 코드 감소<br>- 빠른 개발 속도<br>- 명확한 데이터 흐름 | - domain-data 결합도 높음<br>- Room 의존성 노출 |
| **Strict Clean** (별도 모델) | - 완전한 계층 분리<br>- 테스트 격리 용이<br>- Room 교체 가능 | - 보일러플레이트 증가<br>- 매핑 오버헤드<br>- 복잡도 증가 |

**결론**: 
- 현재 프로젝트 규모와 팀 상황에서는 **기존 패턴 유지가 적절**
- 향후 서버 동기화 등 외부 데이터 소스 추가 시 재검토

#### 15.1.3 추가 관찰 2: 단위 테스트 실행 필요

**리뷰 의견**:
- 작업 기록에서는 `./gradlew compileDebugKotlin`만 수행
- 마이그레이션·DAO 확장이 많으니 최소한 `./gradlew test` 실행 필요

**검토 결과**: ✅ **타당한 지적**

**조치 사항**:
- `./gradlew test` 실행 완료
- 모든 단위 테스트 통과 (34초 소요)

**테스트 결과**:
```
BUILD SUCCESSFUL in 34s
69 actionable tasks: 25 executed, 44 up-to-date
```

**테스트 범위**:
- Debug 빌드 단위 테스트: ✅ 통과
- Release 빌드 단위 테스트: ✅ 통과
- 기존 테스트 회귀 없음 확인

**주요 테스트 대상**:
- `NextAutoRunCalculatorTest` (15개 테스트)
- `AutoRunStatisticsCalculatorTest` (테스트 케이스 확인)
- 기타 도메인 로직 테스트

### 15.2 추가 검증 완료

#### 15.2.1 컴파일 검증
- ✅ `./gradlew compileDebugKotlin` 성공 (6초)
- ✅ RepositoryModule 바인딩 컴파일 확인
- ✅ 의존성 주입 그래프 정상 생성

#### 15.2.2 단위 테스트 검증
- ✅ `./gradlew test` 성공 (34초)
- ✅ Debug 빌드 테스트 통과
- ✅ Release 빌드 테스트 통과
- ✅ 기존 테스트 회귀 없음

#### 15.2.3 경고 확인
**컴파일 경고** (기능 영향 없음):
- Room schema export 경고 (기존)
- Migration_4_5.kt 파라미터명 경고 (기존)
- 사용하지 않는 파라미터 경고 (기존)

**조치 필요 없음**: 모두 기존 경고이며 신규 작업과 무관

### 15.3 체크리스트 업데이트

#### 15.3.1 추가 완료 항목
- ✅ **RepositoryModule 바인딩 추가**
  - ScheduleGroupRepository @Binds 선언
  - import 구문 추가
  - 컴파일 및 테스트 검증 완료

- ✅ **domain 엔티티 노출 패턴 검증**
  - 기존 프로젝트 패턴 확인
  - Pragmatic Clean Architecture 접근 방식 확인
  - 현재 패턴 유지 결정

- ✅ **단위 테스트 실행 및 검증**
  - ./gradlew test 실행 완료
  - 모든 테스트 통과
  - 기존 테스트 회귀 없음 확인

### 15.4 최종 산출물 업데이트

#### 15.4.1 추가 수정된 파일
1. ✅ `core/di/RepositoryModule.kt` (ScheduleGroupRepository 바인딩 추가)

#### 15.4.2 최종 검증 결과
- ✅ 컴파일 검증: `./gradlew compileDebugKotlin` 성공
- ✅ 단위 테스트: `./gradlew test` 성공 (69 tasks)
- ✅ Hilt DI 그래프: ScheduleGroupRepository 바인딩 정상
- ✅ 기존 기능 회귀: 없음

### 15.5 기술적 학습 내용 추가

#### 15.5.1 Hilt DI 바인딩 체크리스트
1. **인터페이스 작성**: `domain/repository/XxxRepository.kt`
2. **구현체 작성**: `data/repository/XxxRepositoryImpl.kt`
3. **@Inject constructor**: 구현체에 생성자 주입 선언
4. **RepositoryModule 바인딩**: @Binds로 인터페이스-구현체 연결 ⚠️ (누락 주의)
5. **컴파일 검증**: `./gradlew compileDebugKotlin`
6. **테스트 검증**: `./gradlew test`

#### 15.5.2 Clean Architecture 실용적 접근
**엄격한 Clean Architecture**:
- domain 모델 ≠ data 엔티티
- Mapper 계층 필요
- 완전한 격리

**실용적 Clean Architecture** (현재 프로젝트):
- domain 모델 = data 엔티티 (Room Entity 공유)
- 간단한 매핑으로 충분
- 중소규모 앱에 적합

**선택 기준**:
- 팀 크기, 프로젝트 규모, 개발 속도, 유지보수성 고려
- 현재 프로젝트: 실용적 접근이 적절

---

**문서 최종 업데이트**: 2025-10-30 오후  
**작성자**: AI Assistant  
**버전**: 1.2 (3.2 작업 결과 리뷰 반영 완료)

